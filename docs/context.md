# Contexto do Projeto — Carlesso Pilates Frontend

## O que é este projeto

O **Carlesso Pilates Frontend** é uma aplicação web Angular desenvolvida para apoiar a gestão administrativa de um estúdio de pilates. O sistema centraliza o cadastro e controle de pacientes, servindo como ponto de partida para um sistema mais amplo de gestão do estúdio.

A aplicação consome uma API REST que roda localmente em `http://localhost:8080`, construída separadamente (backend Spring Boot, presumidamente). Durante o desenvolvimento, todas as chamadas passam por um proxy do Angular CLI (`/api/*` → `localhost:8080/*`) para contornar restrições de CORS.

---

## Estado Atual (Abril 2026)

O projeto está em fase inicial de desenvolvimento (**MVP**). Commits realizados:

1. Setup inicial do projeto Angular 19
2. Implementação do módulo de pacientes (listagem, formulário, detalhe)
3. Adição da pasta `/docs` com documentação do projeto
4. Correção de CORS via proxy do Angular CLI
5. Implementação de testes unitários e atualização da documentação
6. Alinhamento com API v2: PATCH ativar/inativar, e-mail mutável no PUT
7. Implementação dos módulos de Planos, Pagamentos e Aulas
8. Dockerização do frontend com build Angular multi-stage e Nginx
9. Correção da atualização de profissionais para `PUT /profissionais/{id}`
10. Implementação de filtros na listagem de pacientes por nome, e-mail, CPF, telefone e status
11. Implementação de paginação completa na consulta de pacientes, com resumo de registros, tamanho de página configurável e navegação anterior/próxima
12. Correção da paginação de profissionais para renderizar uma janela máxima de 5 botões de página
13. Validação de parâmetros numéricos de rota para evitar chamadas à API com `NaN`
14. Correção da paginação de pacientes contra resposta aninhada do Spring Boot 3.x (`page.page.*`), com fallback nos metadados para evitar `NaN` no resumo e seletor de tamanho de página em branco
15. Correção da navegação de páginas na listagem de profissionais para ignorar páginas negativas, fora do total retornado ou iguais à página atual
16. Implementação da confirmação de aula realizada com seleção e vínculo do profissional responsável
17. Criação da seção de relatórios e do relatório de pagamento de profissional por período
18. Adição da exportação do relatório de pagamento de profissional em PDF e Excel/XLSX
19. Implementação de autenticação JWT: tela de login, AuthService, AuthInterceptor, AuthGuard, logout na navbar e redirecionamento para login em `401` por token expirado
20. Implementação do relatório de emissão de NFSEs por competência, com filtro de nota anterior emitida e exportação CSV/XLSX
21. Correção da listagem de profissionais para voltar automaticamente à última página válida após inativar o último item de uma página removida pela nova paginação
22. Parametrização visual a partir do Design System Carlesso: inclusão dos protótipos em `assets/`, migração dos tokens para `src/styles/_tokens.scss`, suporte a tema claro/escuro via `data-theme` e densidade via `data-density`
23. Correção da listagem de profissionais para sincronizar `currentPage` e `pageSize` com os metadados retornados pela API (`page.number` e `page.size`)
24. Correção do formulário de planos para reutilizar as constantes de labels exportadas pelo model de planos
25. Correção da listagem de aulas por pagamento para exibir estado de carregamento já durante a busca inicial do pagamento
26. Implementação do dashboard inicial com indicadores consolidados de pacientes, profissionais, pagamentos e aulas
27. Refactor: substituição do `CommonModule` por imports individuais (`NgIf`, `NgFor`, `NgClass`, `DatePipe`, `CurrencyPipe`) em todos os componentes standalone
28. Refactor: reorganização da ordem das rotas em `app.routes.ts` — `pacientes/:id` reagrupada junto às demais rotas de pacientes (após `pacientes/:id/editar`), com adição de testes unitários para verificar o agrupamento e a precedência das rotas estáticas
29. Implementação da tela de Anamnese do paciente, com formulário reativo vinculado a `/pacientes/:pacienteId/anamnese`, criação/edição via `AnamneseService` e link de acesso no detalhe do paciente
30. Implementação da tela de Avaliação Fisioterapêutica do paciente, com formulário reativo vinculado a `/pacientes/:pacienteId/avaliacao-fisioterapeutica`, criação/edição via `AvaliacaoFisioterapeuticaService` e link de acesso no detalhe do paciente
31. Implementação do módulo de Plano de Tratamento do paciente, com listagem por paciente, formulário de criação/edição, ações de encerrar/suspender com confirmação e link de acesso no detalhe do paciente
32. Implementação do módulo de Sessões de Pilates/Fisioterapia do paciente, com listagem por paciente, formulário de criação/edição, ações de realizar/cancelar com confirmação e link de acesso no detalhe do paciente
33. Implementação do módulo de Evolução de Sessão no prontuário do paciente, com formulário de criação/edição vinculado a cada sessão por `/pacientes/:pacienteId/sessoes/:sessaoId/evolucao`, botão de acesso na listagem de sessões e integração com `EvolucaoSessaoService`
34. Implementação do módulo de Reavaliações do paciente, com listagem por paciente, formulário de criação/edição e link de acesso no detalhe do paciente, integrado com `ReavaliacaoService`
35. Ajuste do `AuthService` para armazenar os dados do usuário retornados no login (`id`, `name`, `email`, `role`, `active`) em `localStorage` via chave `currentUser`, com métodos `getCurrentUser()`, `getCurrentUserRole()`, `isAdmin()` e `hasRole()` para recuperação centralizada e limpeza automática no logout
36. Implementação do guard de autorização por role: `roleGuard` (factory function) em `core/guards/role.guard.ts` que bloqueia o acesso com base no perfil do usuário logado; usuário não autenticado ou com sessão local inválida é redirecionado para `/login`, usuário autenticado sem o perfil exigido é redirecionado para `/403`. A tela `ForbiddenComponent` exibe código 403, mensagem orientativa e duas ações: link primário **Ir para o início** (`/`) e botão secundário **Voltar** (`Location.back()` com fallback para `/` quando não há histórico do navegador). As rotas de `profissionais` e `relatorios` passam a exigir perfil `ADMIN`, e os links administrativos da navbar/dashboard são exibidos apenas para administradores
37. Implementação da seção administrativa do frontend em `pages/admin/`: rota base `/admin` com `AdminHomeComponent` (hub) e rotas para gestão de usuários (`/admin/usuarios`, `/admin/usuarios/novo`, `/admin/usuarios/:id/editar`), todas protegidas por `roleGuard(['ADMIN'])`. Link **Administração** adicionado à navbar, exibido apenas para `ADMIN`. Componentes seguem o padrão standalone do projeto e cobrem testes unitários para renderização, listagem, formulário reativo de criação/edição de usuário e validação do parâmetro de rota
38. Criação do `UsuarioAdminService` em `core/services/usuario-admin.service.ts` com métodos tipados para consumir os endpoints administrativos de usuários (`GET /api/users`, `GET /api/users/{id}`, `GET /api/users/me`, `POST /api/users`, `PUT /api/users/{id}`, `DELETE /api/users/{id}`, `PATCH /api/users/{id}/ativar`, `PATCH /api/users/{id}/inativar`, `GET /api/users/roles`). Models definidos em `core/models/usuario-admin.ts` (`UsuarioAdminResponseDTO`, `UsuarioAdminCreateRequestDTO`, `UsuarioAdminUpdateRequestDTO`, `RoleOption`, `ROLE_OPTIONS`, `UsuarioAdminPage`) reutilizando `UserRole` de `auth.ts` e `Page<T>` de `paciente.ts`. Cobertura de 11 testes unitários validando cada chamada HTTP esperada
39. Implementação da listagem administrativa de usuários em `/admin/usuarios` (`UsuarioListComponent`): consome `UsuarioAdminService.listar` com paginação server-side e janela de até 5 botões visíveis, exibe tabela com nome, e-mail, perfil (`ROLE_OPTIONS` como label) e status (Ativo/Inativo) e oferece estados de carregamento, erro e lista vazia. Cabeçalho da tela traz botão **+ Novo Usuário** ligando para `/admin/usuarios/novo` e cada linha expõe ações **Editar** (rota `/admin/usuarios/:id/editar`), **Inativar** ou **Reativar** (conforme `active`) e **Excluir**. Toda ação destrutiva (inativar, reativar, excluir) é confirmada em diálogo modal antes do disparo da requisição, e os botões de inativar/excluir são desativados para o próprio usuário logado, alinhados às regras do backend. A listagem recua automaticamente para a página anterior quando o último item da página atual é removido
40. Implementação do formulário administrativo de usuários em `/admin/usuarios/novo` e `/admin/usuarios/:id/editar` (`UsuarioFormComponent`): usa Reactive Forms com campos `name`, `email`, `password` e `role`, valida nome mínimo, e-mail, senha obrigatória no cadastro e opcional na edição, carrega opções de perfil por `GET /api/users/roles` com fallback para `ROLE_OPTIONS`, busca dados para edição com `GET /api/users/{id}`, salva com `POST /api/users` ou `PUT /api/users/{id}` e retorna para `/admin/usuarios` após sucesso. A tela mantém estados de carregamento, erro, salvamento e identificador inválido, seguindo os estilos globais de formulário e botões.
41. Correção do `authInterceptor` para evitar logout indevido em respostas `401` que não indicam token inválido/expirado. A partir desta mudança o interceptor só dispara `authService.logout()` quando há sessão ativa e a resposta `401` traz indicação explícita de credencial inválida — ou via cabeçalho `WWW-Authenticate: Bearer error="invalid_token" | "expired_token"`, ou via corpo com `code` em `{TOKEN_EXPIRED, INVALID_TOKEN, TOKEN_INVALID}`. Demais `401` (rota inexistente, falha pontual do backend) são propagados ao componente, preservando a sessão do usuário, enquanto o fluxo de token expirado real continua redirecionando para `/login`.
42. Implementação do dark mode com base nos tokens do Design System: o `StylePreferencesService` passa a persistir a preferência de tema e densidade em `localStorage` (chave `carlesso.style-preferences`), carregar a preferência salva no startup e, na ausência dela, usar `prefers-color-scheme` como fallback — sem persistir o valor resolvido pelo fallback, para que a preferência do sistema continue valendo até o usuário escolher explicitamente. O serviço ganha o método `toggleTheme()`, e `setTheme`/`setDensity`/`apply` passam a gravar em `localStorage`. A navbar do `AppComponent` ganha o botão **Tema claro/Tema escuro** (`.btn-tema`), com `aria-label` e `aria-pressed`, que alterna o tema pela interface. O dark mode reutiliza integralmente os tokens já definidos em `[data-theme="dark"]` (`src/styles/_tokens.scss` e `assets/tokens.css`), sem paleta paralela.
43. Implementação da troca de senha do usuário autenticado (`AlterarSenhaComponent`) em `/perfil/alterar-senha`, protegida por `authGuard`: formulário reativo com `senhaAtual`, `novaSenha` e `confirmacaoNovaSenha`, validação local de obrigatoriedade, tamanho mínimo de 8 caracteres na nova senha, confirmação coincidente, nova senha diferente da atual e toggle de visibilidade por campo (`aria-pressed`/`aria-label`). O submit chama `UsuarioAdminService.alterarSenha` (`PUT /api/users/me/senha`); erros com código/campo explícito de `senhaAtual` mapeiam para "Senha atual incorreta." junto ao campo, `401`/`403` sem esse sinal exibem mensagem de autorização/sessão, `400` propaga a mensagem do backend quando disponível, e demais erros caem em mensagem genérica. Após sucesso, a tela exibe confirmação, limpa o formulário, chama `AuthService.clearSession()` e redireciona para `/login`. A navbar exibe o link **Alterar senha** para todo usuário autenticado. Cobertura inclui teste de service (`PUT /api/users/me/senha` com payload esperado) e testes de componente (validações, toggle, sucesso/erros, double-submit).
44. Implementação do consumo da persistência de NFSE emitida por paciente (backend issue #38). Novos models `NotaFiscalEmitidaRequestDTO`/`NotaFiscalEmitidaResponseDTO` em `core/models/nfse-emitida.ts` e o `NfseEmitidaService` (`core/services/nfse-emitida.service.ts`) com `listarPorPaciente` (`GET /api/api/nfse-emitidas/paciente/{pacienteId}`) e `salvar` (`POST /api/api/nfse-emitidas`) — o duplo prefixo `/api/api` segue o mesmo padrão do `RelatorioService`, pois o backend expõe o recurso em `/api/nfse-emitidas` e o proxy/Nginx remove o primeiro `/api`. A tela `/pacientes/:pacienteId/nfse-emitidas` (`PacienteNfseEmitidaListComponent`) lista as notas do paciente ordenadas por data de emissão decrescente, destaca a última NFSE emitida, trata estados de carregamento/erro e exibe estado vazio para pacientes sem nota. As telas `/pacientes/:pacienteId/nfse-emitidas/nova` e `/pacientes/:pacienteId/nfse-emitidas/:id/editar` (`PacienteNfseEmitidaFormComponent`) registram/atualizam uma NFSE com `competencia` (obrigatória, formato `MM/AAAA`) e `dataEmissao` (obrigatória) e os opcionais `numeroNota` (máx. 60), `valor` (≥ 0) e `observacoes`, sanitizando campos vazios para `null` antes do `POST`; o modo de edição pré-preenche a nota localizada na listagem do paciente. A tela de detalhe do paciente ganhou o card **NFSEs Emitidas**. As mensagens de erro são derivadas do corpo do backend via `extrairMensagemErro` (`shared/utils/api-error.ts`). O relatório fiscal de emissão de NFSEs passa a refletir o campo `notaAnteriorEmitida` com base no dado persistido pelo backend, mantendo o contrato e o fluxo atuais da tela inalterados. Cobertura inclui teste do utilitário de erro, do service (`GET`/`POST` com payloads esperados), de listagem (ordenação com desempate por `id`, última nota, estado vazio, erros, identificador inválido) e de formulário (validações, sanitização, modo de edição, navegação e erros).
45. Performance (issue #25): adoção de `ChangeDetectionStrategy.OnPush` nos componentes de listagem `PacienteListComponent`, `ProfissionalListComponent`, `PlanoListComponent`, `PagamentoListComponent` e `AulaListComponent`, alinhando-os ao padrão já usado no `DashboardComponent`. Com a estratégia padrão (`Default`) o Angular roda change detection em todos os componentes a cada evento do browser; com `OnPush` o componente só é verificado quando uma referência de `@Input` muda, quando emite um evento ou quando é marcado explicitamente. Cada componente passou a injetar `ChangeDetectorRef` e a chamar `cdr.markForCheck()` ao final de cada atualização assíncrona de estado (callbacks `next`/`error` das assinaturas de `carregar`, `inativar`, `ativar`, `confirmarPagar`, `realizar`, `carregarProfissionais` e resolução do pagamento na rota de aulas), garantindo que a UI continue refletindo o estado após chamadas à API. Como complemento direto do `OnPush`, as tabelas de cada listagem ganharam `trackBy` (`trackByPaciente`, `trackByProfissional`, `trackByPlano`, `trackByPagamento`, `trackByAula`, todas por `id`), evitando que o Angular destrua e recrie todas as `<tr>` a cada recarga — já que `carregar()` reatribui o array de itens com uma nova referência. As assinaturas HTTP passaram a usar `takeUntilDestroyed(this.destroyRef)`, espelhando integralmente o padrão do `DashboardComponent`. Cobertura adicional valida que cada listagem usa `OnPush` (via o helper de teste `isOnPush`, em `src/testing/onpush.ts`, que isola num único ponto o acesso à API interna `ɵcmp`) e que `markForCheck` é disparado após o carregamento da lista.

46. Correção de UI (issue #90): impedir a quebra de linha dos textos de status `ATIVO`/`INATIVO` (e demais badges) em listagens onde o nome ou campos próximos ocupam mais de uma linha. As células de tabela usam `overflow-wrap: anywhere`, o que permitia que rótulos curtos como `Inativo` quebrassem em colunas estreitas. A correção adiciona `white-space: nowrap` à regra base compartilhada de badges (`.badge`, `.badge-status`, `.status-badge` em `src/styles.scss`), cobrindo as listagens de planos, pagamentos, aulas, sessões e planos de tratamento e as telas de detalhe de pacientes e profissionais (a listagem de profissionais não exibe status), e também às regras de status com escopo de componente (`.status-badge` em `paciente-list.component.scss` e `.status` em `usuario-list.component.scss`). O ajuste é puramente de CSS, preserva o comportamento responsivo das telas e mantém os nomes longos livres para quebrar linha sem afetar o status. Como o `src/styles.scss` global não é carregado no TestBed, a cobertura de teste recai sobre as regras com escopo de componente: testes adicionais validam via `getComputedStyle` que os elementos de status de `PacienteListComponent` e `UsuarioListComponent` usam `white-space: nowrap`.

47. Implementação dos filtros de busca na listagem de profissionais (issue #6): a tela `/profissionais` (`ProfissionalListComponent`) ganhou um formulário de filtros por **Nome**, **E-mail**, **Contrato** (`CLT`/`PJ`/`AUTONOMO`), **% por Aula** (`percentualPagamentoAula`) e **Status** (Todos/Ativos/Inativos), espelhando o padrão já adotado na listagem de pacientes. O `ProfissionalService.listar` passou a aceitar um terceiro parâmetro opcional `filtro: ProfissionalFiltro`, anexando como query params apenas os valores preenchidos (ignora `undefined`, `null` e string vazia), mantendo `page`, `size` e `sort=nome`. O componente monta o filtro a partir do estado de UI (`montarFiltro`): `nome`/`email` são enviados com `trim`, `tipoContrato` e `percentualPagamentoAula` só são incluídos quando informados (percentual convertido com `Number`/`Number.isFinite`) e `ativo` é derivado do status (`ativos`→`true`, `inativos`→`false`, `todos`→omitido). As ações **Buscar** (volta para a página 0) e **Limpar** (restaura os filtros padrão e recarrega) seguem o comportamento da tela de pacientes, e a paginação preserva o filtro ativo porque `carregar()` reaplica `montarFiltro()`. Cobertura adicional valida o envio/omissão dos params no service e a montagem do filtro, reset de página e limpeza no componente.

48. Atualização da identidade da aba do navegador (issue #92): o título em `src/index.html` deixou de exibir o valor padrão do Angular `Carlessopilatesfe` e passou a ser **Carlesso Pilates**, alinhado à marca usada na navbar (`AppComponent`) e na tela de login. O favicon padrão do Angular (`public/favicon.ico`) foi removido e substituído por `public/favicon.svg`, um monograma **CP** em SVG com fundo navy (`--c-tempestade #141f2d`), letras na cor creme (`--c-cloud-dancer #f0ede8`) e fonte serifada do Design System (`Italiana`), seguindo a paleta de `src/styles/_tokens.scss`. O `<link rel="icon">` passou a apontar para `favicon.svg` com `type="image/svg+xml"`. Como `public/` é a pasta de assets configurada em `angular.json`, o novo ícone é empacotado automaticamente no build local/produção.

49. Implementação do fluxo público de recuperação de senha via e-mail (issue #101), contraparte frontend dos endpoints `POST /auth/forgot-password` e `POST /auth/reset-password` do backend (issue carlessopilatesapi#83). Novos DTOs em `core/models/auth.ts` (`ForgotPasswordRequestDTO`, `ResetPasswordRequestDTO`) e métodos em `AuthService` (`forgotPassword(email)` → `POST /api/auth/forgot-password`; `resetPassword(dto)` → `POST /api/auth/reset-password`). Nova tela `/esqueci-senha` (`ForgotPasswordComponent`), standalone, com formulário reativo de `email` (mesma validação do login) que sempre exibe a mesma mensagem genérica de sucesso — sem diferenciar e-mail existente de inexistente, pois o backend retorna `200` por design contra enumeração de usuários — e trata `429` com mensagem de excesso de tentativas. Nova tela `/resetar-senha` (`ResetPasswordComponent`), standalone, que lê o `token` da query string e usa formulário reativo com `novaSenha` (mín. 8 caracteres) e `confirmacaoNovaSenha` (validador de grupo `naoConfere`), toggle de visibilidade por campo, tratamento de token ausente/inválido/expirado/já utilizado (`404`, `410` ou corpo com `code`/`field` de token) com ação **Solicitar novo e-mail** apontando para `/esqueci-senha`, propagação da mensagem do backend em `400` (política de senha) e `429`; após sucesso limpa o formulário e redireciona para `/login?redefinicao=sucesso`. A tela de login (`LoginComponent`) ganhou o link **Esqueci minha senha** e passa a exibir a mensagem de confirmação quando `redefinicao=sucesso` está na query string. Ambas as rotas são públicas (sem `authGuard`), seguindo o padrão de lazy-loading (`loadComponent`); como o usuário não está autenticado, o `authInterceptor` não anexa `Authorization` nem interfere no tratamento de erro dessas chamadas. Cobertura inclui testes de service (`POST` dos dois endpoints com payloads esperados), testes de componente das duas telas novas (validações, mensagens, cenários de erro, double-submit), testes de rotas públicas em `app.routes.spec.ts` e testes do link/banner no `login.component.spec.ts`.

50. Feedback de sucesso unificado (issue #139): a classe `.alert-success` passou a ser estilizada globalmente em `src/styles.scss` dentro do bloco `.alert` (tokens `--c-success`/`--c-success-bg`, mesmo padrão de `-danger`/`-warning`), e as 8 duplicações do estilo nos SCSS de componentes do prontuário (anamnese, avaliação fisioterapêutica, evolução de sessão, sessões lista/formulário, planos de tratamento lista/formulário e reavaliação) foram removidas — com isso a mensagem de sucesso da tela **Alterar senha**, que usava a classe sem estilo, passou a ter o destaque correto. Todas as ocorrências de `.alert-success` nos templates ganharam `role="status"`, garantindo anúncio a leitores de tela. As ações de escrita que recarregavam a lista em silêncio passaram a exibir confirmação de sucesso com dismissão automática após 4 segundos, seguindo o padrão já existente em `PacienteSessaoListComponent` (campo `sucesso` + `successTimer` limpo em `ngOnDestroy`; nos componentes `OnPush`, o callback do timer chama `cdr.markForCheck()`): ativar/inativar paciente (`PacienteListComponent`), inativar plano (`PlanoListComponent`), confirmar pagamento (`PagamentoListComponent`), marcar aula como realizada (`AulaListComponent`) e inativar/reativar/excluir usuário (`UsuarioListComponent`). Cobertura adicional valida, em cada componente, que a mensagem é exibida após a ação bem-sucedida, é renderizada com `role="status"` e desaparece após o timeout.

51. Implementação do diálogo de confirmação compartilhado (issue #140). O `ConfirmarDialogComponent` (`shared/components/confirmar-dialog`), até então um stub gerado, passou a concentrar todos os diálogos de confirmação da aplicação: expõe os inputs `titulo`, `mensagem`, `textoConfirmar`, `textoCancelar`, `variante` (`primaria`/`secundaria`/`perigo`, mapeadas para `btn-primary`/`btn-secondary`/`btn-danger`), `processando`, `confirmarDesabilitado` e `fecharAoClicarFora`, os outputs `confirmar` e `cancelar`, e um `<ng-content>` para conteúdo projetado quando a mensagem não é um texto simples (ex.: nome em negrito nas telas de detalhe e o formulário de data no diálogo de pagamento). A acessibilidade ficou centralizada no componente: `role="dialog"`, `aria-modal="true"`, `aria-labelledby` apontando para o título gerado com id único, foco inicial no botão de confirmação (ou no primeiro elemento focável quando ele começa desabilitado, como no diálogo de pagamento), focus trap em Tab/Shift+Tab, fechamento por Esc e devolução do foco ao elemento disparador no `ngOnDestroy`. Enquanto `processando` está ativo, os botões de confirmar e cancelar ficam desabilitados e Esc não fecha o diálogo, evitando duplo disparo da ação — e **todas** as telas que confirmam ações mantêm um estado de ação em andamento (`acaoEmAndamento`/`acaoEmAndamentoId`) que é bindado nesse input e também protege o método de execução com early return, de modo que um duplo clique não dispara duas requisições. O `aria-describedby` aponta para o corpo do diálogo (mensagem ou conteúdo projetado) e, enquanto aberto, o componente aplica a classe `dialog-aberto` no `body` para travar o scroll do conteúdo de fundo. O fechamento por clique no overlay é o padrão (`fecharAoClicarFora` = `true`), desligado apenas no diálogo de pagamento, onde há formulário preenchido e o clique acidental fora causaria perda de dados. Os 12 diálogos inline duplicados em 9 templates (`paciente-list`, `paciente-detail`, `profissional-list`, `profissional-detail`, `plano-list`, `pagamento-list`, `usuario-list`, `paciente-sessao-list` e `paciente-plano-tratamento-list`) foram migrados para o componente, junto com o markup, os estilos locais de `.dialog`/`.dialog-title` e a plumbing manual de foco (`ngAfterViewChecked` + `previousFocusedElement`) que existia em `paciente-sessao-list` e `paciente-plano-tratamento-list`, e o `@HostListener('document:keydown.escape')` de `usuario-list`.
52. Confirmação e acessibilidade ao marcar aula como realizada (issue #141). O `AulaListComponent` (`/aulas/paciente/:pacienteId` e `/aulas/pagamento/:pagamentoId`) passou a exibir o `ConfirmarDialogComponent` compartilhado antes do `PATCH /aulas/{id}/realizar`, mostrando a data da aula e o profissional selecionado — antes a ação era executada imediatamente, sem confirmação, apesar de vincular o profissional sem desfazer na UI. O fluxo foi dividido em `solicitarRealizar` (valida e abre o diálogo), `confirmarRealizar` (dispara a chamada) e `cancelarConfirmacao`, com o estado `acaoEmAndamento` bindado no input `processando` para bloquear duplo disparo. Ao acionar o botão sem escolher profissional, o `select` da linha é destacado (`is-invalid`/`aria-invalid`, com a mensagem `invalid-feedback` associada por `aria-describedby`) em vez de nada acontecer; selecionar um profissional limpa o destaque e o banner de erro. Quando não há profissionais ativos, um `field-hint` ("Cadastre um profissional ativo para confirmar aulas") explica o botão desabilitado. Cada `select` ganhou `aria-label` descritivo com a data da aula. Cobertura de componente valida a abertura/cancelamento do diálogo, a renderização de data e profissional, o destaque do select sem seleção, o hint com botão desabilitado e o `aria-label` do select.

53. Correção de responsividade da listagem de aulas no celular (issue #155). A tabela do `AulaListComponent` era renderizada sem contêiner de scroll: como cada linha traz um `select` de profissional (`max-width: 240px`) e o botão **Marcar como Realizada** (`white-space: nowrap` global), a largura mínima passava de ~600px e o overflow vazava para a página inteira, fazendo a navbar e o cabeçalho deslizarem lateralmente em viewports de celular (375px/414px). A correção tem duas partes, sem mudança de API ou de fluxo: (1) a tabela passou a ser envolvida por um `div.table-wrap` (classe global de `styles.scss`, `overflow-x: auto`), contendo qualquer scroll dentro do card — mesmo padrão da listagem de pacientes; (2) abaixo de 640px, uma media query no `aula-list.component.scss` transforma a tabela em **cards por aula**: o `thead` é ocultado de forma acessível, cada `tr` vira um card com borda/sombra, cada `td` empilha em bloco com o rótulo da coluna exibido via `::before content: attr(data-label)` (células `Data`, `Status` e `Profissional` receberam `data-label` no template; a célula de ações fica sem rótulo por ficar vazia em aulas já realizadas), e o `select` e o botão de ação passam a ocupar largura total. A mensagem de validação (`invalid-feedback`), o `field-hint` de profissional ativo e o diálogo de confirmação permanecem inalterados e visíveis no layout mobile. Em 375px/414px o `body` deixa de ter scroll horizontal (verificado: `scrollWidth === clientWidth`). Cobertura adicional valida que a tabela está dentro de um `.table-wrap` e que as células de dados carregam os `data-label` do layout empilhado.

54. Colapso da navbar no mobile (issue #156). Abaixo de 768px a navbar deixava de ser uma barra e virava uma coluna com **todos** os itens sempre visíveis (marca + até 5 links de menu + 3 ações), empurrando o conteúdo útil para ~300–400px abaixo da dobra em toda tela autenticada. A `.navbar` passou a ter um botão de disclosure **☰ Menu** (`.navbar-toggle`) e os links (`.navbar-menu`) e ações (`.navbar-actions`) foram agrupados num painel colapsável (`.navbar-collapse`, `id="navbar-collapse"`). O `AppComponent` ganhou o estado `menuAberto` com `toggleMenu()`/`fecharMenu()`; o botão expõe `aria-expanded` (refletindo o estado) e `aria-controls="navbar-collapse"`, e o menu fecha ao clicar em qualquer link ou em **Sair**. No desktop (≥ 769px) o layout permanece idêntico: o `.navbar-collapse` usa `display: contents` — não cria caixa própria, preservando a distribuição em três colunas por `justify-content: space-between` — e o botão `.navbar-toggle` fica `display: none`. Abaixo de 768px o botão aparece, a barra fechada mantém apenas marca + botão (~64px de altura, `flex-wrap`), e ao abrir (`.navbar.is-open .navbar-collapse`) o painel expande em coluna com largura total; links e botões (Alterar senha, Tema, Sair) ganham `min-height: 44px` para toque confortável. É uma mudança de template/CSS/estado local, sem alteração de rotas, serviços ou API; o tema claro/escuro e os `aria-*` do botão de tema permanecem inalterados. Verificado em 375px que a barra fechada mede 64px, o painel fica oculto até o toque, os alvos de menu/ações medem 44px e o `body` não tem scroll horizontal (`scrollWidth === clientWidth`), e em 1280px que o botão fica oculto e menu/ações seguem visíveis. Cobertura adicional no `app.component.spec.ts` valida a ligação `aria-controls`/`id`, o estado inicial fechado (`aria-expanded="false"`), a alternância abrir/fechar, o fechamento ao navegar e ao sair, e a presença de menu, tema, alterar senha e sair dentro do `.navbar-collapse`.

55. Correção da largura dos inputs de formulário no mobile (issue #157). Em telas ≤768px o media query global de `src/styles.scss` agrupava `.page-header` e `.form-row` na mesma regra, aplicando a ambos `flex-direction: column` **e** `align-items: flex-start`. Num flex container em coluna, `align-items: flex-start` faz os itens usarem largura de conteúdo (fit-content) em vez de esticar: os `.form-group` (`flex: 1`) encolhiam e, mesmo com `.form-control { width: 100% }`, os campos renderizavam com ~207px em vez dos ~343px disponíveis em 375px — afetando todos os formulários que usam `.form-row` (paciente, profissional, sessão, plano de tratamento, evolução, reavaliação, NFSE, relatórios e usuário admin). A correção separa o `.form-row` do `.page-header` no media query e remove o `align-items: flex-start` apenas do `.form-row`, deixando-o com o alinhamento padrão `stretch` — os `.form-control` voltam a ocupar 100% da largura do card. O `.page-header` mantém `flex-direction: column` + `align-items: flex-start` (comportamento inalterado), e `.navbar-menu`/`.navbar-actions`, que já tinham blocos próprios, não são tocados. É uma mudança exclusivamente de CSS global, sem alteração de template, componente, rota ou API; em ≥769px os formulários de 2+ colunas permanecem inalterados. Verificado em viewport de 375px que os `.form-control` de um `.form-row` empilhado ocupam toda a largura interna do card (341px, contra ~207px antes). Como o `src/styles.scss` global não é carregado no TestBed, a mudança não tem superfície de teste unitário com escopo de componente (mesmo racional das correções globais das issues #90 e #155).

56. Correção do vazamento horizontal das listagens de Profissionais, Planos, Pagamentos e Usuários no celular (issue #158). As quatro telas (`/profissionais`, `/planos/paciente/:id`, `/pagamentos/paciente/:id` e `/admin/usuarios`) renderizavam a `table.table` diretamente na página, sem o contêiner global de scroll — com 5 a 7 colunas mais os botões de ação (`white-space: nowrap` global no `th`), a largura mínima da tabela ultrapassava a viewport do celular e o `body` inteiro ganhava scroll horizontal, fazendo navbar, cabeçalho e paginação deslizarem junto. Cada tabela passou a ser envolvida por um `div.table-wrap` (classe global de `styles.scss`, `overflow-x: auto`), que recebeu o `*ngIf` antes aplicado ao `<table>`, seguindo o padrão já usado nas listagens de pacientes e aulas. Para o scroll ficar previsível dentro do card, cada tabela ganhou um `min-width` no SCSS do componente (820px em profissionais e usuários, 900px em planos e pagamentos, dimensionados pelo número de colunas), e as células de ação passaram a não quebrar linha (`white-space: nowrap`, e `flex-wrap: nowrap` na `.acoes` de usuários, que tem três botões). É uma mudança apenas de template/CSS, sem alteração de dados, serviço ou API: paginação, cabeçalho, alertas e diálogos de confirmação permanecem na largura da viewport, e as ações de linha continuam acessíveis pelo scroll da tabela. Cobertura adicional nos quatro specs valida que a `table.table` está dentro de um `.table-wrap`, mesmo teste já existente em `aula-list.component.spec.ts`.

57. Tratamento global de `403` no interceptor HTTP (issue #116). Até então apenas o `401` tinha tratamento global; um `403` da API (usuário autenticado, sem permissão para o recurso) chegava ao componente, que podia ou não exibir mensagem — a rota `/403` só era alcançada pelo `roleGuard`, na navegação. Foi criado o `forbiddenInterceptor` (`core/interceptors/forbidden.interceptor.ts`), registrado após o `authInterceptor` em `app.config.ts`, mantendo o fluxo de `401` intacto e sem tocar em `accessToken`/`currentUser` no `localStorage`. O tratamento global só vale para sessões ativas (`authService.isAuthenticated()`, mesma condição do `authInterceptor`): nos endpoints públicos (`/auth/login`, `/auth/forgot-password`, `/auth/reset-password`) um `403` significa "sem login", não "sem permissão", e é propagado para a própria tela exibir sua mensagem, sem banner global nem redirecionamento. A distinção entre "carregamento de tela" e "ação pontual" é feita pelo método HTTP: `403` em `GET` (a forma como as telas carregam seus dados) navega para `/403`; `403` em `POST`/`PUT`/`PATCH`/`DELETE` (ação disparada pelo usuário) exibe a mensagem padrão "Acesso negado: você não tem permissão para realizar esta ação." e mantém o usuário na tela. A mensagem é publicada pelo novo `NotificacaoService` (`core/services/notificacao.service.ts`, signal `notificacao` com `erro()`/`limpar()`) e renderizada pelo `AppComponent` como banner `.alert.alert-danger` com `role="alert"` acima do `router-outlet`, dismissível e limpo automaticamente a cada `NavigationEnd`. Para telas que já tratam `403` localmente e passariam a exibir mensagem duplicada ou navegar sem necessidade, o interceptor respeita o `HttpContextToken` `TRATA_403_LOCALMENTE`: as chamadas do `NfseEmitidaService` (listar/salvar NFSE, cujas telas usam `extrairMensagemErro`) e o `UsuarioAdminService.alterarSenha` marcam o contexto com `true` e ficam fora do tratamento global. Cobertura: `forbidden.interceptor.spec.ts` (redirecionamento em `GET`, mensagem sem navegação em ação pontual, sessão preservada, opt-out via `HttpContext`, indiferença a status não-`403`) e `notificacao.service.spec.ts`.

58. Remarcação rápida de sessão do paciente (issue #117). Remarcar uma sessão exigia abrir o formulário completo de edição em `/pacientes/:pacienteId/sessoes/:id/editar`. A listagem `/pacientes/:pacienteId/sessoes` passou a oferecer a ação **Reagendar** em cada sessão com status `AGENDADA` (sessões `REALIZADA`/`CANCELADA` não exibem o botão). A ação abre o `ConfirmarDialogComponent` com um formulário reativo de um campo `datetime-local`, pré-preenchido com o horário atual da sessão, validado como obrigatório e futuro (validador local `dataHoraFutura`, que compara o valor com `Date.now()`); o input recebe `min` com o instante da abertura, para o seletor nativo do browser já sinalizar a restrição. Enquanto o formulário está inválido o botão de confirmação fica desabilitado (`confirmarDesabilitado`) — e como reagendar uma sessão atrasada é justamente o caso mais comum, o pré-preenchimento com um horário passado marca o campo como `touched` na abertura, exibindo a mensagem "Informe uma data e hora futura." de imediato em vez de deixar o botão apagado sem explicação, e o diálogo não fecha ao clicar fora (`fecharAoClicarFora` = `false`), mesmo padrão do diálogo de pagamento. A confirmação envia apenas `dataHora` em `PUT /api/sessoes/{id}` via `SessaoService.atualizar` — o `toApiUpdate` já traduz o campo para `data`/`horario` e omite os demais —, e a resposta substitui a sessão correspondente no array `sessoes`, atualizando só aquela linha em vez de recarregar a listagem. O sucesso exibe "Sessão reagendada com sucesso." no `.alert-success` com dismissão automática após 4 segundos (helper `exibirSucesso`, reaproveitado pelas ações de realizar/cancelar); o erro usa `extrairMensagemErro` para exibir a mensagem do backend (ex.: conflito de horário) e mantém a sessão original na lista. O estado `acaoEmAndamentoId` já existente protege contra duplo disparo e bloqueia abrir/cancelar o diálogo enquanto há requisição pendente. Mudança exclusivamente de frontend: nenhum endpoint novo, apenas o `PUT /api/sessoes/{id}` já consumido pelo formulário de edição. Cobertura adicional em `paciente-sessao-list.component.spec.ts`: exibição do botão apenas para `AGENDADA`, pré-preenchimento do diálogo, validação de obrigatoriedade e data futura, bloqueio da chamada com formulário inválido, atualização da linha sem recarregar a lista, mensagem amigável do backend e mensagem padrão no erro, e bloqueio de duplo disparo.

59. Busca global na navbar (issue #121). Localizar um paciente exigia navegar até `/pacientes` e preencher os filtros da listagem. O `BuscaGlobalComponent` (`shared/components/busca-global`, standalone e `OnPush`) foi adicionado às `.navbar-actions` do `AppComponent` e reusa os endpoints já paginados: `GET /api/pacientes` (via `PacienteService.listar`) e, apenas para `ADMIN`, `GET /api/profissionais` (via `ProfissionalService.listar`), ambos com `size` reduzido a 5. O campo usa um `FormControl` com `debounceTime(300)` + `distinctUntilChanged` e só dispara a busca a partir de 2 caracteres, evitando uma requisição por tecla e consultas que trariam a base inteira. O termo é classificado pelo formato: valores compostos apenas por dígitos e pontuação de máscara (`.`, `-`, espaço) viram filtro `cpf`; os demais viram filtro `nome`. O termo vai para a API **como digitado**, sem normalização: os filtros `nome`/`cpf` do backend são `LIKE %termo%` (`contemIgnorandoCase` em `PacienteService`/`ProfissionalService`) sobre o valor gravado, e o cadastro persiste o CPF exatamente como preenchido (não há máscara nem normalização no `PacienteRequestDTO`) — reduzir o termo a dígitos faria `12345678901` deixar de casar com um `123.456.789-01` salvo. Como o `LIKE` é parcial, buscar por parte do CPF funciona e o mínimo de 2 caracteres vale também para o termo numérico. Nenhum filtro envia `ativo`, e o `porStatus` do backend trata `ativo == null` como `true`: a busca devolve apenas registros ativos, mesmo padrão das listagens. A requisição de profissionais **não é disparada** para perfis não-`ADMIN` — o backend responderia `403` e o `forbiddenInterceptor` redirecionaria a tela para `/403` — nem quando o termo é um CPF, já que o endpoint não filtra por esse campo. As duas buscas são combinadas com `combineLatest` e cada ramo devolve `{ resultados, falhou }` no `catchError`, em vez de uma lista vazia: assim a falha de um tipo não apaga os resultados do outro **e** uma falha de rede não é confundida com "não existe" — sem nenhum resultado, o dropdown exibe "Não foi possível buscar agora. Tente novamente." em vez de "Nenhum resultado encontrado.". O dropdown segue o padrão ARIA de combobox com `aria-activedescendant`: setas ↑/↓ percorrem a lista (com wrap), Enter abre `/pacientes/:id` ou `/profissionais/:id`, Esc fecha e o clique fora fecha via `HostListener`. O Esc chama `stopPropagation()` porque o `AppComponent` fecha a navbar colapsada em `document:keydown.escape` — sem isso, dispensar o dropdown no mobile fecharia o menu inteiro. As mensagens de estado ficam numa live region `aria-live="polite"` **sempre presente no DOM** (escondida por clip quando vazia, já que leitores de tela só anunciam a mudança de uma região que já existia). O foco no campo é acessível por atalho global: `Ctrl`/`Cmd`+`K` de qualquer lugar e `/` quando o foco não está em outro campo de texto — o `focus()` é adiado com `setTimeout`, porque focar ainda dentro do `keydown` fazia o próprio caractere `/` ser inserido no campo (validado no navegador: o campo abria com `/` já digitado, quebrando a busca seguinte). Ao selecionar um resultado o controle é limpo **emitindo** no stream, para que o `distinctUntilChanged` não engula uma nova digitação do mesmo termo logo em seguida. Mudança exclusivamente de frontend, sem endpoint novo. Cobertura em `busca-global.component.spec.ts`: debounce e termo mínimo, filtro por nome vs. CPF (máscara preservada e busca parcial), ausência de chamada a profissionais para não-`ADMIN` e para termos de CPF, mescla de resultados para `ADMIN`, resiliência à falha de um dos ramos, estado de erro distinto do de lista vazia, live region persistente, navegação por teclado e clique do mouse até o detalhe, Esc com `stopPropagation`, fechamento por clique fora e os dois atalhos de foco; em `app.component.spec.ts`, a presença do `<app-busca-global>` nas `.navbar-actions` apenas com sessão ativa.

60. Indicação da seção ativa na navbar (issue #142). Nenhum link da navbar sinalizava a rota atual — não havia `routerLinkActive` em lugar nenhum do projeto —, então quem estava em `/pacientes/12/sessoes` não via destaque em **Pacientes** e a navegação não comunicava "onde estou". Os links de menu do `AppComponent` (`Início`, `Pacientes` e, para `ADMIN`, `Profissionais`, `Relatórios` e `Administração`) passaram a usar `routerLinkActive="is-active"` com `ariaCurrentWhenActive="page"`, marcando o link da seção atual com a classe `.is-active` e `aria-current="page"` para leitores de tela. O `Início` recebe `[routerLinkActiveOptions]="{ exact: true }"` para acender apenas na rota raiz `/` — sem o `exact`, o casamento por prefixo do `routerLinkActive` deixaria `Início` sempre aceso. Os demais links usam o casamento por prefixo padrão, de modo que rotas filhas mantêm o link da seção ativo (ex.: `/pacientes/:id/sessoes` conserva **Pacientes** destacado). O estilo `.navbar-menu a.is-active` em `src/styles.scss` usa cor literal clara (`#f6f4f0`), `font-weight: 600` e sublinhado com `text-underline-offset` — cores fixas, não tokens, porque o fundo da navbar é sempre `--c-horizonte` (`#374f6c`, não sobrescrito no tema escuro) e os links já usam valores `rgba` literais; assim o contraste do estado ativo fica correto em ambos os temas. Mudança exclusivamente de frontend (template, CSS global e import do `RouterLinkActive`), sem alteração de rotas, serviços ou API. Cobertura adicional em `app.component.spec.ts`: link da seção atual com `.is-active` e `aria-current="page"`, `Início` ativo apenas na rota exata `/` (e sem `aria-current` em outra rota) e persistência do destaque de **Pacientes** em rota filha.

61. Responsividade para tablet na navbar, listagens e formulários (issue #123). O uso na recepção acontece em tablet (~768px a ~1024px, retrato e paisagem), faixa em que o layout ainda seguia o desktop. O problema central era a navbar: o menu colapsável (botão ☰ + painel expansível) só entrava em ação em `≤768px`; entre 769px e 1024px a barra exibia inline a marca, até cinco links (para `ADMIN`) e as três ações mais a busca global, que não cabiam e transbordavam/sobrepunham, gerando scroll horizontal da página. A media query da navbar em `src/styles.scss` — `.navbar`, `.navbar-toggle`, `.navbar-collapse`, `.navbar-menu` e `.navbar-actions` — foi movida de `max-width: 768px` para `max-width: 1024px`, passando a colapsar em toda a faixa de tablet; o `DESKTOP_MIN_WIDTH` do `AppComponent`, que fecha o menu ao voltar para o desktop no `window:resize`, subiu de `769` para `1025` para acompanhar o breakpoint. A busca global (`busca-global.component.scss`) também teve seu ponto de quebra de `768px` para `1024px`, ocupando a largura total dentro do painel colapsado em vez dos 260px fixos do layout de barra. Para toque adequado em tablet, os botões de ação de linha das tabelas (`.table .acoes .btn`/`.btn-sm` e também `.table .acoes-cell .btn`/`.btn-sm`, cobrindo listagens como o `aula-list` que põem a ação direto na célula, sem `.acoes` interno) e os controles de paginação (`.pagination button`), que no desktop usam 32px, passam a ter `min-height: 44px` (e `min-width: 44px` na paginação) na mesma faixa `≤1024px`. As demais regras mobile permanecem em `≤768px`: `.page-header` empilhado, `.form-row` em coluna (grid fluido de 2+ colunas → 1) e a margem do `.container` — entre 769px e 1024px os formulários de múltiplas colunas seguem inalterados. As tabelas já eram contidas pelos contêineres globais de scroll (`.table-responsive`/`.table-wrap`, issue #158), então não houve overflow horizontal de página a corrigir. Tema claro/escuro e densidades não foram tocados. Mudança de CSS global + uma constante do componente; cobertura adicional em `app.component.spec.ts` valida que o menu **permanece aberto** ao redimensionar para uma largura de tablet (1000px), complementando o teste existente que o fecha em `≥1025px` (1200px).

62. Botão "Inativar" da listagem de pacientes em uma única linha (issue #91). Na listagem `/pacientes`, o grupo de ações `.acoes` da célula usava `flex-wrap: wrap` enquanto as demais listagens com o mesmo botão (`profissional-list`, `usuario-list`, `plano-list`) já usam `nowrap`. Com a coluna de ações reservada em 224px (≈192px úteis após o padding da célula) e três botões (`Ver` + `Editar` + `Inativar`/`Ativar`, ~200px somados com os gaps), o último botão quebrava para uma segunda linha, aumentando a altura da linha e deixando a listagem visualmente maior. Em `paciente-list.component.scss`, o `.acoes` passou a `flex-wrap: nowrap` com `white-space: nowrap` (mesmo padrão das outras listagens) e a largura reservada de `.acoes-header`/`.acoes-cell` subiu de 224px para 248px, para os três botões caberem em uma linha; a tabela já tem `min-width: 820px` e rola horizontalmente no mobile via `.table-wrap`, então `nowrap` não gera overflow de página. Mudança exclusivamente de CSS de componente, sem tocar template, serviço, rotas ou API. Cobertura em `paciente-list.component.spec.ts`: o teste do grupo de ações passou a verificar, além da presença dos três botões, que o `.acoes` computa `flex-wrap: nowrap` (garantindo que o "Inativar" não quebra linha).

63. Teclado numérico/telefônico e autocomplete nos campos de formulário no mobile (issue #159). Nenhum input do projeto usava `inputmode` (confirmado por busca global), então no celular CPF, telefone, CEP e competência (`MM/AAAA`) — todos `type="text"` puros por causa das máscaras — abriam o teclado alfabético completo, obrigando a alternar manualmente para números a cada campo e tornando o cadastro pelo celular lento e sujeito a erro. A correção é exclusivamente de atributos HTML, sem tocar validações, componentes ou API: os campos de CPF (`paciente-form`, `profissional-form` e o filtro de `paciente-list`), CEP (`paciente-form`) e competência (`nfse-relatorio` e `paciente-nfse-emitida-form`) receberam `inputmode="numeric"`, mantendo `type="text"` para preservar máscara e pontuação; o telefone (`paciente-form`, `profissional-form` e o filtro de `paciente-list`) passou a `type="tel"` com `autocomplete="tel"`, abrindo o teclado telefônico e permitindo preenchimento automático. Os campos de contato/identificação dos formulários de cadastro também ganharam `autocomplete` semântico: `name` no nome, `email` no e-mail (já `type="email"`) e `postal-code` no CEP. O `inputmode="numeric"` não restringe a digitação (diferente de `type="number"`), então máscaras com pontos, hífen e parênteses continuam digitáveis e as validações existentes (CPF obrigatório, e-mail) permanecem inalteradas. Como o teclado numérico do iOS **não** oferece a tecla `/`, e a competência é validada por `Validators.pattern(/^(0[1-9]|1[0-2])\/\d{4}$/)` (que exige a barra), o campo de competência recebeu uma máscara de digitação: o helper `formatCompetencia` (`shared/utils/competencia-mask.ts`) mantém apenas os dígitos (no máximo MM + AAAA) e insere a `/` automaticamente a partir do terceiro dígito, ligado via `(input)="formatarCompetencia()"` nos dois formulários (`nfse-relatorio` e `paciente-nfse-emitida-form`). Assim o usuário digita só números no celular e o valor resultante (`MM/AAAA`) continua satisfazendo o validador; a máscara não valida o intervalo do mês (segue a cargo do `pattern`). Cobertura adicional verifica, via `querySelector` no DOM renderizado, os atributos aplicados: `paciente-form.component.spec.ts` (nome/e-mail `autocomplete`, CPF/CEP `inputmode="numeric"`, telefone `type="tel"`+`autocomplete="tel"`, CEP `autocomplete="postal-code"`), `profissional-form.component.spec.ts` (mesmos campos do cadastro), `nfse-relatorio.component.spec.ts` e `paciente-nfse-emitida-form.component.spec.ts` (competência `inputmode="numeric"` e máscara `MM/AAAA` a partir de dígitos). O helper `formatCompetencia` tem cobertura própria em `competencia-mask.spec.ts` (dígitos parciais, inserção da barra, remoção de não-dígitos, limite de tamanho e entradas nulas).

64. Filtros recolhíveis no mobile nas listagens de pacientes e profissionais (issue #163). O formulário `.filters` das telas `/pacientes` e `/profissionais` usa `grid-template-columns: repeat(auto-fit, minmax(180px, 1fr))`, que em ≤480px colapsa para 1 coluna: os 5 campos empilhados + os botões **Buscar**/**Limpar** ocupavam a primeira dobra inteira, empurrando o primeiro resultado da listagem para ~600px abaixo em 375px/414px — o caso de uso mobile mais comum (abrir a lista e achar alguém pelo nome) ficava atrás de um painel raramente usado por completo. A correção é somente de UI, sem mudança de API ou de fluxo: cada componente (`PacienteListComponent` e `ProfissionalListComponent`) ganhou o estado local `filtrosAbertos` (fechado por padrão), o método `alternarFiltros()` e o contador `filtrosAtivos()`, que conta os filtros preenchidos ignorando o status padrão `ativos`. No template, um botão de disclosure **Filtros** (`.filtros-toggle`) foi adicionado antes do `<form>`; ele expõe `aria-expanded` (refletindo o estado) e `aria-controls` apontando para o `id` do formulário (`filtros-pacientes`/`filtros-profissionais`), exibe um badge com a contagem de filtros ativos quando `filtrosAtivos() > 0` e um caret que gira ao abrir. O `<form>` recebeu `[class.filtros-recolhidos]="!filtrosAbertos"`. O `buscar()` (chamado também pelo **Limpar** via `limparFiltros()`) passou a fechar o painel (`filtrosAbertos = false`) para que o primeiro resultado apareça na primeira dobra após aplicar. Toda a distinção mobile/desktop é feita por CSS de componente: no desktop `.filtros-toggle` fica `display: none` e o painel permanece sempre aberto (a classe `.filtros-recolhidos` só tem efeito dentro do media query `≤768px`), mantendo o layout desktop **inalterado**; em ≤768px o botão aparece com `min-height: 44px` para toque confortável e o painel fica oculto até o toque. Cobertura adicional em `paciente-list.component.spec.ts` e `profissional-list.component.spec.ts` valida o estado inicial fechado, a alternância abrir/fechar, o fechamento do painel ao buscar, a contagem de filtros ativos (ignorando o status padrão) e, no DOM renderizado, a ligação `aria-controls`/`id`, o `aria-expanded` e o badge de contagem.

65. Resumo de paginação e seletor de itens por página consistentes em Profissionais e Usuários (issue #144). As telas `/profissionais` (`ProfissionalListComponent`) e `/admin/usuarios` (`UsuarioListComponent`) tinham paginação server-side com janela de 5 botões, mas — ao contrário da listagem de pacientes — não exibiam nem o resumo "Exibindo X–Y de Z" nem o seletor de itens por página, apesar de já receberem os mesmos metadados `page.*` (incluindo `totalElements`). Para eliminar a triplicação do bloco `.pagination-summary` (que existia inline em `paciente-list`), o markup/lógica de apresentação foi extraído para o componente compartilhado `PaginationSummaryComponent` (`shared/components/pagination-summary`, standalone e `OnPush`, mesmo padrão do `ConfirmarDialogComponent` da issue #140). O componente é puramente de apresentação: recebe `currentPage`, `pageSize`, `totalElements`, `itemLabel` (ex.: "profissionais"/"usuários") e `pageSizeOptions` (padrão `[5, 10, 20, 50]`), calcula internamente `pageStart()`/`pageEnd()` (retornando 0/0 quando não há elementos, evitando `NaN`) e emite o novo tamanho via `pageSizeChange`. O reset para a página 0 e o recarregamento ficam a cargo do componente pai, que detém o estado da listagem. Ambos os componentes ganharam o campo `totalElements` (sincronizado com `meta.totalElements` no `carregar()`), a constante `pageSizeOptions` e o método `alterarTamanhoPagina(size: number)`, que ignora tamanhos fora das opções ou iguais ao atual, zera a página e recarrega. Os templates passaram a envolver o `.table-wrap` em um `<ng-container>` (condicionado à existência de linhas, como já era) com o `<app-pagination-summary>` antes da tabela. O comportamento existente foi preservado: janela de 5 botões, bloqueio de navegação fora dos limites, sincronização com metadados e recuo automático de página após remoção do último item. A listagem de pacientes não foi migrada nesta issue (permanece com o bloco inline já coberto por testes), mas o componente compartilhado está pronto para adotá-la em um follow-up. Cobertura: `pagination-summary.component.spec.ts` (cálculo do intervalo, texto do resumo com o rótulo, opções renderizadas/seleção do tamanho atual e emissão do tamanho numérico no `change`), além de novos testes em `profissional-list.component.spec.ts` e `usuario-list.component.spec.ts` (sincronização de `totalElements`, reset/recarregamento ao trocar o tamanho, rejeição de tamanho inválido/igual e renderização do resumo com total e seletor no DOM).

66. Aba Postural na avaliação fisioterapêutica: nova análise com seleção de vista e upload de foto comprimida (issue #181, Tela 1 do "Simetrógrafo Virtual" — especificação em `carlessopilatesapi/docs/simetrografo-virtual.md`). O simetrógrafo virtual substitui o simetrógrafo físico por uma foto do paciente com grade e prumo sobrepostos (marcação de pontos e cálculo de medidas ficam para as issues do editor/resultados, ainda não implementadas); esta issue entrega apenas a porta de entrada dentro da avaliação fisioterapêutica já existente. Novo model `core/models/avaliacao-postural.ts` (`VistaPostural`: `FRENTE`/`COSTAS`/`LADO_DIREITO`/`LADO_ESQUERDO`; `StatusAvaliacaoPostural`: `RASCUNHO`/`CONCLUIDA`; `AvaliacaoPosturalRequestDTO`, `AvaliacaoPosturalResponseDTO`, `AvaliacaoPosturalFotoResponseDTO`, `LandmarkDTO`, `MetricasPosturaisDTO`) e `AvaliacaoPosturalService` (`core/services/avaliacao-postural.service.ts`) consumindo `POST /api/avaliacoes-posturais`, `GET /api/avaliacoes-posturais/avaliacao-fisioterapeutica/{avaliacaoId}`, `PUT /api/avaliacoes-posturais/{id}/foto` (multipart, campo `foto`) e `GET /api/avaliacoes-posturais/{id}/foto` (`responseType: 'blob'`) — API implementada em carlessopilatesapi#143/#144/#145. Duas rotas seguem o padrão de listagem/formulário já usado em sessões, reavaliações, plano de tratamento e NFSEs: `/pacientes/:pacienteId/avaliacao-fisioterapeutica/postural` (`PacienteAvaliacaoPosturalListComponent`) resolve a avaliação fisioterapêutica mais recente do paciente (mesma lógica de `avaliacoes[0]` da tela de avaliação) e lista as análises posturais existentes (vista, status, data), com ação **Ver foto** por análise que baixa a foto como `Blob` autenticado e exibe via `URL.createObjectURL` (nunca `<img src>` direto para a API, conforme a regra de privacidade da spec) — URLs revogadas no `ngOnDestroy` e, individualmente, ao cancelar a análise correspondente. Cada análise também expõe **Cancelar análise** (`PATCH /api/avaliacoes-posturais/{id}/cancelar` — exclusão lógica no backend, histórico preservado) confirmada pelo `ConfirmarDialogComponent`, com o estado `acaoEmAndamentoId` desabilitando todos os botões de cancelar enquanto a requisição está em voo (mesmo padrão de `paciente-sessao-list`). Essa ação é o caminho de recuperação para uma análise `RASCUNHO` cujo upload de foto falhou: sem ela, a vista ficaria ocupada indefinidamente pela regra de 1 análise ativa por vista; `/pacientes/:pacienteId/avaliacao-fisioterapeutica/postural/nova` (`PacienteAvaliacaoPosturalFormComponent`) implementa a Tela 1 do mockup: seleção de vista em cards (`FormGroup` com `vista` obrigatório), desabilitando vistas que já têm análise ativa nesta avaliação com o rótulo "Já existe análise" (um `409` defensivo na criação também é tratado via `extrairMensagemErro`); área de arraste (`dragover`/`drop`) e `<input type="file" accept="image/*" capture="environment">` para tirar foto ou escolher da galeria; dica de posicionamento fixa ("em pé, parede lisa ao fundo, celular na vertical e nivelado"); e o botão **Continuar**, que cria a análise (`POST`) e envia a foto (`PUT .../foto`), voltando para a listagem `/postural` em caso de sucesso — como o editor de marcação (Tela 2) ainda não existe, a listagem atualizada é o destino provisório; qualquer falha em criar ou enviar a foto mantém o usuário na tela com a mensagem do backend, preservando o `id` da análise já criada para que o **Continuar** seguinte reenvie só a foto, sem tentar criar de novo (evitando um `409` espúrio). Compressão client-side em `shared/utils/image-compressor.ts` (`comprimirImagem`): usa `createImageBitmap` + `canvas` para redimensionar o maior lado a ~1080px (sem ampliar imagens menores) e exporta JPEG em qualidade 0,85 via `canvas.toBlob`, ficando bem abaixo do limite de 2 MB da API (validado manualmente: uma foto de teste 4000×3000 foi reduzida a poucos KB). Como o Chrome headless de CI roda com `--disable-gpu` e `canvas.toBlob('image/jpeg', ...)` não resolve de forma confiável nesse modo, a função pura tem cobertura própria com `canvas`/`createImageBitmap` mockados (`image-compressor.spec.ts`) e o componente depende dela através do wrapper injetável `ImageCompressorService` (`shared/services/image-compressor.service.ts`), que existe só para permitir mockar a compressão via `TestBed` nos testes do componente (spiar a função exportada diretamente falha em CI com "not declared writable", já que o bundle de teste trata os named exports como somente leitura). Novos links de navegação: card **Análise Postural** no detalhe do paciente e botão **Postural** no cabeçalho da tela de avaliação fisioterapêutica. Cobertura: `avaliacao-postural.service.spec.ts`, `image-compressor.spec.ts`, `image-compressor.service.spec.ts` e os specs dos dois componentes (identificador inválido, avaliação fisioterapêutica inexistente, vistas indisponíveis, compressão/preview/remoção de foto, drag-and-drop, fluxo completo criar→upload→navegar, conflito `409` e retomada após falha no upload da foto). Validado manualmente contra a API local (`localhost:8080`): login, criação de avaliação fisioterapêutica, fluxo completo de nova análise postural com foto real comprimida, listagem exibindo o rascunho criado e exibição da foto autenticada via Blob.
67. Editor de marcação postural — Tela 2 do "Simetrógrafo Virtual" (issue #182): grade ajustável, linha de prumo, marcação guiada, zoom e desfazer. Nova rota `/pacientes/:pacienteId/avaliacao-fisioterapeutica/postural/:id/marcar` (`PacienteAvaliacaoPosturalEditorComponent`), alcançada pela ação **Marcar pontos** / **Ver marcação** de cada análise com foto na listagem da aba Postural. A página carrega a análise por `GET /api/avaliacoes-posturais/{id}` e a foto como `Blob` autenticado (`URL.createObjectURL`, revogado no `ngOnDestroy`), e hospeda o `SimetrografoEditorComponent` (`shared/components/simetrografo-editor/`) — componente reutilizável, porque a issue da Tela 3 (resultados) desenha as linhas de referência sobre o mesmo editor. Model estendido em `core/models/avaliacao-postural.ts`: união `CodigoLandmark` com os 15 códigos do enum da API (`OLHO_ESQ`…`TORNOZELO_DIR` nas vistas frontais, `ORELHA`/`OMBRO`/`QUADRIL`/`JOELHO`/`TORNOZELO` nas laterais), sequências ordenadas `LANDMARKS_FRONTAIS`/`LANDMARKS_LATERAIS` (cada `DefinicaoLandmark` traz `label` e `alvo`, este já com a preposição, para compor a instrução sem concatenação frágil), `landmarksDaVista()`/`vistaEhLateral()` e `AvaliacaoPosturalUpdateDTO`; `LandmarkDTO.codigo` deixou de ser `string` e passou a ser `CodigoLandmark`. `AvaliacaoPosturalService` ganhou `buscarPorId` e `atualizar` (`PUT /api/avaliacoes-posturais/{id}`). Decisões de interação: (a) **coordenadas sempre normalizadas (0–1) em relação à imagem natural** — zoom e pan são aplicados por `transform` CSS no palco, então o `getBoundingClientRect()` da `<img>` já os embute e a conversão tela ↔ normalizado permanece correta em qualquer aproximação, sem precisar desfazer a matriz manualmente; (b) **a linha de prumo só é arrastável pela alça na base da foto** — a linha cruza a imagem inteira e, se fosse agarrável em toda a extensão, capturaria os toques de marcação (problema encontrado durante a implementação, quando o toque no centro da foto movia o prumo em vez de marcar o ponto); a base também é onde a linha é alinhada ao ponto médio entre os pés; (c) **um único gesto resolve marcar, corrigir e deslocar**: o `pointerdown` decide entre arrastar um ponto existente (dentro de 22px), arrastar a alça do prumo ou aguardar; o gesto vira pan a partir de 6px de deslocamento e, abaixo disso, o `pointerup` marca o próximo ponto pendente — evita um seletor de modo na barra de ferramentas; (d) **desfazer por pilha de snapshots** (`HistoricoEdicao`, limite de 50) em vez de ações invertíveis, porque marcação, arraste e ajuste de prumo compartilham o mesmo estado pequeno; (e) marcadores e traços têm raio/espessura divididos pelo zoom, mantendo tamanho aparente constante ao aproximar. Ajustes vindos do code review do PR #193: a foto passou a ser dimensionada para caber inteira na dobra (`max-height` descontando cabeçalho e barra de ferramentas, viewport com `width: fit-content` centralizado) — como o viewport consome os gestos (`touch-action: none`), uma foto maior que a tela tornava a alça do prumo, na base, inalcançável em tablet; o deslocamento ganhou limite (`limitarPan`) para a foto não ser arrastada para fora da vista; a página deixou de realimentar o editor com a resposta do `PUT` (fixando `landmarksIniciais` só na carga), porque isso descartava em silêncio as marcações feitas enquanto o salvamento estava em voo, e passou a contar revisões para manter o aviso de pendência nesse caso; e o prumo padrão virou a constante `PRUMO_PADRAO`, compartilhada entre editor e página, para que salvar sem tocar na linha não gravasse `null` enquanto a tela mostrava a linha centralizada. A proporção da imagem passou a chegar por `@Output() imagemMedida` em vez de `ViewChild`. Toda a lógica pura vive em `shared/utils/simetrografo.ts` (conversão de coordenadas com clamp em [0,1], `proximoPendente`, `landmarkMaisProximo`, `definirLandmark`, `HistoricoEdicao`, `montarPayloadRascunho`), isolada do DOM para ser testável sem canvas. **Salvar rascunho** envia landmarks parciais + `linhaPrumoX` + `proporcaoImagem` (largura/altura naturais — sem ela a API assume imagem quadrada e os ângulos saem errados) e nunca envia métricas, que a API recalcula a cada `PUT`. Análises `CONCLUIDA` abrem em modo somente leitura (sem marcar, arrastar, refazer, alça de prumo nem botão de salvar; zoom, pan e toggles seguem ativos). Acessibilidade e tablet: alvos de toque ≥44px nos controles, `touch-action: none` no viewport (o componente trata pan e pinça), painel do checklist abaixo da foto em ≤1024px e cores vindas dos tokens do Design System (a área da foto é neutra em ambos os temas). Cobertura: `simetrografo.spec.ts` (conversão sob zoom/pan, clamp, sequência guiada, histórico, payload), `simetrografo-editor.component.spec.ts` (marcação, arraste, desfazer, zoom, grade, somente leitura, restauração de rascunho) e `paciente-avaliacao-postural-editor.component.spec.ts` (carga da análise e da foto, salvamento, erros, imutabilidade da concluída). Validado manualmente contra a API local (`localhost:8080`): rascunho reaberto restaurou os pontos e retomou do próximo pendente; ponto marcado sob zoom 2,5× persistiu com a coordenada normalizada exata (0.5387/0.3246); `proporcaoImagem` 0.75 derivada da foto 810×1080; e uma análise concluída via API abriu corretamente em somente leitura.
68. Resultados da análise postural — Tela 3 do "Simetrógrafo Virtual" (issue #183): medidas calculadas, destaque de assimetrias, observações clínicas e conclusão. Entregue na mesma rota `/postural/:id/marcar` do editor da Tela 2, porque as linhas de referência são desenhadas sobre o mesmo canvas e os números precisam acompanhar a marcação em tempo real. `SimetrografoEditorComponent` ganhou o getter `linhasReferencia` (um segmento por par com os dois pontos marcados; nas vistas laterais, que têm um ponto por região, a lista fica naturalmente vazia) e um `<ng-content />` no topo do painel lateral — a página projeta ali o novo `PainelMedidasPosturaisComponent` (`shared/components/painel-medidas-posturais/`), de modo que as medidas fiquem ao lado da foto sem acoplar o editor às métricas. A trigonometria vive em `shared/utils/metricas-posturais.ts`, **espelhando `MetricasPosturaisCalculator` do backend**: reescala o Δx pela `proporcaoImagem` antes do `atan2` (sem isso os graus saem distorcidos, já que as coordenadas normalizadas só têm a mesma escala nos dois eixos em fotos quadradas), normaliza o ângulo para (-90, 90] e arredonda meio-para-cima nas mesmas escalas da API (2 casas nos graus, 4 no desvio de prumo — `Math.round` sozinho divergiria em negativos). Decisões: (a) **a direção do desnível ("dir./esq. baixo") vem da comparação dos `y`, não do sinal do ângulo** — o sinal depende de qual ponto ficou à esquerda na imagem, o que se inverte entre as vistas de frente e de costas, enquanto o maior `y` é sempre o ponto mais baixo na foto; (b) **prévia local x fonte da verdade**: com alterações não salvas o painel mostra o cálculo do cliente (retorno imediato ao arrastar um ponto) e, salvo, passa a exibir as `metricas` devolvidas pela API, indicando no rótulo qual das duas está em tela — divergências resolvem-se a favor da API; (c) **limiar único** `LIMIAR_ASSIMETRIA_GRAUS = 2` (mais `LIMIAR_DESVIO_PRUMO_NORMALIZADO = 0.02`, o equivalente relativo do prumo, que não é ângulo), isolado em constante porque o valor clínico ainda será validado com a fisioterapeuta; (d) **novos tokens `--c-success-text`/`--c-danger-text`** em `styles/_tokens.scss`, clareados no tema escuro: as originais também servem de fundo (`.btn-danger`, com texto branco) e clareá-las quebraria esse contraste; sobre `--bg-elev` escuro as originais ficavam em ~2:1, e as novas medem 4,96:1 (vermelho) e 5,44:1 (verde) — no tema claro, 7,55:1 e 4,79:1, todas acima de AA. As **observações clínicas** são enviadas sempre como texto, inclusive vazio: a API só altera campos não nulos, então um `null` deixaria a observação anterior gravada mesmo depois de apagada na tela (confirmado contra a API local). **Concluir análise** só habilita com todos os pontos obrigatórios da vista marcados e executa `PUT` seguido de `PATCH .../concluir`, para o prontuário registrar exatamente o que estava na tela; `422` (pontos incompletos ou sem foto) é exibido via `extrairMensagemErro` e o sucesso deixa a tela somente leitura. Fora de escopo: UI de calibração em cm (pendente de validação) e a ação de cancelar análise a partir desta tela — a listagem já a oferece com o `ConfirmarDialogComponent`, e replicá-la aqui não era trivial. Cobertura: `metricas-posturais.spec.ts` (ângulos com e sem proporção, normalização do ângulo, desvio de prumo frontal/lateral, direção sob vista espelhada, limiar em 1,9° vs 2,0°, cm só com calibração), `painel-medidas-posturais.component.spec.ts` (pendências, destaque, recálculo ao mudar os pontos via `setInput` — atribuição direta não marcaria o componente OnPush) e novos casos em `paciente-avaliacao-postural-editor.component.spec.ts` (prévia x API, habilitação do concluir, fluxo `PUT`+`PATCH`, `422`, observações apagadas). Validado contra a API local (`localhost:8080`) com um roteiro de 22 verificações: as métricas calculadas no cliente bateram exatamente com as da API nos 10 comparativos (marcação parcial e completa), `422` com pontos incompletos, conclusão mudando o status para `CONCLUIDA` e recusa de `PUT`/`concluir` em análise já concluída; e conferido no navegador que arrastar um ponto atualiza linha e medida na hora (ombros passaram de 6,3° dir. baixo para 29,8° esq. baixo).
69. Breadcrumbs nas rotas do prontuário e padronização dos botões "Voltar" (issue #145). A navegação profunda do paciente (sessões → evolução, com 3 níveis; planos de tratamento, reavaliações, anamnese, avaliação fisioterapêutica e suas subtelas posturais, NFSEs, planos/pagamentos/aulas) dependia só de botões de voltar com rótulos heterogêneos ("Voltar", "Voltar ao Paciente", "Voltar aos Pagamentos", "Voltar às Sessões", "Voltar aos Relatórios") e sem nenhuma trilha de onde o usuário está. Novo componente compartilhado `BreadcrumbComponent` (`shared/components/breadcrumb/`, standalone e `OnPush`, mesmo padrão de `pagination-summary`/`confirmar-dialog`): recebe um `@Input() items: BreadcrumbItem[]` (cada item com `label` e `link` opcional — `string` ou array de `routerLink`) e concentra num único ponto a marcação acessível (`<nav aria-label="breadcrumb">` com `<ol>`), os separadores `/` (`aria-hidden="true"`) e o tratamento do nível atual: o **último** item é sempre renderizado sem link, como `<span aria-current="page">`, enquanto os demais viram links navegáveis quando trazem `link`. Cada tela monta a trilha a partir da própria rota e do nome do paciente já carregado (`GET /api/pacientes/{id}`) — ex.: `Pacientes / Maria Silva / Sessões / Evolução` —, com fallback para o rótulo genérico "Paciente" quando o nome ainda não chegou ou a tela não carrega o objeto do paciente (postural-editor, plano-form, pagamento-form e aula-list, que só têm o `pacienteId`); o breadcrumb é envolto em `*ngIf` do identificador de rota, para não emitir link com `id` nulo em telas de parâmetro inválido. Junto, os botões de voltar foram padronizados: rótulo único **"Voltar"** (as variações citadas foram removidas), sempre em `<div class="acoes">` dentro do `page-header` e apontando para o nível imediatamente superior do breadcrumb (o mesmo destino que a maioria já usava). As telas que tinham duas variantes do botão (`paciente`/fallback via `paciente.id` vs `pacienteId`) foram colapsadas em um único `<a *ngIf="pacienteId">` para `['/pacientes', pacienteId]`, e o `aula-list`, que exibia dois botões ("Voltar ao Paciente" e "Voltar aos Pagamentos") conforme a origem, passou a mostrar **um** "Voltar" por contexto (para o paciente na rota `/aulas/paciente/:id`, para os pagamentos na rota `/aulas/pagamento/:id`), com a trilha correspondente. 19 telas aninhadas do paciente passaram a importar e renderizar o `<app-breadcrumb>`; os dois relatórios (`/relatorios/pagamento-profissional` e `/relatorios/nfse`), citados na issue apenas como exemplo da heterogeneidade de rótulos, tiveram o texto do botão padronizado para "Voltar" (sem breadcrumb, por não serem rotas do paciente). Mudança exclusivamente de frontend (novo componente + template/imports das telas), sem alteração de rotas, serviços ou API. Cobertura: `breadcrumb.component.spec.ts` valida a estratégia `OnPush`, a ausência de `<nav>` sem itens, o landmark `nav[aria-label="breadcrumb"]`, a renderização de um item por nível, os links navegáveis em todos os níveis exceto o atual (`href` resolvido pelo `routerLink`), o `aria-current="page"` apenas no último item (renderizado como `<span>`, não como link), o item intermediário sem `link` como texto puro e o separador entre itens (sem separador após o último).

70. Ordenação clicável nas colunas das listagens paginadas (issue #151). As listagens `/pacientes`, `/profissionais` e `/admin/usuarios` eram sempre ordenadas por nome (`sort` fixo no service), impedindo, por exemplo, achar os profissionais com maior % por aula sem paginar manualmente. Agora os cabeçalhos de coluna são clicáveis e alternam a ordenação, enviando o campo escolhido no parâmetro `sort` **já existente** das chamadas (ex.: `sort=email,asc`), sem qualquer mudança de contrato com a API — apenas o valor de `sort` muda, mantendo o mesmo endpoint e o Pageable do Spring. Novo model `core/models/ordenacao.ts` concentra o estado de ordenação (`Ordenacao = { campo, direcao }`, `DirecaoOrdenacao = 'asc' | 'desc'`), a conversão para o parâmetro `sort` (`toSortParam` → `'campo,direcao'`) e o ciclo de clique (`proximaOrdenacao`): coluna diferente → asc; mesma coluna asc → desc; mesma coluna desc → **volta ao padrão** (nome asc; `name` na tela de usuários, cujo backend usa esse campo), sem mutar os argumentos. Novo componente compartilhado `SortableHeaderComponent` (`shared/components/sortable-header/`, standalone e `OnPush`, mesmo padrão de `pagination-summary`/`confirmar-dialog`): aplicado como atributo no `<th>` (`th[app-sortable]`, preservando a semântica da tabela e o `aria-sort` no próprio cabeçalho), projeta o rótulo original da coluna via `<ng-content>` e adiciona um `<button>` acessível que emite o campo clicado; expõe `[attr.aria-sort]` (`ascending`/`descending`/`none`) refletindo a coluna/direção ativa e exibe seta `▲`/`▼` na coluna ativa e um indicador neutro `⇅` (revelado no hover/foco) nas demais. Para habilitar componentes de seletor por atributo, a regra `@angular-eslint/component-selector` do `eslint.config.js` passou a aceitar `type: ["element", "attribute"]` (antes só `element`). Cada listagem ganhou o estado `ordenacao`/`ordenacaoPadrao` e o método `ordenar(campo)`, que calcula a próxima ordenação (nova referência, para os cabeçalhos `OnPush` refletirem a mudança), **reseta para a página 0** e recarrega — como `carregar()` reaplica `montarFiltro()`, os filtros ativos são preservados. Já as ações **Buscar** e **Limpar** dos filtros (pacientes/profissionais) voltam a ordenação para o padrão (`nome asc`), para que uma nova busca parta sempre da ordem conhecida. Os services `PacienteService.listar` e `ProfissionalService.listar` receberam um 4º parâmetro opcional `sort` (default `'nome'`, mantendo o comportamento anterior); `UsuarioAdminService.listar` já expunha `sort` (default `'name'`). Colunas ordenáveis por tela, restritas aos campos simples do DTO exibidos em coluna: pacientes → nome, e-mail e status (`ativo`); profissionais → nome, e-mail, contrato (`tipoContrato`) e % por aula (`percentualPagamentoAula`); usuários → nome (`name`), e-mail, perfil (`role`) e status (`active`). Mudança exclusivamente de frontend, sem endpoint novo. Cobertura: `ordenacao.spec.ts` (formatação do `sort` e ciclo asc→desc→padrão, sem mutação), `sortable-header.component.spec.ts` (projeção do rótulo, `aria-sort` da coluna ativa/inativa, emissão do campo no clique, seta ativa vs. indicador neutro), novos casos nos três specs de listagem (ordem inicial, reset de página, preservação de filtros, ciclo asc→desc→padrão e `aria-sort` no DOM) e novos testes de service validando o envio do `sort` customizado (`paciente`/`profissional`).

71. Correção dos campos não persistidos no formulário de edição de sessão (issue #173). Em `/pacientes/:pacienteId/sessoes/:id/editar` os campos **Status**, **Tipo** e **Profissional** apareciam editáveis, mas nenhum deles era aplicado pelo `PUT /api/sessoes/{id}`: o backend só expõe `data`, `horario`, `local`, `duracaoMinutos` e `observacoes` no `SessaoPilatesUpdateDTO`, de modo que o `status` enviado pelo `toApiUpdate` era descartado pelo Jackson e `tipo`/`profissionalId` sequer chegavam a ser enviados. O usuário alterava qualquer um dos três, via a mensagem "Sessão atualizada com sucesso." e, ao recarregar, encontrava o valor anterior — falha silenciosa, sem erro de API nem aviso na tela. A correção alinha a interface ao contrato que a API sempre teve, sem tocar em nenhum endpoint: os três campos passam a ser **somente informativos na edição** (`disable()` no `ngOnInit` quando `modoEdicao`, mesmo padrão já usado pelo CPF imutável do `paciente-form`), cada um com um `field-hint` explicando o motivo — tipo e profissional são "definidos no agendamento", o status é "alterado pelas ações Realizar e Cancelar na listagem de sessões". No modo de criação nada muda: `tipo` e `profissionalId` continuam editáveis e o `POST /api/sessoes` continua aceitando ambos. O `SessaoUpdateDTO` (`core/models/sessao.ts`) foi reduzido a `dataHora`, `duracao` e `observacoes`, e `SessaoService.toApiUpdate` deixou de enviar `status` no corpo. Como `salvar()` passou a usar `form.getRawValue()`, os controles desabilitados não vazam para o payload nem quebram a montagem do `SessaoRequestDTO` da criação. A alteração de status de uma sessão existente continua exclusivamente pelos `PATCH /api/sessoes/{id}/realizar` e `PATCH /api/sessoes/{id}/cancelar` da listagem, que validam a transição (só aceitam sessão `AGENDADA`) — permitir status pelo `PUT` abriria um bypass dessas regras. Permitir a troca de tipo ou profissional de uma sessão já criada ficou fora de escopo: nunca existiu e exigiria mudança de backend. Cobertura: `sessao.service.spec.ts` assertando o corpo exato do `PUT` (apenas `data`, `horario`, `duracaoMinutos` e `observacoes`) e `paciente-sessao-form.component.spec.ts` cobrindo quais controles ficam habilitados em criação vs. edição, o `disabled` no DOM e o payload do `atualizar` mesmo quando os campos informativos são alterados via `patchValue`.

72. Padrão único de validação de formulários: submit habilitado, `markAllAsTouched` e foco no primeiro campo inválido (issue #148). Dois padrões conviviam nos formulários: o login (e as telas públicas de senha) **desabilitava o botão** enquanto o form era inválido — o usuário clicava e nada acontecia, sem saber o motivo —, enquanto os demais já permitiam o submit e chamavam `markAllAsTouched()` para revelar os erros; em nenhum deles o foco ia para o primeiro campo com erro, então em formulários longos do prontuário a mensagem podia ficar fora da viewport. A correção adota um único padrão em todos os formulários: submit sempre habilitado (o `[disabled]` fica só com o estado de salvamento — `carregando`/`enviando`/`salvando`/`processando`), `markAllAsTouched()` no submit inválido e **foco + scroll até o primeiro campo com erro**, mais foco inicial no primeiro campo ao abrir telas de criação. Novo helper compartilhado `shared/utils/form-focus.ts` com duas funções puras sobre o DOM: `focarPrimeiroInvalido(form, host)` — retorna `false` cedo quando `form.valid`, senão faz `querySelector('input.ng-invalid:not([disabled]), select…, textarea…')` no host e foca+`scrollIntoView({ block: 'center' })` o primeiro em ordem de documento (a ordem do DOM já reflete a ordem visual dos campos; a classe `ng-invalid` do Angular existe independentemente de `touched`, então funciona logo após o `markAllAsTouched`) — e `focarPrimeiroCampo(host)` — foca o primeiro campo focável ignorando `type=hidden` e desabilitados. Ambas aceitam `ElementRef` ou `HTMLElement`. Cada componente de formulário injeta o `ElementRef` do host e chama `focarPrimeiroInvalido(this.form, this.host)` no ramo inválido do submit. O `[disabled]="form.invalid || …"` foi removido do login, `forgot-password`, `reset-password` e `alterar-senha`, e o login (que só dava `return` no inválido) passou a marcar touched + focar. Os dois diálogos modais que desabilitavam o confirmar por invalidez (`[confirmarDesabilitado]="…invalid"`) — reagendamento em `paciente-sessao-list` e confirmação de pagamento em `pagamento-list` — deixaram de desabilitar e passaram a marcar touched + focar no `confirmar`. O foco inicial na criação usa `ngAfterViewInit` → `focarPrimeiroCampo` nas telas de entrada síncronas (login, forgot/reset senha, alterar senha, `paciente-form`/`profissional-form`/`usuario-form` em modo criação, `plano-form`, `pagamento-form` e os dois relatórios); nas telas do prontuário que decidem criação × edição só depois de carregar o registro (anamnese, avaliação fisioterapêutica, sessão, evolução, reavaliação, plano de tratamento, NFSE), o foco é chamado no ramo "sem registro" do callback de carga e **adiado com `setTimeout`**, porque o formulário só entra no DOM depois que o `*ngIf="!loading"` renderiza. O `paciente-avaliacao-postural-form` (seleção de vista por cards + upload de foto, submit já governado por `enviando`/`comprimindo`) recebeu apenas o foco no inválido. Mudança exclusivamente de frontend, sem endpoint novo. Cobertura: `form-focus.spec.ts` (foco no primeiro inválido em ordem de documento, suporte a select/textarea, ignora desabilitados, aceita `ElementRef`, no-op quando o form é válido/sem host; foco do primeiro campo pulando ocultos/desabilitados) e novos casos de componente (`login.component.spec.ts` e `paciente-form.component.spec.ts`: submit permanece habilitado com form inválido, foco no primeiro inválido e foco inicial na criação; `paciente-anamnese.component.spec.ts`: foco inicial adiado na criação e foco no inválido). Os specs que instanciam o componente manualmente (`plano-form`, `pagamento-form`, `pagamento-list`) passaram a receber um `ElementRef` no construtor.

73. Correção do enquadramento do botão **Voltar à Administração** na listagem de usuários (issue #199). Em `/admin/usuarios`, o rótulo do botão aparecia colado no topo da borda, com um vão vazio ocupando a metade inferior — defeito visível em qualquer viewport e evidente no mobile, onde o botão fica sozinho no rodapé da página. A causa era o `display: inline-block` declarado em `.back-link` (`usuario-list.component.scss`): como o SCSS de componente ganha o atributo `[_ngcontent-*]` do encapsulamento emulado, ele vence em especificidade o `.btn` global (`styles.scss`), que monta o botão como `inline-flex` e centraliza o rótulo por `align-items`/`justify-content`. Trocado o `display`, essas duas propriedades ficam inertes e, como o `.btn` não tem padding vertical (`padding: 0 var(--sp-5)`) e usa `line-height: 1`, o texto se apoiava no topo enquanto o `min-height: var(--btn-h)` mantinha a altura total. A correção remove o override de `display` — restando ao componente apenas o espaçamento externo, agora com o token `--sp-6` (24px, o mesmo valor do `1.5rem` anterior) — e o botão volta a ser idêntico aos demais `.btn btn-outline` do sistema. Medição antes × depois no CSS compilado, nas seis combinações de tema (claro/escuro) × densidade (`default` 40px, `compact` 34px, `comfortable` 44px): a folga acima do rótulo saiu de 0px com 19–29px sobrando embaixo, para folga simétrica e desvio do centro igual a 0 em todos os casos, sem alterar a altura do botão. Auditoria confirmou que `.back-link` era o **único** override de `display` sobre um `.btn` no projeto: `.status` no mesmo arquivo é um `<span>` (legítimo), `.palco` do simetrógrafo é um `<div>`, `.btn-entrar`/`.btn-enviar`/`.btn-voltar-login` são classes próprias fora do seletor global, e os overrides de `.btn`/`.btn-sm` em `forbidden` e `aula-list` mexem só em `width`. Mover o botão para dentro do `.page-header`, como fazem as demais telas, ficou fora de escopo por ser decisão de padronização de navegação, não o defeito reportado. Cobertura: novo caso em `usuario-list.component.spec.ts` assertando `getComputedStyle` do `a.back-link` com `display: inline-flex` e `margin-top: 24px` — o guard foi verificado contra o CSS antigo e falha com `Expected 'inline-block' to be 'inline-flex'`. Convenção registrada no README: em SCSS de página, sobrescrever de um componente global apenas o que o posiciona no layout (`margin` e, quando necessário, `width` — como já fazem `forbidden` e `aula-list` para botões de largura total no mobile), nunca as propriedades que montam o interior do componente (`display`, `padding`, `min-height`, `line-height`).

74. Correção da seta dos selects, que renderizava em tamanho gigante, e padronização dela em toda a aplicação (issue #200). No campo **Perfil** de `/admin/usuarios/:id/editar` — e em todos os demais `select.form-control` — a seta ocupava a altura inteira do campo em vez dos 12×8px pretendidos. A causa era o SVG do data URI declarar apenas `viewBox='0 0 12 8'`, sem `width`/`height`, combinado com a ausência de `background-size` na regra global (`styles.scss`): um SVG assim tem proporção intrínseca (3:2) mas **nenhum tamanho intrínseco**, e o algoritmo padrão do CSS (Images 3, §5.5) com `background-size: auto` ajusta a imagem por *contain* contra a área de posicionamento do fundo, escalando-a até a altura do campo — 60×40px na densidade padrão, 66×44 na `comfortable` e 51×34 na `compact`, sempre invadindo o texto, já que o `padding-right: 36px` foi calculado para uma seta de 12px. A correção declara `background-size: 12px 8px` e acrescenta `width='12' height='8'` ao SVG, defesa dupla para o defeito não voltar caso a regra seja reescrita. O desenho saiu da regra e virou o token `--select-chevron` (`src/styles/_tokens.scss`), com uma versão por tema: o stroke mora dentro do data URI e `url()` não interpola `var()`, então `[data-theme="dark"]` substitui o token inteiro pelo mesmo desenho em `#b9bcc1` (o `--text-secondary` escuro). O grafite `#455157` do tema claro rendia ~2:1 sobre o `--bg-elev` escuro, abaixo do mínimo de 3:1 da WCAG 1.4.11 para componentes de interface; medidos agora 8,2:1 no claro e 8,4:1 no escuro. Além do tamanho, a auditoria mostrou que **metade dos selects nem chegava a receber a seta customizada**: os 5 campos sem `class="form-control"` (filtros de `paciente-list` e `profissional-list`, seletor de itens por página do `pagination-summary` compartilhado e da cópia local em `paciente-list`) exibiam a seta nativa do sistema operacional, e o `select.form-control-sm` de `aula-list` não exibia seta **alguma** — o `appearance: none` global removia a nativa e a shorthand `background: var(--bg-elev)` do SCSS do componente, que vence o global em especificidade por causa do atributo `[_ngcontent-*]`, zerava o `background-image`. Todos os 5 selects passaram a usar `.form-control` e, como o SCSS local desses componentes redeclarava o campo inteiro (altura fixa de 38px, `padding` que roubava o espaço da seta, a shorthand `background`, borda e foco duplicados do global), essa cópia foi removida — os `input` dos mesmos filtros também passaram a `.form-control` para a linha não ficar com campos de alturas diferentes. Efeito colateral desejado: os filtros deixam de ter altura fixa e passam a acompanhar a densidade (34/40/44px), como o resto dos formulários, e herdam do global o salto para `font-size: 16px` no mobile que evita o zoom do Safari iOS (issue #160), antes duplicado em cada componente. Nos casos em que o campo não pode esticar — o seletor de itens por página dentro de um `label` flex —, o componente mantém apenas `width: auto`, que é override de layout e não de interior, conforme a convenção do issue #199. A mesma armadilha apareceu **dentro do próprio estilo global**: a regra `&:disabled` do campo (`.form-control:disabled`, especificidade maior que `select.form-control`) usava `background: var(--bg-sunken)` e apagava a seta de todos os selects desabilitados — defeito pré-existente, encontrado na validação visual e coberto pelo critério de aceite do issue; corrigido para `background-color`. Nova regra global `select.form-control-sm` aproxima a seta da borda (`right 8px`) e reduz o `padding-right` para 26px, já que o campo pequeno tem 32px de altura e recuo horizontal de 12px e herdaria os 36px pensados para o campo padrão. A tríade utilitária `.input`/`.select`/`.textarea` foi **removida** do `styles.scss`: nenhum template a usava, e mantê-la significaria replicar em cada regra nova (seta, `background-size`, recuo) um caminho de estilo que ninguém exercita. O protótipo do Design System conserva as classes no seu próprio `assets/tokens.css`, que não entra no build do Angular. A remoção deixa `.form-control` como a única API de campo do app. O mesmo defeito foi corrigido na origem, em `assets/tokens.css`, para que o protótipo do Design System não devolva a regra quebrada ao app na próxima cópia. Ficaram fora de escopo, como previsto no issue: trocar o `<select>` nativo por um combobox customizado e redesenhar o design system de formulários. Cobertura: 7 novos casos assertando `getComputedStyle` com os estilos globais carregados (o `karma` já inclui `src/styles.scss`) — tamanho 12×8 e `padding-right` de 36px no select do `usuario-form`, seta preservada com o controle desabilitado, troca do token no tema escuro, seta preservada com `padding-right` de 26px no `form-control-sm` de `aula-list`, e `.form-control` + seta customizada nos selects de `paciente-list`, `profissional-list` e `pagination-summary`, este último também verificando que o campo não estica dentro do label. Todos os guards foram verificados contra o CSS antigo e falham com `Expected 'auto' to be '12px 8px'`, `Expected 'none' not to be 'none'`, `Expected '8px' to be '26px'` e `Expected '…455157…' to contain 'b9bcc1'`. Três achados do review do PR foram corrigidos no mesmo ciclo. (a) O `font-size: 0.9rem` local do `.form-control-sm` de `aula-list` saiu: vencia por especificidade a regra global que sobe os campos para 16px no mobile e reabria o zoom automático do Safari iOS que a issue #160 havia fechado — o campo passa a usar `--fs-caption` no desktop e 16px no mobile, como os demais. (b) `paciente-list` deixou de duplicar o markup do `.pagination-summary` e passou a consumir o `<app-pagination-summary>` compartilhado — a duplicação era o motivo de a mesma correção precisar ser feita em dois lugares; com ela saíram também `pageStart()`/`pageEnd()` do componente (o cálculo já vivia no compartilhado, com testes próprios), o `alterarTamanhoPagina` passou a receber `number` em vez de `string`, e o SCSS local do resumo — inclusive a media query de 640px, que nunca casou com o DOM do componente filho por causa do atributo de encapsulamento — foi apagado. (c) A tríade utilitária citada acima. O quarto achado, a migração dos inputs dos filtros para `.form-control`, foi mantido: é decisão de padronização, não defeito. Armadilha registrada no README: em elemento que depende de `background-image` herdado do global, usar `background-color:` e nunca a shorthand `background:`.

75. Indicação de scroll horizontal nas tabelas de resultado dos relatórios no mobile (issue #164). Em `/relatorios/pagamento-profissional` (tabelas de 6 e 8 colunas) e `/relatorios/nfse` (8 colunas), as tabelas já rolavam contidas no `.table-responsive`, mas em 375px apenas ~2 colunas cabiam no viewport e nada sinalizava que havia mais à direita: o usuário podia não perceber que **Valor Profissional** — a coluna que interessa — estava fora da tela, e a coluna de identificação da linha (**Data**/**Nome**) rolava junto, dificultando cruzar linha × valor. A correção é só de CSS/markup, sem mudança de contrato. (a) Os contêineres globais `.table-responsive`/`.table-wrap` ganharam sombras de borda em quatro camadas de `background-image`: duas "tampas" opacas da cor do contêiner com `background-attachment: local` — que rolam junto com a tabela — sobre duas sombras presas ao contêiner (`scroll`). Sem transbordo as tampas nunca saem de cima das sombras e nada aparece; ao rolar, a tampa correspondente sai de vista e a sombra da borda oposta surge, tudo sem JavaScript. Como as sombras são pintadas no fundo do contêiner, a tabela dentro dele passou a ter `background-color: transparent` (com fundo próprio ela cobriria as camadas); o visual das linhas não muda, porque o contêiner já é opaco na mesma cor. A cor da sombra vive no token `--table-scroll-shadow`, com versão por tema (`rgba(20, 31, 45, 0.16)` no claro, `rgba(0, 0, 0, 0.55)` no escuro, onde a versão clara quase não se distingue do fundo), espelhado em `src/styles/_tokens.scss` e `assets/tokens.css`. (b) Nova classe utilitária global `.table-sticky-first-col`, aplicada às três tabelas de relatório, congela a primeira coluna com `position: sticky; left: 0` e fundo opaco por token (`--bg-elev` nas células, `--bg-sunken` no cabeçalho) — sem fundo opaco as colunas seguintes passariam por baixo e o texto se sobreporia. O separador da coluna congelada é um pseudo-elemento absoluto de 1px com sombra difusa, e não `border-right` nem `box-shadow` da célula: numa tabela com `border-collapse: collapse` a borda pertence à tabela e o WebKit não a repinta na posição sticky, enquanto o Chromium não pinta box-shadow de célula colapsada — verificado em navegador, o separador simplesmente sumia. Essa sombra também compensa a sombra esquerda do contêiner, que fica encoberta pela coluna congelada. (c) Um `.table-scroll-hint` ("Arraste a tabela para o lado para ver todas as colunas →", com a seta em `aria-hidden`) precede cada tabela e só aparece em ≤768px, junto com o restante das regras mobile do `styles.scss`. A opção de ocultar colunas intermediárias em ≤480px, listada como opcional na issue, não foi implementada: com a coluna congelada e a affordance de scroll, todas as colunas continuam alcançáveis, e o detalhamento completo já está nas exportações PDF/XLSX. Como o fundo do contêiner é o que pinta as sombras, duas listagens que declaravam fundo próprio na tabela em SCSS de componente — `/pacientes` e `/aulas` — passaram a `background-color: transparent` (o SCSS local empata em especificidade com a regra global por causa do atributo de encapsulamento e vencia por ordem de injeção, cobrindo as sombras); no modo card do `/aulas` (≤640px) o fundo elevado foi movido para dentro do media query, onde cada linha é de fato um card. Todos os contêineres de tabela da aplicação (relatórios e listagens) ganharam `tabindex="0"`, `role="region"` e `aria-label`, para o teclado alcançar o scroll horizontal (WCAG 2.1.1); em `aula-list` os três atributos são condicionais (`modoCard`, alimentado por um `matchMedia('(max-width: 640px)')` com listener removido no `ngOnDestroy`), porque naquele breakpoint a tabela vira cards e não rola — um tab stop ali não levaria a lugar nenhum. O anel de foco não reaproveita o `--shadow-focus`: com 18% de alfa ele compõe 1,32:1 sobre `--bg-app`, longe dos 3:1 da WCAG 2.4.11, então o contêiner usa `outline: 2px solid var(--text-brand)` (7,2:1 no claro, 9,67:1 no escuro — o `--border-brand` não é redefinido no tema escuro e cairia para 2,26:1); e o media query local dos relatórios passou de 760px para os 768px do bloco mobile global, ficando só com `.form-actions` (o `.form-row` já empilha pela regra global). Os specs dos dois relatórios ganharam guardas de computed style para sticky/fundo opaco (claro e escuro), para as camadas de fundo do contêiner (`background-attachment: local, local, scroll, scroll`) e a transparência da tabela, para a visibilidade do hint por media query, além de regressões dos critérios já atendidos: `summary-grid` em 1 coluna a 375px e botões de ação empilhados no mobile. Esses testes de breakpoint rodam dentro de um iframe de largura fixa (helper `src/testing/viewport.ts`, que serializa as folhas de estilo do documento para dentro dele): a janela do Karma é sempre larga e, com asserções condicionais por `matchMedia`, só o ramo desktop era exercitado — as regras mobile, que são o objeto da issue, ficavam sem teste efetivo.

76. Tela de histórico de evoluções do paciente em linha do tempo, fase 1/3 (issue #205). Até aqui a única forma de ler a evolução clínica era abrir `/pacientes/:pacienteId/sessoes/:sessaoId/evolucao` uma sessão por vez pelo botão **Evolução** da listagem: acompanhar semanas de tratamento significava entrar, ler, voltar e repetir, sem nenhuma visão consolidada. A nova rota `/pacientes/:pacienteId/evolucoes` (`PacienteEvolucaoListComponent`, `authGuard` + `loadComponent`, posicionada entre a listagem de sessões e o bloco de plano de tratamento) entrega essa segunda forma de leitura em uma única página, da evolução mais recente para a mais antiga. A tela é **somente leitura** — nenhum `POST`/`PUT` sai dela —, e a tela de cadastro/edição por sessão continua exatamente como estava, o que mantém o risco de regressão restrito à navegação. O `EvolucaoSessaoService` ganhou `listarPorPaciente` (`GET /api/evolucoes-sessao/paciente/{pacienteId}`), espelhando `ReavaliacaoService.listarPorPaciente`; o endpoint já existia no backend e devolve lista vazia, não `404`, quando o paciente não tem registros. A carga é um `forkJoin` de `SessaoService.listarPorPaciente` + `EvolucaoSessaoService.listarPorPaciente`, cruzado no cliente por `sessaoId`: as duas chamadas existem porque o `EvolucaoSessaoResponseDTO` não traz data/tipo/profissional da sessão, e desnormalizá-lo economizaria uma requisição ao custo de a tela deixar de conhecer as sessões **sem** evolução. Entram na linha do tempo as sessões com evolução, as sessões `REALIZADA` ainda sem evolução (badge **Sem evolução registrada** + link **Registrar evolução**) e, defensivamente, evoluções cuja sessão não veio na listagem — sem esse ramo um registro clínico sumiria da tela se a sessão fosse excluída. Sessões `AGENDADA`/`CANCELADA` sem evolução ficam de fora. A ordenação é decrescente por data/hora da sessão, com desempate por `sessaoId`, e a coleção derivada (`itens`) mora num único ponto do componente, para que o gráfico de dor (#206) e os filtros de período/tipo (#207) consumam a mesma lista. Cada card traz no cabeçalho data/hora, tipo, profissional e a variação `dorAntes → dorDepois` com melhora/piora nos tokens `--c-success-text`/`--c-danger-text` (e não `--c-success`/`--c-danger`, que também servem de fundo e não são clareados no tema escuro); no corpo, **Observações do fisioterapeuta** aparece sem nenhuma interação — é o campo mais lido (#204) e, na base real importada, o único preenchido — e exercícios, equipamentos, cargas/molas, resposta do paciente, intercorrências e orientações ficam atrás de um `<button type="button">` com `aria-expanded` e `aria-controls` apontando para o id do painel, com **Expandir tudo**/**Recolher tudo** no topo. Campos vazios não são renderizados, e o botão de expandir só aparece quando há algum campo colapsável. O componente usa `ChangeDetectionStrategy.OnPush` com `cdr.markForCheck()` após cada atualização assíncrona **e** após alternar a expansão (sem isso o toggle só funcionaria pelo clique no template, que já marca a view suja — o guard de teclado do spec pegou a diferença). Duas armadilhas de estilo apareceram na validação em navegador e ficaram registradas no CSS: (a) as observações vêm da API com quebras de linha (blocos de "QUEIXAS E RELATOS INICIAIS" e "CONDUTA"), então o texto usa `white-space: pre-wrap` e ocupa a linha inteira do `detail-grid` via `grid-column: 1 / -1`, em vez de uma coluna de 200px; (b) os tokens `--surface` e `--c-primary`, usados pelos cards das quatro listagens do prontuário (`reavaliacao`, `sessao`, `nfse-emitida`, `plano-tratamento`), **nunca foram definidos** em `_tokens.scss` — o fundo do card não é pintado e a borda de destaque cai para `currentColor`, o que no tema escuro rende uma faixa creme de 4px. O componente novo usa `--bg-elev` e `--text-brand` (o único token de marca com versão própria no tema escuro); corrigir as outras quatro telas ficou fora do escopo desta issue. Entradas de navegação: `link-card` **📈 Histórico de Evoluções** no bloco de links do detalhe do paciente e botão homônimo no `page-header` de `paciente-sessao-list`. Ficaram fora de escopo, como previsto na issue: gráfico de evolução da dor (fase 2), filtros de período e tipo (fase 3), edição inline, exportação e paginação/scroll infinito. Cobertura: 2 casos novos em `evolucao-sessao.service.spec.ts` (o `GET` por paciente e a lista vazia), 22 casos em `paciente-evolucao-list.component.spec.ts` (identificador inválido sem nenhuma requisição, ordenação decrescente, join por `sessaoId`, tendência de dor nas três direções e ausente quando falta um dos valores, sessão realizada sem evolução marcada, agendada/cancelada fora da lista, evolução órfã preservada, campos vazios não renderizados, expandir/recolher individual — por clique e por teclado — e em massa, `aria-controls` casando com o id do painel, ausência do toggle e dos controles em massa quando não há campo colapsável, link para a tela de evolução da sessão, estado vazio, e erro em cada uma das três requisições) e o novo caminho no `app.routes.spec.ts`. Os dois guards de `getComputedStyle` (fundo/borda do card nos dois temas e `pre-wrap` das observações) foram verificados contra o CSS antigo e falham com `Expected 'rgba(0, 0, 0, 0)' to be 'rgb(255, 255, 255)'`, `Expected 'rgb(235, 232, 226)' to be 'rgb(168, 188, 202)'` e `Expected 'normal' to be 'pre-wrap'`. Validação manual com o backend local em dados reais (paciente com 381 evoluções renderizando sem scroll horizontal, ida e volta pela tela de edição refletindo a nova dor no histórico, 375px, tema claro e escuro, e operação do expandir por teclado). Seis achados do review do PR foram corrigidos no mesmo ciclo. (a) As guardas do par de dor usavam `!== null`; um corpo de resposta **sem** os campos (`undefined`) passava pela guarda e a interpolação do Angular renderizava string vazia em vez do fallback `—`, exibindo um badge "Dor → " em branco. `dorAntes`/`dorDepois` passaram a ser normalizados para `null` no `montarItem` (`evolucao?.dorAntes ?? null`) e o template usa `?? '—'`, que cobre os dois casos. (b) O `forkJoin` colapsa as duas requisições num único ramo de erro, então a mensagem "Erro ao carregar evoluções." aparecia mesmo quando quem falhava era `GET /api/sessoes/paciente/{id}`; passou para a neutra "Não foi possível carregar o histórico de evoluções.". (c) Os dois ramos de erro passaram a usar `extrairMensagemErro`, como já fazem seis telas do prontuário — incluindo `paciente-sessao-list`, cujos dados esta tela consome —, propagando a mensagem do backend quando existe (validado em navegador: um paciente inexistente agora exibe "Paciente não encontrado: 999999" em vez do texto genérico). (d) O `aria-controls` do botão apontava para um id que só existia **enquanto expandido**, porque o painel era `*ngIf="temDetalhes && expandido"` — referência quebrada para o leitor de tela e para o axe. O painel passou a ficar sempre no DOM quando há campos colapsáveis, escondido pela classe `.evolucao-detalhes-recolhido`, exatamente o padrão de disclosure já usado pelos filtros de `paciente-list`/`profissional-list` (issue #163). A classe precisa de `display: none` explícito: o `.detail-grid` global declara `display: grid` e venceria o atributo `hidden` do HTML em especificidade. (e) O `→` do cabeçalho é `aria-hidden` e o par de dor era anunciado como "Dor 7 3 melhora"; o `.evolucao-dor` ganhou `role="img"` e um `aria-label` montado no componente (`descreverDor`), que produz "Dor antes 7, depois 3, melhora" e "Dor antes 5, depois não informada". (f) Os três `*ngIf` que escreviam melhora/piora/sem variação no template viraram o mapa `TENDENCIA_DOR_LABEL`, seguindo a convenção de `SESSAO_STATUS_LABEL`. Cobertura dos achados: 5 casos novos (fallback `—` no DOM, corpo de resposta sem os campos de dor, `aria-label`/`role` do par de dor, desempate por `sessaoId` em timestamps iguais e propagação da mensagem do backend), mais a reescrita dos casos de expandir/recolher para assertar `display` computado e a resolução do `aria-controls` com o card recolhido. Os guards novos foram verificados contra o template anterior e falham com `Expected null not to be null` (o `aria-controls` sem alvo), `Expected null to be 'Dor antes 7, depois 3, melhora'` e `Expected 0 to be 1`.

77. Gráfico de evolução da dor no histórico de evoluções, fase 2/3 (issue #206). A linha do tempo entregue em #205 exige comparar `dorAntes`/`dorDepois` cartão por cartão para perceber se o paciente melhorou ao longo de semanas; o gráfico mostra a tendência de imediato e é o principal argumento da tela na conversa com o paciente. O desenho é **SVG inline** no próprio `paciente-evolucao-list`, sem nova dependência — o projeto não tem biblioteca de gráficos e o `simetrografo-editor` já monta SVG assim. Toda a matemática mora na função pura exportada `montarGraficoDor(itens)`, que recebe a mesma coleção derivada que alimenta a linha do tempo e devolve `GraficoDor | null`: **nenhuma requisição a mais** entra na carga da tela (o spec trava as três chamadas em `toHaveBeenCalledTimes(1)`). A lista é decrescente e o eixo X é cronológico crescente, então a função inverte a coleção-fonte em vez de reordenar — as duas vistas saem da mesma ordenação, sem lógica duplicada. Decisões que ficaram registradas em comentário no código: (a) o eixo Y é **fixo em 0–10**, a escala da dor no formulário de evolução, e não normalizado pelos valores presentes — normalizar faria uma variação de 4 para 5 parecer uma queda dramática; há um `clamp` defensivo para que um valor fora da escala não desenhe o ponto por cima dos rótulos. (b) Uma sessão com dor `null` **não vira ponto em `0`**: a série é quebrada em trechos e cada trecho contínuo com dois pontos ou mais vira um `<polyline>` próprio (`serie.linhas`), enquanto trechos de um ponto só ficam apenas como círculo — sem isso um `polyline` de um vértice seria invisível. (c) O gráfico não é renderizado enquanto **nenhuma série chega a dois pontos**: uma medição isolada não desenha tendência nenhuma, e a linha do tempo segue normal. (d) O eixo X reserva posição para **todas** as sessões da linha do tempo, inclusive as sem evolução: é o intervalo em que a dor não foi medida, e comprimi-lo distorceria o espaçamento. Só até 5 delas ganham data escrita (`indicesDasMarcasX`, sempre com a primeira e a última, e as das pontas ancoradas em `start`/`end` para não vazar do `viewBox`); acima de 30 sessões os círculos viram uma faixa sólida — e centenas de nós no DOM —, então só os pontos isolados seguem marcados. (e) As datas são formatadas a partir do prefixo ISO de `dataHora` (`dd/MM` no eixo, `dd/MM/yyyy` no `aria-label`) em vez de `new Date`: a string vem sem fuso e o construtor já viraria um dia a menos no rótulo. (f) As coordenadas são arredondadas a duas casas, o que basta na escala do `viewBox` e evita `26,64.20000000000002` no atributo `points`. Acessibilidade: o `<svg>` inteiro é um `role="img"` com `aria-label` textual ("Gráfico da evolução da dor ao longo de 2 sessões, de 01/05/2026 a 20/05/2026. Dor antes: de 7 em 01/05/2026 para 2 em 20/05/2026. Dor depois: …"), e grade, eixos e rótulos internos são `aria-hidden` — sem nome acessível eles seriam só ruído para o leitor de tela. As duas séries são nomeadas numa legenda `<figcaption>` e distinguidas por cor **e** por tracejado, para quem não separa âmbar de azul. As cores vêm de `--c-warning-text` (antes) e `--text-brand` (depois), os dois tokens com versão própria no tema escuro; a escolha é deliberadamente neutra, e não `--c-success`/`--c-danger`, porque a série "depois" não é necessariamente a boa notícia. Cada série publica a cor num custom property `--serie-cor` no `<g>`, o que faz o mesmo token pintar o `stroke` do SVG e a borda do quadradinho HTML da legenda sem repetir a lista de cores. O responsivo é `viewBox` + `preserveAspectRatio="xMidYMid meet"` com `width: 100%` e `max-width: 480px`: o desenho encolhe junto com a coluna e nunca empurra scroll horizontal, e o teto existe porque o texto dentro do SVG está em unidades do `viewBox` e cresceria junto com o desenho. Cobertura: 13 casos novos em `paciente-evolucao-list.component.spec.ts` (gráfico acima da linha do tempo; omitido com zero e com uma sessão com dor; pontos do mais antigo para o mais recente, inversos à lista; eixo Y em 0–10 independente dos valores; série quebrada em vez de ponto em `0`; `role`/`aria-label`; legenda nomeando as duas séries; nenhuma requisição extra; cor de cada série nos temas claro e escuro; `viewBox`/`preserveAspectRatio`/`max-width`; o teto de 5 marcas no eixo X com a primeira e a última sempre presentes; e a supressão dos marcadores num gráfico denso preservando o ponto isolado). Validação manual com o backend local: **nenhuma evolução da base real importada tem `dorAntes`/`dorDepois`** — como já registrado na fase 1, o único campo preenchido é `observacoesFisioterapeuta` —, então o cenário foi montado pela própria tela de evolução em Carolina Safadi (paciente 17, 7 sessões), com dor em 5 delas (8→6, 7→4, 5→3, 4→2, 3→1) e duas deixadas em branco de propósito. Conferidos: gráfico ausente com uma única sessão com dor e a linha do tempo intacta; as duas quebras de série com o ponto isolado do meio desenhado como círculo; `aria-label` completo no DOM; exatamente três requisições na carga da tela (paciente, sessões e evoluções — nenhuma por causa do gráfico); os dois temas; e 375px sem scroll horizontal. Dois ajustes saíram dessa validação: (a) num card de largura total o desenho ficava encostado à esquerda com um vazio à direita do mesmo tamanho, então o `figure` passou a ter `max-width` acompanhando o teto do SVG mais o padding; (b) os rótulos dos eixos subiram de 11 para 13 unidades do `viewBox` — em 375px o desenho encolhe para ~0,6 e os 11 rendiam ~6,6px na tela. Dois achados do review do PR foram corrigidos no mesmo ciclo, ambos com a mesma raiz — a guarda operava sobre **sessões com dor** em vez de **pontos por série**. (a) Quando `dorDepois` nunca era preenchida (cenário real: a fisioterapeuta que só registra a dor inicial), a legenda continuava anunciando **Dor depois** com o traço da cor enquanto nada era desenhado, contradizendo o `aria-label`, que já omitia a série. Séries sem nenhum ponto passaram a ser filtradas antes do desenho, o que remove de uma vez o `<g>` e o item da legenda. (b) Com uma sessão só com `dorAntes` e outra só com `dorDepois`, o total de sessões com dor chegava a dois e o gráfico renderizava eixos e legenda em volta de **dois pontos soltos, sem linha nenhuma** — atendia a letra do critério da issue e derrotava o propósito da tela. A guarda passou a exigir que ao menos uma série chegue a `GRAFICO_MIN_PONTOS_SERIE` pontos; a constante renomeada de `GRAFICO_MIN_SESSOES_COM_DOR` também virou a guarda explícita da divisão em `posicaoX`, que antes dependia do efeito colateral de o total de sessões nunca ser menor que o de sessões com dor. Cobertura dos achados: 2 casos novos (gráfico omitido quando nenhuma série chega a dois pontos, com as duas medições ainda legíveis nos cards; e série sem ponto fora do SVG, da legenda e do `aria-label`), verificados em navegador contra o código anterior nos dois cenários. Ficaram fora de escopo, como previsto na issue: filtros de período e tipo (fase 3, quando o gráfico passa a respeitá-los), gráficos de outras métricas, tooltip/zoom/exportação e qualquer biblioteca de gráficos.

78. Filtros de período e tipo de sessão no histórico de evoluções, fase 3/3 (issue #207). Com a linha do tempo (#205) e o gráfico (#206), a tela mostrava todo o tratamento de uma vez: um paciente de longa data rende dezenas de cards e um gráfico com dezenas de pontos, sem como recortar o mês que interessa antes de um atendimento nem separar as sessões de fisioterapia das de pilates. A barra de filtros — data inicial, data final e tipo (todos / pilates / fisioterapia) — fica no topo, acima do gráfico, e recorta **client-side** a coleção já carregada: o spec trava as três requisições da carga em `toHaveBeenCalledTimes(1)` depois de aplicar e limpar filtros, e a validação em navegador confirmou zero chamadas `/api/` em toda a sessão de filtragem. A decisão central é o recorte morar num **único ponto**: a função pura exportada `filtrarItens(itens, filtro)` produz `itens`, e é esse resultado que alimenta ao mesmo tempo o `*ngFor` da linha do tempo e `montarGraficoDor` — por construção não há como o gráfico plotar uma sessão que a lista não mostra, que é o critério central da issue. A fonte carregada passou a ficar em `itensCarregados`, preservada para o "limpar filtros" restaurar a coleção completa sem recarregar. Decisões registradas em comentário: (a) as datas são comparadas pelo prefixo ISO (`yyyy-MM-dd`) de `dataHora` contra os limites do `<input type="date">`, que já vêm nesse formato — a ordem lexicográfica coincide com a cronológica, o dia inteiro entra nas duas pontas (a sessão das 14h do dia final não pode cair fora) e nenhum `Date` é construído, o que reintroduziria o deslocamento de fuso que `formatarData` já evitava desde a fase 2; (b) o item da linha do tempo ganhou `tipoSessao` (o tipo cru) além do rótulo exibido, para o filtro não comparar contra strings traduzidas, e o select é montado a partir de `SESSAO_TIPO_LABEL` em vez de redeclarar as opções; (c) data final anterior à inicial é recorte **impossível**, não recorte vazio: exibe a mensagem no formulário e não aplica o período, mas mantém o filtro de tipo, que é independente — derrubá-lo junto surpreenderia quem só errou a data; (d) a regra do gráfico (menos de dois pontos numa série ⇒ sem desenho) passa a valer sobre o conjunto filtrado, então um recorte de uma sessão só esconde o gráfico e mantém a linha do tempo, verificado em navegador; (e) o recorte vazio tem estado próprio, com a ação **Limpar filtros** que a issue #146 pede para as listagens, distinto do "Nenhuma evolução registrada" de paciente sem histórico. A barra usa o grid `auto-fit`/`minmax(180px, 1fr)` dos filtros de `paciente-list` (empilha sozinho em 375px, sem media query) e os `.form-control` globais, o que traz a seta padronizada do issue #200 de graça. A validação em navegador rendeu um ajuste de acessibilidade: o `.invalid-feedback` global usa `--c-danger`, que também serve de fundo (`.btn-danger`) e por isso **não** é clareado no tema escuro — medido sobre a superfície elevada do card de filtros dava 2,12:1 em 11px. O `.filtro-erro` passou a usar `--c-danger-text`, a variante criada em `_tokens.scss` exatamente para texto sobre superfície elevada: idêntica no tema claro e 4,96:1 no escuro (medido no DOM). O `aria-describedby` dos dois campos é ligado por `[attr.]` e some junto com a mensagem, em vez de apontar para um id inexistente — o mesmo achado de referência quebrada que o review da fase 1 pegou no `aria-controls`. Cobertura: 19 casos novos em `paciente-evolucao-list.component.spec.ts` (barra acima do gráfico; label e id de cada campo mais as opções vindas do mapa de rótulos; período reduzindo lista e pontos de forma coerente; dia inteiro nas duas pontas; filtro por tipo nos dois tipos e o retorno em "todos"; período + tipo combinados; sessão realizada sem evolução respeitando os mesmos filtros; período invertido sem aplicar, com `role="alert"`, `aria-describedby` e `is-invalid`; erro limpo ao reordenar as datas; estado vazio do recorte com a ação de limpar; restauração da coleção completa pela barra, com o botão desabilitado sem filtro ativo; gráfico oculto quando o recorte deixa menos de dois pontos e de volta ao limpar; nenhuma requisição ao filtrar ou limpar; filtro aplicado pelo campo e pelo select renderizados; barra ausente quando não há o que filtrar; contraste da mensagem de erro nos dois temas; e o empilhamento em 375px sem overflow). Três desses guards foram verificados contra uma versão em que o gráfico deriva da coleção completa em vez do recorte, e falham. QA manual com o backend local em Carolina Safadi (paciente 17), que tinha 7 sessões de pilates entre dez/2025 e jan/2026 com dor em 5 delas; foram criadas mais duas sessões de fisioterapia com dor (09/12 e 13/01) para o cenário misto que a issue pede. Conferidos: recorte de janeiro levando lista e gráfico de 9 para 4 sessões com o `aria-label` acompanhando; tipo fisioterapia isolando as duas sessões e o gráfico ficando com exatamente dois pontos; janeiro + fisioterapia deixando uma sessão só e escondendo o gráfico; datas invertidas exibindo o erro sem aplicar o período e mantendo o tipo; recorte em março exibindo o estado vazio; operação só por teclado até o botão do estado vazio, com `Enter` restaurando as 9 sessões; zero requisições `/api/` no painel de rede durante toda a sessão; e os dois temas. Seis achados do review do PR foram corrigidos no mesmo ciclo. (a) A troca de filtro era **silenciosa para o leitor de tela**: a lista mudava sem nenhum retorno e nem o estado vazio era anunciado — a única região viva da página era a busca global da navbar. Entrou um `<p class="filtro-resumo" role="status">` **permanente** com o texto "4 de 9 sessões no recorte" / "Nenhuma sessão no recorte"; permanente de propósito, porque o leitor de tela anuncia mudança de texto de uma região que já está no DOM de forma bem mais confiável do que a inserção de um elemento novo com `role="status"`. Fica com string vazia sem filtro ativo (mudar para vazio não anuncia nada) e sai do layout por `:empty`, em vez de deixar um parágrafo vazio ocupando margem. (b) Faltava o **disclosure mobile do issue #163**: medido com o container em 375px, a barra empilhada tem 323px de altura e empurrava o gráfico 343px para baixo — quase meia tela de filtro antes de qualquer conteúdo, exatamente o problema que o #163 resolveu em `paciente-list` e `profissional-list`. Entraram o `.filtros-toggle` com `aria-expanded`/`aria-controls`, o badge de filtros ativos e o mesmo bloco de media query em 768px. É a terceira cópia desse bloco no projeto; extrair para o global mexeria nas outras duas telas e vale issue própria. O botão e o painel compartilham o mesmo `*ngIf`, então o `aria-controls` sempre resolve. (c) Os dois estados vazios se protegiam de forma diferente — o de paciente sem evolução exigia `!erro`, o do recorte não; hoje não há caminho que produza erro com a coleção preenchida (não existe botão de retry), mas a assimetria vazaria no dia em que houvesse. (d) `filtrarItens` devolvia a **própria fonte por referência** quando não havia filtro ativo, de modo que `itens === itensCarregados`: nada muta os arrays hoje, mas um `sort()`/`splice()` futuro no recorte corromperia em silêncio a coleção que o "limpar filtros" restaura. Passou a devolver cópia. (e) Como consequência do item anterior, a asserção `expect(component.itens).toEqual(component.itensCarregados)` comparava o mesmo array consigo mesmo e passava por construção; virou comparação pelos `sessaoId` esperados. (f) A evolução órfã (sessão excluída) tem `tipoSessao` nulo e sai de qualquer recorte por tipo — comportamento correto, já que não há tipo a conhecer, mas não estava documentado nem coberto; ganhou comentário no código e caso de teste. A correção do disclosure rendeu um achado de bancada: o runner do Karma roda a **765px**, dentro do breakpoint de 768px, então o painel nasce recolhido nos testes e o caso de empilhamento passou a abrir o disclosure antes de medir o grid — o que de quebra permitiu testar o comportamento real do #163 (painel `display: none` até o clique) num viewport de verdade, com uma guarda de `matchMedia` para o teste falhar dizendo o porquê caso a janela do runner mude. Cobertura dos achados: 8 casos novos (cópia em vez da fonte, com `splice` no recorte não afetando a fonte; órfã fora do recorte por tipo e de volta em "todos"; região viva preexistente ao DOM anunciando as três transições; região fora do layout enquanto vazia; disclosure com `aria-controls` resolvível e painel escondido/exibido por CSS; badge contando os filtros ativos; estado vazio do recorte suprimido com erro na tela), mais a asserção tautológica reescrita, totalizando 27 casos novos na tela e 1155 na suíte. Ficaram fora de escopo, como previsto na issue: filtros server-side, paginação/scroll infinito, persistência do filtro na URL ou em `localStorage`, atalhos de período pré-definido e filtros por profissional, faixa de dor ou intercorrências.

79. Identificação do usuário autenticado na navbar (issue #215). A barra exibia marca, navegação, busca global e as ações "Alterar senha", tema e "Sair", mas em nenhum ponto da interface aparecia **quem** estava logado — num computador compartilhado na recepção não havia como conferir a conta antes de registrar dado clínico ou financeiro, nem como entender por que "Profissionais", "Relatórios" e "Administração" (renderizados sob `authService.isAdmin()`) não apareciam. O dado já estava no cliente: `AuthService.getCurrentUser()` devolve o `{ id, name, email, role }` gravado em `localStorage` no login e validado por `isAuthenticatedUser(...)`, então a mudança é inteiramente de frontend, sem contrato novo com o backend. O `ROLE_LABEL` que era local em `usuario-list.component.ts` subiu para `core/models/usuario-admin.ts`, derivado de `ROLE_OPTIONS` e exportado ao lado dele: a listagem administrativa e a navbar passam a ler "Administrador"/"Usuário" da mesma fonte, e a listagem só troca o import (nenhuma mudança de comportamento — a suíte existente cobre isso). No `AppComponent` o valor é resolvido em **campo**, não na interpolação: `getCurrentUser()` faz `JSON.parse` + validação a cada chamada e o template da raiz reavalia a cada ciclo de detecção de mudanças. A resolução acontece na construção e a cada `NavigationEnd`, reaproveitando a assinatura que já existia para limpar a notificação global — login e logout sempre passam por uma navegação, então a identificação acompanha a sessão sem virar polling nem exigir estado reativo em cima do `localStorage`. O bloco fica sob `@if` do usuário corrente: com `accessToken` presente e `currentUser` ausente ou corrompido, `getCurrentUser()` devolve `null`, a navbar renderiza inteira e apenas a identificação some. É um `<span>` — não é botão nem link, não entra na ordem de tabulação — com `title` contendo o texto completo, para o nome truncado continuar legível no hover. A cor é a **literal** `#f0ede8`, e não um token — o review do PR derrubou a premissa da issue aqui. O raciocínio original era usar `--c-cloud-dancer` como `.btn-sair`/`.btn-tema`, já que o fundo da navbar é o `--c-horizonte` fixo nos dois temas e `--text-secondary` ficaria ilegível no tema claro. Só que `--c-cloud-dancer` **é re-tematizado** em `[data-theme="dark"]` (`_tokens.scss:153`, `#f0ede8` → `#0e1620`): medido no DOM, o token rende **2,16:1** sobre o `#374f6c` da navbar no tema escuro, abaixo dos 4,5:1 da WCAG AA para 13px, contra **7,20:1** da literal nos dois temas. O irmão certo a copiar era o `.navbar-menu a`, que usa a literal `rgba(240, 237, 232, 0.82)` exatamente por isso — é o que o comentário em `styles.scss:100` sempre disse ("Cores literais (não tokens) para manter o contraste sobre o fundo fixo `--c-horizonte` da navbar em ambos os temas"). Vale registrar que o mesmo 2,16:1 já atinge `.navbar-brand a`, `.btn-sair`, `.btn-tema` e `.btn-alterar-senha` desde antes desta issue: é defeito pré-existente na `master`, fora do escopo aqui, e pede issue própria. No desktop o `max-width` + `ellipsis` + `nowrap` impedem que um nome longo empurre os botões ou quebre a barra em duas linhas; em `≤1024px`, dentro do bloco que já trata `.navbar-actions`, o item ocupa a largura toda como os irmãos, deixa de ser truncado (no painel expandido há espaço) e **não** recebe o `min-height: 44px` dos botões, porque não é alvo de toque. A issue pedia o elemento "antes do link Alterar senha" e "alinhado ao topo do painel expandido": a primeira tentativa usou `order: -1` no mobile, mantendo o DOM depois da busca global, mas isso fazia o leitor de tela anunciar a identificação **depois** da busca enquanto ela aparecia antes. A segunda rodada de review trocou o `order` pela posição real no DOM — a identificação virou o primeiro filho de `.navbar-actions`, ainda antes do link "Alterar senha" —, o que satisfaz as duas exigências e deixa ordem visual e ordem de leitura idênticas nos dois breakpoints, sem CSS de reordenação. Cobertura: 9 casos novos em `app.component.spec.ts` (nome + "Administrador" para `ADMIN`; nome + "Usuário" para `USER`; `title` com o texto completo; navbar íntegra e identificação ausente com `getCurrentUser()` nulo; nada renderizado sem autenticação; e a identificação aparecendo após uma navegação, que é o caminho real do login). O spy de `AuthService` das duas fábricas do spec passou a incluir `getCurrentUser`. Os três últimos vieram do review e cobrem justamente o que a suíte não olhava — CSS: (a) a cor travada contra o tema, que é o que o token quebrava; (b) o truncamento no desktop, medido em iframe de 1280px com o `renderizarEmViewport` (`src/testing/viewport.ts`, do #217), porque a janela do Karma roda a 765px e o ramo desktop ficaria sem teste efetivo — assere `ellipsis`, `scrollWidth > clientWidth`, largura ≤ 220px, navbar em 64px e `.navbar-menu` mais largo que a identificação; (c) o painel mobile em 375px, com largura cheia, sem o `min-height` de 44px e com a identificação acima da busca. Os três foram verificados contra uma versão com os defeitos reintroduzidos e falham. A ausência desse tipo de guard foi o motivo de o achado de contraste ter passado na primeira rodada. A segunda rodada de review ainda ajustou o guard de truncamento: ele tinha duas asserções suspeitas de serem vazias e a medição, feita primeiro numa marcação sintética **sem** o `app-busca-global`, indicou que ambas passariam sempre. Medindo na fixture real — onde a busca global consome ~160px da barra — só uma era: o `.btn-sair` fica em `right: 1241px` com ou sem truncamento, porque `min-width: auto` impede um item flex de comprimir abaixo do próprio rótulo. A da altura da navbar é o melhor discriminador que a tela tem: sem truncamento o nome espreme `.navbar-menu` de 305px para 98px, um link por linha, e a barra vai de **64px para 185,2px**. A asserção do botão saiu; entrou a comparação entre a largura do menu e a da identificação (305,5 > 220 com truncamento; 98,2 > 438,6 falha sem). Lição registrada: probe de layout com marcação sintética omite o que os componentes filhos ocupam e leva a conclusão errada sobre o que a asserção protege. Fora de escopo, como previsto na issue: menu dropdown de usuário, e-mail/avatar, tela de perfil, mudanças de backend e sincronização do `currentUser` quando o próprio usuário edita seus dados em `/admin` — o `currentUser` é gravado apenas no login, então uma troca de nome pelo `/admin` só reflete no login seguinte.

80. Consolidação das ações da navbar num menu de conta (issue #219). A barra acumulava marca, até 5 links, busca global, identificação do usuário e três botões de texto de peso visual idêntico — "Alterar senha", "Tema claro/escuro" e "Sair" —, e quebrava em duas linhas em qualquer notebook. Medindo as larguras intrínsecas no DOM: marca 179px + menu 424px + ações 734px (identificação 198 + busca 202 + alterar senha 122 + tema 106 + sair 59) + 88px de padding e gaps = **1426px necessários para uma linha limpa**, enquanto a barra só colapsava no hambúrguer em `≤1024px`. A faixa de **1025px a 1425px não tinha layout que funcionasse**, e é onde ficam 1280, 1366 e 1440 (1425 de viewport com a scrollbar). A causa não era estética: 367px da barra eram os três botões, nenhum deles ação frequente. Os três viraram um `MenuContaComponent` em `shared/components/menu-conta/` — gatilho com avatar de iniciais + primeiro nome + chevron, painel com cabeçalho (nome completo, e-mail, perfil) e os itens `Alterar senha` / alternância de tema / `Sair` —, e as classes `.btn-alterar-senha`, `.btn-tema` e `.btn-sair` saíram do global, onde eram usadas exclusivamente pela navbar. A decisão estrutural, porém, é outra e vale mais que a remoção dos botões: **definir quem absorve a pressão de espaço**. Antes marca, menu e ações encolhiam todos juntos e a barra estourava; agora marca, menu e menu de conta são `flex: none` com `white-space: nowrap` (o `flex-wrap: wrap` do `.navbar-menu` saiu — a quebra era o sintoma, não a solução) e a **busca é o único elástico** (`flex: 0 1 260px` com `min-width: 160px`), então a barra degrada estreitando o campo em vez de quebrar. Entrou também o wrapper `.navbar-inner`, que repete `max-width: 1120px` e `padding: 0 var(--sp-4)` do `.container`: só a faixa de fundo sangra de ponta a ponta. O padding interno não é decorativo — sem ele a marca começava 16px à esquerda da coluna do conteúdo, defasagem que só apareceu na conferência visual e que o teste de alinhamento agora trava comparando `left` da marca contra `left + paddingLeft` do container. A altura de 64px continua saindo do `.navbar`, e não do wrapper: um `min-height` lá dentro empilhava com a borda de 1px e a barra ia para 65px (o teste pegou). A marcação da seção ativa trocou o `text-decoration: underline` com offset — que flutuava no meio da barra — por uma borda inferior colada na base, o que exigiu a cadeia `.navbar { align-items: stretch }` → `.navbar-inner` esticado → `.navbar-menu { align-self: stretch }` → link com `height: 100%`; no compacto os links voltam a 44px com sublinhado, que não depende da borda da barra. O breakpoint de 1024px **não mudou**, e ganhou fonte única em `shared/utils/breakpoints.ts` (`DESKTOP_MIN_WIDTH`/`MEDIA_QUERY_COMPACTO`), consumida pelo `AppComponent`, pelo `MenuContaComponent` e espelhada no media query — antes era cópia solta em `app.component.ts`. O menu de conta troca de forma no compacto: dentro do painel colapsado da navbar ele renderiza **plano**, sem gatilho, porque dropdown ali seria menu dentro de menu. Essa troca vive no TS via `matchMedia`, e não só no CSS, porque a **semântica ARIA muda junto** — `aria-haspopup="menu"`, `aria-expanded` e `role="menu"/"menuitem"` só fazem sentido com gatilho, e atributo não se altera por media query. Duas descobertas da validação em navegador mudaram o desenho: (a) em 1025px o gatilho com o nome ("Administrador", do usuário semeado) media 169px e empurrava a busca exatamente contra o `min-width` de 160px, zerando a folga — o nome passou a ser ocultado na faixa 1025–1199px, deixando avatar + chevron (66px) e **48px de folga**, com o `aria-label="Conta de <nome>"` preservando a identificação; (b) o gatilho exibe só o **primeiro nome**, com o nome completo no cabeçalho do painel — exibir o nome inteiro na barra era justamente o que obrigava a truncar com reticências. As cores do gatilho e dos itens no compacto são **literais** (`#f0ede8`, e `#f0a9a9` no "Sair"), pelo mesmo motivo já registrado no item 79: o fundo é o `--c-horizonte` fixo nos dois temas, enquanto `--c-cloud-dancer` é re-tematizado em `[data-theme="dark"]`; o painel flutuante, que fica sobre `--bg-elev`, usa tokens normalmente. Um efeito colateral da mudança precisou de conserto próprio: com o logout fora do `AppComponent`, nada mais fechava o painel colapsado ao sair, e `menuAberto` seguiria `true` fazendo a navbar reaparecer expandida no login seguinte — o `AppComponent` passou a fechar o menu em `NavigationEnd`, ao lado da limpeza da notificação global. Cobertura: 42 casos novos em `menu-conta.component.spec.ts` (iniciais de nome composto, de palavra única e com acento; rótulos de perfil; ausência de sessão; atualização por navegação; resolução uma vez por navegação e não por ciclo de detecção; contraste do gatilho nos dois temas; abrir/fechar; `aria-controls` resolvível; Escape devolvendo o foco e **sem** vazar para o `document`, que fecharia a navbar inteira no mobile; clique fora; Tab dispensando o painel; setas circulares, Home e End; delegação de tema e logout; rótulos do item de tema; e o modo compacto sem semântica de menu), mais o teste de regressão do bug em `app.component.spec.ts`, que mede em 1025px altura de 64px, cinco links compartilhando o mesmo topo e a marca sem quebra. Dois achados de bancada: o `focus()` do gatilho não tinha efeito no Karma porque a janela do runner roda a 765px e o próprio SCSS do componente esconde o gatilho no compacto — o caso passou a usar o `renderizarEmViewport` em 1280px; e o `@angular-eslint/template/interactive-supports-focus` exigiu `tabindex="-1"` no painel, que ouve o keydown das setas enquanto quem tem o foco é sempre um item filho. Conferido em navegador com o backend local, perfil ADMIN, em 1025 / 1100 / 1199 / 1200 / 1280 / 1366 / 1920px e nos dois temas: 64px de altura, uma linha de links, sem scroll horizontal, marca e menu de conta alinhados exatamente às bordas do conteúdo (`xMarca === xConteudo` em todas as larguras) e 56–64px de folga; e em 381px o painel plano com gatilho oculto, `role` nulo e itens de 44px. Ficou fora de escopo, como previsto na issue: agrupar `Profissionais`/`Relatórios`/`Administração` sob um menu "Gestão" (derrubaria o mínimo para 816px e escala melhor, mas esconde navegação em um nível a mais) e migrar para sidebar lateral (elimina a competição por largura, mas altera o layout de todas as páginas).

81. Correção dos tokens CSS inexistentes usados pelos cards do prontuário e pelos badges de status (issue #213). Seis arquivos de `src/` consumiam **doze custom properties que nunca foram declaradas** em `_tokens.scss`, e nada no projeto sinalizava: `var()` com nome inexistente não quebra build, não acusa lint e não derruba teste. Sem fallback, a declaração fica *inválida no momento da computação* (CSS Variables 1, §3.2) e a propriedade cai para o valor herdado ou inicial — como `background-color`, `border-color` e `border-radius` não são herdáveis, o card ficava transparente, com a borda de destaque em `currentColor` e cantos retos. Medido no navegador antes da correção: `borderRadius === "0px"`, `backgroundColor === "rgba(0, 0, 0, 0)"` e `borderLeftColor === "rgb(235, 232, 226)"` no tema escuro — uma faixa creme de 4px, o elemento mais chamativo da tela. As quatro listagens do prontuário (reavaliações, sessões, NFSEs emitidas e planos de tratamento) trocaram `--surface` → `--bg-elev`, `--c-primary` → `--text-brand` e `--radius-lg` → `--r-lg`, alinhando-se a `paciente-evolucao-list`, que já nasceu certa em #205/#212; a superfície passou a ser declarada por `background-color:` e não pela shorthand, conforme a convenção da issue #200. `--text-brand` e não `--border-brand` porque é o único token de marca redefinido em `[data-theme="dark"]` (`#a8bcca`), como já registrado na #164. Com fallback o sintoma é outro e pior de achar: o CSS renderiza, mas pelo literal, que é sempre a cor do tema claro. Era o caso dos badges de `/admin/usuarios`, que usavam `var(--bg-success, #e6f4ea)` e `var(--text-success, #1e6b3a)` e pintavam **verde-claro sobre fundo escuro** no dark mode; agora usam `--c-success-bg`/`--c-success-text` (ativo) e `--c-danger-bg`/`--c-danger-text` (inativo, mantendo o vermelho que o fallback já pintava), com a variante `-text` porque `--c-success`/`--c-danger` também servem de fundo (`.btn-danger`) e não podem ser clareadas no escuro. Em `/perfil/alterar-senha`, `--space-md`/`--space-sm`/`--radius-sm`/`--text-default`/`--surface-muted` viraram `--sp-4`/`--sp-2`/`--r-md`/`--text-primary`/`--bg-sunken`: `--r-md` (4px) e não `--r-sm` (2px) porque 4px era o raio que o fallback pintava e é o mesmo do `.form-control` ao lado. Ali os literais coincidiam com os tokens e a tela não muda de aparência — o ganho é o hover do botão, que era `transparent`, e a saída do limbo do tema. **Fora de escopo por decisão da issue**: criar `--surface`/`--c-primary`/`--radius-lg` como aliases, que faria o CSS funcionar sem corrigir a causa e deixaria dois nomes para a mesma coisa. Cobertura: seis guards de `getComputedStyle` que alternam `data-theme` no `documentElement` e travam fundo, cor da borda esquerda e raio nos dois temas — os cinco das listagens e de `/admin/usuarios` foram conferidos contra o CSS antigo e **falham** nele (é o critério das issues #164/#199/#200); o de `/perfil/alterar-senha` passa nos dois, porque lá os literais rendiam os mesmos números, e existe para que a troca de nome não vire troca de desenho. O que fecha a porta para a classe inteira de erro é o `npm run lint:tokens` (`scripts/lint-tokens.mjs`, chamado pelo job de lint da CI): ele extrai todo `var(--x)` de `src/` e exige que o nome exista em `_tokens.scss` ou esteja declarado no próprio arquivo (caso do `--serie-cor` do gráfico de evoluções). O nome é validado **inclusive quando há fallback** — foi justamente o fallback que escondeu o desvio de tema por meses. Rodado contra o commit anterior, o script acusa as 24 ocorrências dos 12 tokens fantasmas; depois da correção, os 100 tokens do Design System cobrem 100% dos usos. Conferido em navegador com o backend local, perfil ADMIN, nos dois temas: card de sessão em `#ffffff`/`#182230` com raio de 8px, e a borda de 4px do modificador em `#374f6c`/`#a8bcca` — os mesmos números nas quatro telas do prontuário, nos badges de `/admin/usuarios` (`#e6ede4`/`#5a7a5e` no claro, `#1d2a23`/`#6da372` no escuro para "ativo") e no botão "Mostrar" de `/perfil/alterar-senha` (raio 4px, padding lateral 16px, texto acompanhando `--text-primary`). Como o banco de dev não tem plano, reavaliação nem NFSE semeados, essas três telas foram medidas por elemento-sonda com o atributo `_ngcontent-*` do componente renderizado, o que exercita a folha de estilo real da aplicação. A revisão do PR #221 rendeu quatro correções no próprio verificador, todas com regressão em sandbox: (a) a varredura era **linha a linha**, então um `var(` quebrado em várias linhas escapava — exatamente a classe de erro que o script existe para pegar —, e passou a rodar sobre o arquivo inteiro, com a linha derivada do offset do match; (b) uma custom property escrita dentro de um bloco `/* */` era lida como declaração e **whitelistava** o nome, enquanto uma menção a `var(--x)` em comentário era acusada como uso — os comentários agora são trocados por espaços antes da análise, caractere a caractere em SCSS/CSS/TS porque `//` também aparece dentro de string (o data URI de `--select-chevron` traz `http://www.w3.org/2000/svg`) e só via `<!-- -->` em HTML, onde rastrear string faria um apóstrofo de texto corrido engolir o resto do arquivo; (c) o escopo das variáveis locais passou de arquivo para **pasta**, e a posição de declaração passou a aceitar `"`/`'`, eliminando o falso positivo de `style="--x: 4px; padding: var(--x)"` e o de uma variável declarada no `.scss` e consumida no `.html` irmão; (d) `lint:tokens` foi encadeado em `npm run lint`, de modo que a verificação deixa de ser exclusiva da CI. A versão corrigida continua acusando as mesmas 24 ocorrências contra a `master`.

82. Reposicionamento de **Observações do fisioterapeuta** no formulário de evolução da sessão (issue #204). O campo era o **décimo e último** do formulário, no fim da seção "Resposta e orientações", depois de exercícios, equipamentos, cargas/molas, dor antes/depois, resposta do paciente, intercorrências e orientações — e é justamente o de uso mais frequente no atendimento: é onde a fisioterapeuta escreve o texto livre da evolução e, na base real importada, o **único preenchido** (fato já registrado em #205, que por isso o deixa sempre visível nos cards do histórico). Escrever ou reler a observação exigia rolar o formulário inteiro. Ele passou para logo depois de **Data/hora do registro**, ainda dentro da seção **Registro**, o que o deixa visível sem rolagem ao abrir a tela; **Exercícios realizados** passou a ser o campo seguinte e **Orientações fornecidas** virou o último. A mudança é de **ordem no DOM do template** e nada mais: `label`, `id`/`for`, `formControlName` e classes seguem idênticos, então não há alteração de contrato com o backend — `montarPayload` e `patchValue` operam sobre os nomes dos controles, não sobre a posição. A chave no `fb.group` foi reordenada junto, mas é cosmético: a ordem no `FormGroup` não influencia validação nem payload. O `rows` do textarea subiu de 2 para 3, alinhando-se a `exerciciosRealizados`/`respostaPaciente`, que já usavam 3. Alternativa descartada: dar uma seção própria ao campo entre "Registro" e o resto, o que renderia uma seção de um único campo. A ordem de tabulação vem do DOM, sem `tabindex` — conferido no navegador com uma pegadinha do Chrome que vale registrar: o `input[type=datetime-local]` consome **cinco** Tabs entre seus segmentos (dia, mês, ano, hora, minuto), e é o sexto que cai no textarea de observações. Cobertura: 2 casos novos em `paciente-evolucao-sessao.component.spec.ts` (a sequência dos `.form-group` do formulário, exigindo `dataHoraRegistro` → `observacoesFisioterapeuta` → `exerciciosRealizados` e `orientacoes` por último, e o valor carregado renderizado na nova posição com `rows === 3`), mais a asserção do campo no caso de pré-preenchimento; o `mockEvolucao` ganhou um `observacoesFisioterapeuta` não nulo, e o ramo `null → ''` do `preencherFormulario` continua coberto por `intercorrencias`. Os dois guards foram verificados contra o template anterior e falham nele com `Expected $[1] = 'exerciciosRealizados' to equal 'observacoesFisioterapeuta'` e `Expected 2 to be 3`. Validação manual com o backend local, perfil ADMIN: em um registro real (Carolina Safadi, sessão 1487) o campo aparece logo abaixo da data com o texto da API, e o `PUT` gravou uma linha de marcação que voltou preenchida após recarregar — o texto clínico original foi restaurado em seguida e conferido caractere a caractere. O `POST` foi exercitado em uma **sessão temporária criada e apagada ao final** (`DELETE /sessoes/{id}` remove a evolução em cascata), porque a API **não expõe `DELETE` para `/evolucoes-sessao`** e qualquer evolução de teste ficaria permanente no prontuário: cadastro com o texto digitado na nova posição retornou "Evolução cadastrada com sucesso.", e o recarregamento trouxe o valor de volta no segundo `.form-group`, já em modo de edição. A checagem de 375px foi feita medindo o `form-card` com largura fixada em 375px (o gerenciador de janelas do ambiente ignorou o redimensionamento): nenhum transbordo, campo em 325px dentro do card. As `@media (max-width: 768px)` que alteram layout de formulário só atingem `.form-row`, que esta tela não usa — os campos já são blocos empilhados de largura cheia nos dois tamanhos, então trocar dois deles de lugar não muda o comportamento responsivo.

83. Atualização do Angular 19.2 → 22.1 pelo caminho incremental do `ng update` (issue #208). O projeto estava três majors atrás e fora da janela de suporte, e as três PRs que o Dependabot abriu para cobrir o gap (#110, #111, #112) eram mutuamente dependentes e nenhuma instalável sozinha — a raiz era o grupo `dev-dependencies`, definido por `dependency-type: development`, capturar o toolchain do Angular e separá-lo do runtime em outra PR. A migração foi feita um major por vez, com `lint`, `test:ci` e `build` verdes entre cada passo, sob Node 22.23.2 (o piso `^22.22.3` do v22 veio antes, na issue #224). **19 → 20:** migration do `DOCUMENT`, que mudou de `@angular/common` para `@angular/core`; TS 5.7 → 5.9.3. **20 → 21:** só bump de versão. **21 → 22:** TS 5.9 → 6.0.3, dentro da faixa `>=6.0 <6.1` que o `compiler-cli` exige — o TypeScript 7 da PR #111 nunca teve como funcionar. A conversão para o control flow em bloco (`@if`/`@for`/`@switch`, 43 templates, 73 arquivos) foi rodada **antes** do passo 21, ainda no v20: no v21 ela deixa de ser opcional e roda dentro do `ng update`, e o angular-eslint 21 promove `prefer-control-flow` a *recommended* (426 erros), então fazê-la antes é o que mantém os dois commits verdes e isola a reescrita de 73 arquivos do bump de versão. Os seis `trackBy` das listagens foram preservados como `track trackByPaciente($index, p)`. Os três riscos reais do v22 são de **runtime**, não de compilação, e por isso o CI verde não provava nada sobre eles: (a) o `FetchBackend` virou o transporte padrão — a migration `http-xhr-backend` adicionou `withXhr()` ao `provideHttpClient`, congelando o XHR; migrar para fetch não foi feito aqui e não tem prazo, já que o `withXhr()` é opt-out suportado e não deprecado; se um dia for feito, precisa de QA próprio, porque os 50 `HttpClientTestingModule` substituem o backend e teste nenhum toca o transporte real, deixando os 9 `blob`, o `FormData` e os 3 `responseType` sem cobertura; (b) o OnPush virou o padrão de change detection — a migration `change-detection-eager` marcou 35 componentes com `Eager`, e somados aos 15 que já usavam OnPush os 47 componentes ficaram com estratégia explícita, nenhum implícito; (c) `?.` em template passou a devolver `undefined` em vez de `null` — a migration `safe-optional-chaining` não alterou nada apesar das 287 ocorrências, e por isso a supressão de `nullishCoalescingNotNullable`/`optionalChainNotNullable` que ela escreveu nos tsconfig foi descartada: existe para acomodar o que a migration reescreve, e aqui ela não reescreveu nada. Duas regras do angular-eslint foram desligadas com o motivo no `eslint.config.js`, ambas por serem refactors próprios e não parte de uma troca de versão: `prefer-inject` (188 ocorrências; três specs instanciam componentes com `new Component(dep, ...)` e quebrariam) e `prefer-on-push-component-change-detection` (que acusa exatamente as 35 marcações `Eager` que o próprio Angular acabou de adicionar). Limpezas: `@angular/platform-browser-dynamic` removido (zero referências no projeto) e `typescript-eslint` elevado a 8.65.0, cuja faixa `>=4.8.4 <6.1.0` cobre os três majores de uma vez — o pin anterior em 8.33.1 declarava `<5.9.0` e fazia o primeiro `ng update` abortar. `zone.js` fica em 0.15.1, que satisfaz o peer `~0.15.0 || ~0.16.0` do core 22, tornando o bump da PR #112 desnecessário. O `dependabot.yml` passou a agrupar toolchain e runtime do Angular juntos e a ignorar major desses pacotes: não é só questão de agrupar, já que o `ng update` roda migrations de código que bot nenhum executa — sem elas o resultado compila e quebra em runtime. Suíte estável em 1198 specs em todos os passos.

84. Estado vazio, e não faixa de erro, nas listagens de sessões e planos de tratamento quando a API responde `404` (issue #203). Abrir **Sessões** ou **Planos de Tratamento** de um paciente **inativo** exibia `Erro ao carregar sessões.` / `Erro ao carregar planos de tratamento.` em vez do `Nenhuma sessão cadastrada.` que já existia no template, enquanto as outras três abas do **mesmo** paciente (planos, pagamentos, aulas) abriam com a listagem vazia. A causa tem duas metades. A do backend é a inconsistência entre os endpoints `…/paciente/{id}`: `/sessoes` e `/planos-tratamento` validavam o paciente e tratavam **inativo como inexistente** (`404 {"erro":"Paciente não encontrado: 1"}`), enquanto `/planos`, `/pagamentos` e `/aulas` não validavam nada — `/planos/paciente/99999` devolvia `200 []` para paciente que não existe. A do frontend é o `error:` do `subscribe` colapsar qualquer falha numa mensagem única, sem olhar o status; como o estado vazio é renderizado sob `@if (!loading && sessoes.length === 0 && !erro)`, a faixa de erro o suprimia. Corrigida a metade do frontend: as duas listagens ganharam `catchError` que converte `404` em lista vazia (`of([])`) e repropaga os demais status via `throwError`, seguindo o precedente de `paciente-anamnese.component.ts` e `paciente-evolucao-sessao.component.ts` ("registro que ainda não existe não é erro"). O `catchError` fica **no componente, não no service**, por dois motivos: no service o comportamento valeria para todo consumidor, inclusive os que ainda não carregam o paciente antes de listar, e a segurança da decisão depende justamente desse pré-requisito — em ambas as telas `carregar()` faz `GET /api/pacientes/{id}` e só chama a listagem no `next:`, então quando o `404` da lista chega a existência do paciente **já foi confirmada** e ele só pode significar "sem registros"; paciente inexistente falha antes, com `Erro ao carregar dados do paciente.`, e continua falhando. O comentário em cada `catchError` registra esse pré-requisito, porque um refactor que paralelizasse as duas chamadas num `forkJoin` (como o da anamnese) derrubaria a garantia e faria paciente inexistente virar lista vazia. Fora de escopo, como previsto na issue: padronizar o contrato dos endpoints `…/paciente/{id}` no backend (a issue está rotulada `needs-backend`), a regra que impede paciente inativo de receber novo plano (`422`, intencional) e o tratamento de erro das demais listagens do prontuário, que já recebem `200`. O review do PR apontou um terceiro consumidor do mesmo contrato, que a issue não previa e que **não** se enquadrava no "fora de escopo" dela: `paciente-evolucao-list.component.ts` chama `SessaoService.listarPorPaciente` dentro de um `forkJoin` com `EvolucaoSessaoService.listarPorPaciente`, e o `forkJoin` colapsa qualquer falha num ramo só — com o `404` forçado, `/pacientes/1/evolucoes` exibia a faixa vermelha com a mensagem crua da API (`Paciente não encontrado: 1`, propagada por `extrairMensagemErro`) numa tela cujo breadcrumb e subtítulo mostram o nome do paciente, enquanto `/pacientes/1/sessoes` já exibia o estado vazio. Mesma resposta da API, duas telas se contradizendo. O `catchError` foi estendido ao membro `sessoes` do `forkJoin` (e só a ele: `/evolucoes-sessao/paciente/{id}` devolve `200 []` e não entra nesse contrato), com o mesmo pré-requisito — ali também `carregar()` resolve o paciente antes de `carregarEvolucoes()`. O resultado é melhor que o estado vazio: as evoluções continuam carregadas e caem no **ramo da evolução órfã**, que já existia para o registro clínico não sumir quando a sessão é excluída, então a linha do tempo segue exibindo os 9 registros do paciente 1 — sem data/tipo/profissional no cabeçalho, que vinham da sessão. Cobertura: 7 casos novos ao todo. Dois por listagem corrigida — `404` levando a lista vazia, `erro` nulo, `.empty-state` com o texto certo no DOM e ausência de `.alert-danger`; e `500` preservando a mensagem de erro sem renderizar o estado vazio —, mais três no histórico de evoluções: `404` das sessões sem faixa de erro e com as duas evoluções renderizadas como órfãs (`tipoSessao === null`), `404` com as duas listas vazias caindo em `Nenhuma evolução registrada.`, e `500` preservando a mensagem neutra. Todos os casos de `404` foram verificados contra os componentes sem a correção e falham (`Expected 'Erro ao carregar sessões.' to be null`, `Expected undefined to be 'Nenhuma sessão cadastrada.'`); os casos de erro que já existiam usam `new Error('fail')`, cujo `status` é `undefined`, e por isso continuam caindo no ramo de erro sem alteração. Validação manual com o backend local, perfil ADMIN, com uma constatação que vale registrar: **o backend em execução já não reproduz o `404`** — todos os pacientes inativos conferidos (1, 2, 5, 7, 8, 12, 13, 14, 16, 18, 20, 24, 25) respondem `200` nos dois endpoints, com lista cheia ou vazia conforme o caso, e o `404` ficou restrito a paciente inexistente. O sintoma original, portanto, não é mais observável contra este build, e o cenário do issue foi reproduzido de propósito por um `--proxy-config` temporário que reescrevia `/api/sessoes/paciente/{id}` e `/api/planos-tratamento/paciente/{id}` para o id inexistente `99999` (`404` real vindo do backend) mantendo `/api/pacientes/{id}` intacto: com ele, as duas telas do paciente 1 exibem o estado vazio e nenhuma faixa de erro. Um segundo proxy, reescrevendo para `/paciente/abc` (`400` do backend), confirmou que falha real continua exibindo `Erro ao carregar sessões.` / `Erro ao carregar planos de tratamento.`. Sem proxy: paciente inativo sem registros (5) exibe os dois estados vazios, paciente inativo com 9 sessões (1) lista normalmente e paciente inexistente (99999) exibe `Erro ao carregar dados do paciente.` nas duas telas. A correção permanece válida como defesa — o contrato do backend não é garantido pelo frontend e a issue segue aberta do lado da API.

85. Número de registro no conselho profissional no cadastro de profissionais, exibido em cada evolução da linha do tempo (issue #214, backend em FabioCarlesso/carlessopilatesapi#161). O prontuário registrava **o que** foi feito na sessão, mas não **quem** assinou com qual registro (CREFITO, CREF etc.), o que impedia o histórico de evoluções de servir como documentação clínica rastreável. O cadastro ganhou `numeroRegistro`: opcional, texto livre com `maxLength(30)` e sem máscara, porque o formato varia por conselho e nem todo profissional do estúdio tem um. O campo entra no `form-row` do telefone, aparece no detalhe do profissional com fallback `-` e — o ponto de atenção da issue — precisou ser listado **explicitamente** no `ProfissionalUpdateDTO` montado por `salvar()`, que enumera os campos um a um: omiti-lo ali faria o `PUT` limpar no servidor o valor de quem editasse qualquer outro campo. Do lado da leitura, o `EvolucaoSessaoResponseDTO` passou a trazer `profissionalId`/`profissionalNome`/`profissionalNumeroRegistro`, um **snapshot** gravado pelo backend a partir da sessão no momento do registro; eles ficam de fora do `Request`/`Update` porque o frontend só os consome — nunca os envia nem os recalcula a partir do cadastro atual. A linha do tempo passou a derivar `responsavel` em `descreverResponsavel(...)`: `<nome> — Nr. de Registro: <numero>` quando há os dois, só o nome quando não há registro, e a linha inteira some quando não há profissional nenhum. O snapshot tem precedência sobre `sessao.nomeProfissional` — o vínculo da sessão pode mudar depois que a evolução foi assinada, e o prontuário tem que continuar apontando quem de fato registrou. O nome da sessão fica como **retaguarda** de dois casos: a sessão `REALIZADA` que ainda não tem evolução (é quem vai registrar) e as evoluções anteriores ao snapshot no backend, que vêm com os três campos nulos e de outra forma perderiam a identificação que o cabeçalho já exibia desde a issue #205 — foi o que se confirmou no backend local, onde nenhuma das evoluções pré-existentes foi retro-preenchida. Cobertura: 14 casos novos — 6 no histórico (os três cenários de exibição, precedência do snapshot sobre a sessão, retaguarda da evolução sem snapshot e sessão sem evolução), 6 no formulário (válido sem valor, `maxlength` inválido com a mensagem no DOM, `numeroRegistro` no `POST`, valor carregado na edição, preservado no `PUT` sem tocar no campo e alterado no `PUT`) e 2 no detalhe (valor e fallback); o teste existente de controles do formulário passou a listar também o campo novo. A mensagem de `maxlength` só é alcançável por `setValue`: o `maxlength="30"` do template trunca a digitação em 30 caracteres, então na UI o validador funciona como defesa do valor que vier do servidor na edição, não como aviso ao usuário. Validação manual com o backend local, perfil ADMIN: profissional criado com `350544-F`, `PUT` de outro campo preservando o valor, os três cenários da linha do tempo lado a lado num mesmo paciente e, por fim, o número alterado no cadastro para `999999-F` com a evolução já registrada continuando a exibir `350544-F`. Fora de escopo, conforme a issue: máscara ou validação de formato por conselho, tornar o campo obrigatório, exibir o registro em anamnese/avaliações/reavaliação/plano de tratamento (telas que hoje não têm vínculo com profissional) e exportação em PDF dos acompanhamentos.

86. Calendário mensal/semanal das sessões e aulas do paciente (issue #119), em `/pacientes/:pacienteId/calendario`. As duas coleções só existiam em lista, o que responde "o que aconteceu" mas não **quando** — a distribuição na semana e no mês, os buracos entre atendimentos e a frequência efetiva contra a contratada no plano. É a terceira leitura do mesmo prontuário, depois da listagem de sessões e da linha do tempo de evoluções, e como elas é **somente leitura**: nenhum `POST`/`PUT`/`PATCH` sai da tela. A carga repete o padrão do histórico de evoluções — `GET /api/pacientes/{id}` primeiro e, só então, um `forkJoin` de `GET /api/sessoes/paciente/{pacienteId}` e `GET /api/aulas/paciente/{pacienteId}`, cada um com `catchError` que converte `404` em coleção vazia; o paciente já foi carregado antes do `forkJoin`, então `404` ali só pode significar "sem registros" (é o que a API devolve para paciente inativo, issue #203, e para paciente sem plano gerado), e sem essa conversão o `forkJoin` colapsaria o `404` na faixa de erro e o calendário contradiria a listagem de sessões diante da mesma resposta. A coleção é carregada **uma vez**: navegar entre períodos e alternar visão são recorte no cliente, sem requisição nova — garantido por teste que conta as chamadas depois de três navegações.

    Toda a lógica pura vive em `shared/utils/calendario.ts`, no mesmo espírito de `simetrografo.ts` e `metricas-posturais.ts`: aritmética de datas, mapeamento dos dois DTOs para um evento único e montagem da grade, tudo sem DOM e sem injeção, de modo que o spec asserte 42 células e a virada de mês sem TestBed. **Nenhuma data é construída com `new Date(iso)`** — o construtor interpreta `2026-05-20` como UTC e devolveria 19/05 em qualquer fuso a oeste de Greenwich, que é o do estúdio. As datas circulam como `yyyy-MM-dd` e só viram `Date` pelo construtor numérico (`new Date(ano, mes - 1, dia)`), que é local por definição, nas operações que exigem calendário de verdade (dia da semana, fim de mês). Pelo mesmo motivo os nomes de mês e de dia da semana são constantes locais, e não `DatePipe` com `LOCALE_ID`: o pipe obrigaria a registrar o locale em cada spec justamente para checar a montagem que os testes precisam fazer fora do TestBed. `somarMeses` limita o dia ao tamanho do mês de destino — sem isso, 31/01 + 1 mês estouraria para 03/03 pelo transbordo do `setMonth` e a navegação pularia fevereiro inteiro.

    A grade tem 7 colunas começando no domingo e usa **apenas as linhas de que o mês precisa** (`ceil((deslocamento + diasNoMes) / 7)`), e não 6 fixas: fevereiro de 2026 começa num domingo e cabe em 4, e uma quinta linha vazia só empurraria a agenda para fora da dobra. Os dias do mês vizinho que completam a primeira e a última semana entram esmaecidos, com `doPeriodo: false`, e ficam de fora da contagem anunciada — o total é do período, não da grade. O evento distingue **dois eixos ao mesmo tempo**, como a issue pede: a cor diz a situação (agendada em `--c-info-bg`/`--text-brand`, realizada em `--c-success-bg`/`--c-success-text`, cancelada em `--c-danger-bg`/`--c-danger-text`) e o preenchimento diz a origem (sessão sólida, aula contornada e tracejada), com a cancelada ainda riscada — cor sozinha não é sinal suficiente (WCAG 1.4.1), e o tracejado sobrevive à impressão em preto e branco. São as variantes `-text` porque `--c-success`/`--c-danger` também servem de fundo (`.btn-danger`) e por isso não são clareadas em `[data-theme="dark"]`; "agendada" usa `--text-brand`, o único token de marca com versão própria no tema escuro, já que não existe `--c-info-text`. A legenda reusa exatamente as mesmas classes dos chips, para que exemplo e evento real não possam divergir.

    A aula não tem status próprio na API — só o booleano `realizada` — e é traduzida para o vocabulário da sessão (`AGENDADA`/`REALIZADA`), de modo que a legenda tenha uma escala só; "aula cancelada" não entra na legenda porque não chega a existir. A aula também não tem horário (a API guarda apenas a data; o horário é o do plano e não vem no DTO), então fica com `horario: null` em vez de virar "00:00", que mentiria, e é ordenada **depois** das sessões marcadas do mesmo dia — no começo, pareceria a primeira atividade da manhã. O clique da sessão abre `/pacientes/:pacienteId/sessoes/:id/editar`; o da aula, que não tem tela individual, cai em `/aulas/paciente/:pacienteId`, onde a presença é confirmada.

    Abaixo de 768px a grade some e entra uma **agenda** com os dias do período que têm evento, em linhas de largura total: a coluna do mês mediria ~46px, sem espaço nem para o chip nem para o alvo de toque de 44px que o projeto exige em tela de toque, e rolar 42 dias vazios num telefone para achar os cinco que importam não ajuda ninguém. As duas leituras convivem no DOM, alternadas por media query, e derivam da mesma `grade.dias` — não há como uma mostrar um período e a outra, outro. Acessibilidade: `role="table"`/`row`/`columnheader`/`cell` na grade — e **não** `grid`, que a ARIA APG define como widget interativo com navegação por setas e roving tabindex: anunciar assim uma tabela somente leitura cujos links são alcançados por Tab prometeria ao leitor de tela uma interação que a tela não tem —, cabeçalho de coluna com o dia da semana por extenso no `aria-label` (a abreviação visível é `aria-hidden`), célula rotulada com a data por extenso e ", hoje" quando for o caso, chip com a frase completa do evento no `aria-label` (data, origem, tipo, horário, situação e profissional) já que na célula ele mostra só hora e tipo, setas de navegação nomeadas por `aria-label` que muda com a visão ("Mês anterior"/"Semana anterior"), alternador de visão com `aria-pressed` e um `role="status"` que anuncia período e contagem a cada navegação — sem ele a troca de período seria silenciosa para o leitor de tela. O dia corrente é marcado por barra superior (`box-shadow: inset`), e não por fundo colorido, que competiria com as cores de situação dos chips dentro da própria célula.

    Cobertura: 44 casos novos — 22 no utilitário (aritmética de datas incluindo o limite de fim de mês e as viradas de mês/ano, mapeamento de sessão e de aula com link e descrição, ordenação com a aula sem horário no fim do dia, grade mensal de 6 e de 4 linhas, título, marcação de hoje, posicionamento dos eventos por dia, contagem restrita ao período, semana de domingo a sábado inclusive atravessando o mês, e navegação por mês e por semana) e 22 no componente (carga das três chamadas, abertura no mês corrente, cabeçalho e 42 células, papéis ARIA da grade, contorno tracejado da aula e sólido da sessão por `getComputedStyle`, borda pintada na cor do texto e não na do fundo — com os dois tokens injetados na raiz da fixture, já que o `styles.scss` global não entra no TestBed —, situação fora da união omitida da lista de classes em vez de virar a classe literal `"undefined"`, distinção por origem e situação, `href` de sessão e de aula, `aria-label` do chip, marcação única de hoje, troca de visão preservando o dia âncora, navegação sem requisição nova, botão **Hoje**, região viva, agenda só com dias que têm evento, estado vazio, `404` nas duas listagens, erro nos demais status, falha ao carregar o paciente e identificador inválido). O relógio é fixado com `jasmine.clock().mockDate` apenas em volta da criação do componente — que resolve `hoje` na construção e carrega tudo de forma síncrona com os spies — e desinstalado logo depois, para não substituir os timers do resto da suíte. O SCSS do componente fica em 4,42 kB e emite o mesmo aviso de *budget* de `paciente-evolucao-list` (teto de aviso em 4 kB, de erro em 8 kB), sem quebrar o build. Fora de escopo, conforme a issue: a visão de calendário **geral do estúdio**, que depende de listagem global no backend.

87. Agenda geral do estúdio (issue #125), em `/agenda`. É a continuação direta do que a #119 deixou fora de escopo: aulas e sessões só podiam ser consultadas **por paciente**, e responder "o que acontece no estúdio esta semana?" exigia varrer paciente por paciente. A issue nasceu rotulada `needs-backend`, mas os dois endpoints globais já existiam quando ela foi implementada — `GET /aulas?inicio&fim` e `GET /sessoes?inicio&fim`, ambos com o período obrigatório, limitado a 92 dias e a 5000 registros, e com filtros opcionais de profissional, paciente, tipo e status. A tela é a única do projeto que **não** é por paciente e a primeira que escreve a partir de uma visão consolidada.

    A diferença de fundo em relação ao calendário do paciente é o que a coleção representa. Lá ela é o prontuário inteiro, carregado uma vez, e navegar é recorte no cliente; aqui ela é **do período**, e trocar de semana, trocar de dia ou alternar a visão buscam de novo — o estúdio inteiro num ano não caberia numa resposta só, e é justamente por isso que a API impõe o teto de 92 dias. Os **filtros**, ao contrário, são recorte no cliente sobre o que o período trouxe: não custam ida ao servidor a cada troca e, principalmente, permitem o filtro combinado "Aula", que nenhum par de parâmetros do endpoint expressa — a aula não tem tipo na API (só o booleano `realizada`), então "Pilates | Fisioterapia | Aula" é uma escala que só existe no cliente. Os parâmetros server-side de filtro ficaram sem uso pelo mesmo motivo: adotá-los para tipo e status obrigaria a manter dois regimes de filtragem na mesma tela.

    A `shared/utils/calendario.ts` foi estendida em vez de duplicada, e é o que garante que as duas telas mostrem o mesmo objeto do mesmo jeito. Entraram: a visão `diaria` (uma célula, navegação de um dia, título com o dia da semana por extenso — sem a grade de 7 colunas, é a única pista de onde o dia cai), `intervaloDoPeriodo`, que devolve as duas pontas visíveis **antes** de montar a grade, porque a busca depende delas, e os campos `paciente`/`pacienteId`/`profissionalId`/`tipo` no evento. O `mapearEventos` original perdeu o parâmetro `pacienteId`: ele só servia para montar os links e é redundante, já que os dois DTOs carregam o próprio `pacienteId` — a versão do estúdio precisa exatamente disso, e mantê-lo obrigaria a passar um id que não existe quando os pacientes são vários. As duas funções públicas (`mapearEventos` e `mapearEventosDoEstudio`) delegam aos mesmos construtores de evento, com um único parâmetro de diferença: no estúdio o **título do chip é o nome do paciente** — é o que a recepção procura na grade, já que o tipo se repete a tarde inteira — e o tipo desce para o rótulo da lista; no calendário do paciente o nome fica de fora, porque o cabeçalho da tela já o nomeia, e a frase acessível segue byte a byte idêntica à de antes.

    O detalhe do evento é um **painel na própria página**, e não um diálogo: a confirmação das ações já usa o `ConfirmarDialogComponent` global, e dois modais empilhados prenderiam o foco um dentro do outro. O painel recebe o foco ao abrir por um setter de `@ViewChild` (dispara quando o elemento entra no DOM) — sem isso o Tab seguinte continuaria de onde estava e o leitor de tela não anunciaria nada. As ações são as que a API permite: `PATCH /sessoes/{id}/realizar`, `PATCH /sessoes/{id}/cancelar` e `PATCH /aulas/{id}/realizar?profissionalId=`, este último com o select obrigatório quando a aula ainda não tem profissional atribuído, como já faz a listagem de aulas. A aula não tem cancelamento na API, e o botão simplesmente não existe para ela. Reagendar continua nas telas que já sabem fazê-lo, alcançadas por link do painel — a issue pede a ação a partir do evento, e duplicar o formulário de sessão dentro da agenda seria uma segunda fonte de verdade para a mesma edição.

    O erro de **ação** é separado do erro de **carga** (`erroAcao` vs. `erro`), e não por preciosismo: a faixa de erro esconde a grade, então um "Erro ao cancelar a sessão." no mesmo campo apagaria da tela uma agenda que continua perfeitamente válida. A falha ao carregar os profissionais não acende faixa nenhuma — sem eles ainda se lê a agenda, só o filtro fica vazio. A grade de 7 colunas vale na visão semanal e no desktop; abaixo de 768px e na visão diária vale a lista, que deriva das mesmas `grade.dias`. O link **Agenda** entrou na navbar entre Início e Pacientes, elevando para 6 os links da barra — o teste de layout da issue #219, que garante uma única linha a 1025px, foi atualizado e continua verde.

    Cobertura: 39 casos novos — 12 no utilitário (mapeamento do estúdio com título, paciente e link por DTO, ausência do nome no calendário do paciente, ordenação entre pacientes, visão diária com célula única/título/contagem, `intervaloDoPeriodo` nas três visões incluindo as células de preenchimento do mensal, e navegação por dia atravessando o mês), 2 nos services (o `GET` de cada listagem por período com os dois parâmetros) e 25 no componente (semana corrente pedida à API, cabeçalho e 7 células, título do chip pelo paciente, distinção por origem e situação, papéis ARIA, região viva, navegação e troca de visão refazendo a busca com o período certo, botão **Hoje**, os três filtros sem tocar a API, período vazio por filtro vs. por ausência, limpar filtros, erro de carga escondendo a grade mas não a barra de período, falha dos profissionais sem faixa de erro, abertura e fechamento do painel, realizar e cancelar com confirmação e recarga, ausência de cancelamento para aula, aula exigindo profissional e concluída com o escolhido, e ação falhando sem apagar a grade). As ações são disparadas por **clique nos botões**, não pelos métodos do componente: com `OnPush` quem marca a view suja é o evento de clique, e chamar o método direto testaria um estado que a tela não chegou a exibir. O SCSS fica em 6,73 kB e emite o aviso de *budget* (teto de aviso em 4 kB, de erro em 8 kB), como `paciente-calendario` e `paciente-evolucao-list` — a duplicação é da aparência do evento e da grade, que não pode virar CSS global sem quebrar os testes de `getComputedStyle` da #119, que dependem de o estilo ser do componente. Fora de escopo: reagendamento dentro da agenda, filtro por paciente (o servidor aceita, mas a busca global já resolve "onde está o fulano") e a visão mensal, que com todos os pacientes juntos não caberia numa célula.

A funcionalidade central de **gestão de pacientes** está operacional, incluindo filtros de busca, paginação server-side com tamanho de página configurável na listagem, anamnese clínica, avaliação fisioterapêutica, planos de tratamento, sessões de pilates/fisioterapia, evolução de sessão, calendário mensal/semanal de sessões e aulas e reavaliações periódicas vinculados ao paciente, e cobertura de testes unitários para o serviço e todos os componentes de página. A tela inicial `/` apresenta um dashboard protegido por autenticação com resumo consolidado de pacientes, profissionais, pagamentos e aulas do mês atual, consumindo o endpoint único `GET /dashboard/resumo`. A listagem de profissionais oferece filtros de busca por nome, e-mail, tipo de contrato, percentual por aula e status (ativos/inativos/todos), também usa paginação server-side, limita os botões visíveis a uma janela de 5 páginas, bloqueia navegação para páginas inválidas ou repetidas, sincroniza `currentPage` e `pageSize` com os metadados da API e recua automaticamente para a última página válida quando a página atual deixa de existir após inativação. As telas de planos reutilizam as constantes `TIPO_LABEL`, `FREQUENCIA_LABEL` e `DIAS_SEMANA_LABEL` exportadas por `src/app/core/models/plano.ts`, evitando duplicação de labels entre formulário, listagem e demais fluxos. A tela de aulas permite marcar uma aula como realizada somente após selecionar o profissional responsável, enviando esse vínculo para o backend, e exibe carregamento também enquanto resolve o pagamento inicial na rota de aulas por pagamento. A seção de relatórios já possui consulta de pagamento de profissional por período, com seleção de profissional ativo, validação de datas, resumo por pagamento, detalhamento por aula realizada e exportação em PDF/XLSX, além do relatório fiscal de emissão de NFSEs por competência com exportação CSV/XLSX. A seção administrativa em `/admin` está disponível como hub inicial protegido por `roleGuard(['ADMIN'])`, e a listagem `/admin/usuarios` já consome o `UsuarioAdminService` com paginação server-side, exibe nome, e-mail, perfil e status, oferece criar/editar/inativar/reativar/excluir com confirmação antes de ações destrutivas e bloqueia o usuário logado de inativar ou excluir a própria conta. O `UsuarioAdminService` fornece a camada de serviço tipada para os endpoints `/api/users`, cobrindo listagem paginada, busca por ID, busca do perfil próprio, criação, atualização, exclusão, ativação, inativação e consulta de roles disponíveis. A aplicação agora pode ser executada em container Docker. A autenticação via JWT está implementada com tela de login, guard de rotas, interceptor HTTP, logout explícito e redirecionamento para `/login` apenas quando uma resposta `401` indica token inválido ou expirado; demais `401` são propagados ao componente. O estilo global do Angular consome tokens do Design System Carlesso em `src/styles/_tokens.scss`, com estratégia de tema e densidade centralizada pelo `StylePreferencesService`. O dark mode está disponível para o usuário final: a alternância entre tema claro e escuro fica no menu de conta da navbar, a preferência é persistida em `localStorage` e o tema inicial considera a preferência salva, com `prefers-color-scheme` como fallback. Os componentes de listagem (`PacienteListComponent`, `ProfissionalListComponent`, `PlanoListComponent`, `PagamentoListComponent` e `AulaListComponent`), assim como o `DashboardComponent`, usam `ChangeDetectionStrategy.OnPush` e disparam `cdr.markForCheck()` após cada atualização assíncrona de estado, reduzindo verificações de change detection desnecessárias.

---

## Decisões Técnicas

### Angular 22 com Standalone Components
Todos os componentes são standalone (sem NgModules), seguindo a arquitetura moderna do Angular. Isso reduz boilerplate e melhora o tree-shaking. Os imports de cada componente usam apenas os pipes e diretivas necessários (`DatePipe`, `CurrencyPipe`, `RouterLink` etc.), evitando a importação completa do `CommonModule`. Desde a migração para o v22 (issue #208) os templates usam o control flow em bloco (`@if`/`@for`/`@switch`), então `NgIf`, `NgFor`, `NgClass` e `NgSwitch` não são mais importados por componente algum — o control flow é parte da linguagem de template e não precisa de import.

Duas escolhas do v22 ficaram explícitas no código, e nenhuma delas é estado temporário: `withXhr()` no `provideHttpClient` mantém o transporte HTTP em XHR em vez do `FetchBackend`, que virou o padrão; e 35 componentes carregam `ChangeDetectionStrategy.Eager`, porque o OnPush passou a ser o padrão. Ambas as APIs são suportadas e não estão deprecadas — o que o v22 deprecou foi `ChangeDetectionStrategy.Default`, justamente em favor do `Eager`, de modo que as marcações são a forma atual e não dívida a ser desfeita. Adotar OnPush nos 35 é otimização de performance (exige `markForCheck()` nos callbacks assíncronos, um componente por vez, como fez a issue #25 nas cinco listagens), não continuação da migração.

### Design System por tokens
Os protótipos do Design System foram preservados em `assets/` para consulta e validação visual. O frontend Angular importa os tokens por `src/styles/_tokens.scss`, mantendo nomes semânticos para cores, tipografia, espaçamento, raios, sombras e densidade. A camada de componentes deve preferir esses tokens a valores hexadecimais ou medidas avulsas.

A nomenclatura é fechada e não coincide com a de outros design systems: **`--bg-*`** para superfície, **`--text-*`** para texto, **`--border-*`** para borda, **`--sp-*`** para espaçamento e **`--r-*`** para raio. `--surface`, `--space-md`, `--radius-lg` e `--c-primary` são nomes plausíveis que simplesmente não existem aqui, e o CSS não reclama de nenhum deles: sem fallback a declaração vira inválida e a propriedade cai para o herdado/inicial; com fallback ela renderiza pelo literal e ignora `[data-theme="dark"]`. Como nem o compilador, nem o ESLint, nem o Karma enxergam esse erro, a validação é um passo próprio — `npm run lint:tokens` (`scripts/lint-tokens.mjs`), encadeado em `npm run lint` para valer também na máquina de quem desenvolve, exige que todo `var(--x)` de `src/` referencie um token de `_tokens.scss` ou uma variável declarada na pasta de quem o usa, conferindo o nome mesmo quando há fallback (issue #213).

### Tema e densidade
Tema e densidade são parametrizados por atributos no elemento raiz: `data-theme` alterna as paletas clara e escura, enquanto `data-density` ajusta `--row-h`, `--input-h`, `--btn-h`, `--btn-h-sm` e `--gutter`. O `StylePreferencesService` (`core/services/style-preferences.service.ts`) centraliza a resolução, aplicação e persistência dessas preferências.

No startup, o serviço resolve a preferência inicial nesta ordem: (1) preferência salva em `localStorage` na chave `carlesso.style-preferences`; (2) na ausência de preferência salva, o tema segue `prefers-color-scheme` do sistema operacional via `matchMedia`, com densidade `default`. O valor resolvido pelo fallback é apenas aplicado ao DOM, **não** é persistido — assim a preferência do sistema continua valendo até o usuário escolher explicitamente um tema. Os métodos `setTheme`, `setDensity`, `apply` e `toggleTheme` gravam a preferência resultante em `localStorage`; falhas de acesso ao storage (modo privado, quota) são silenciadas e a preferência permanece apenas em memória.

O dark mode em si é definido inteiramente pelos tokens em `[data-theme="dark"]` (`src/styles/_tokens.scss`, espelhado em `assets/tokens.css`), que sobrescrevem superfícies (`--bg-*`), texto (`--text-*`), bordas (`--border-*`), sombras e fundos funcionais. Não há paleta paralela nem cores hardcoded específicas de tema na camada de componentes.

### Controle de tema na interface
A alternância entre tema claro e escuro fica no **menu de conta** da navbar (`shared/components/menu-conta/`), ao lado de **Alterar senha** e **Sair** — até a issue #219 os três eram botões soltos em `.navbar-actions`, e juntos consumiam 367px da barra. A tela de login expõe o mesmo controle visual por meio de `.login-theme-toggle`, permitindo alternar o tema antes da autenticação. Ambos chamam `StylePreferencesService.toggleTheme()` e expõem `aria-pressed` (estado do tema escuro) e `aria-label` descritivo da ação. A navbar continua aparecendo apenas quando o usuário está autenticado; a tela de login respeita e também pode alterar o tema persistido ou resolvido por `prefers-color-scheme`.

### Lazy Loading em todas as rotas
Cada componente de página é carregado sob demanda via `loadComponent()`, otimizando o bundle inicial.

### Reactive Forms
Formulários construídos com `FormBuilder` e `FormGroup` para controle granular de validação e estado.

### Soft Delete (Inativação e Reativação)
O backend não remove pacientes fisicamente. Inativação usa `PATCH /pacientes/{id}/inativar` e reativação usa `PATCH /pacientes/{id}/ativar`. A listagem exibe apenas pacientes ativos; o detalhe exibe qualquer status e oferece o botão correto (Ativar ou Inativar) conforme o estado atual do paciente.

### CPF imutável após cadastro
Por regra de negócio, o CPF não pode ser alterado após o cadastro. O formulário de edição desabilita apenas o campo CPF. O e-mail pode ser atualizado via `PUT /pacientes/{id}` e é incluído no `PacienteUpdateDTO`.

### Anamnese do paciente
A tela `/pacientes/:pacienteId/anamnese` usa Reactive Forms para criar ou editar a anamnese principal do paciente. O componente valida o parâmetro `pacienteId` com `parseRouteNumberParam`, carrega a identificação do paciente por `PacienteService.buscar` e tenta carregar a anamnese via `GET /api/anamneses/paciente/{pacienteId}`. Retorno `404` nessa consulta indica ausência de anamnese e mantém o formulário em modo de cadastro. O salvamento usa `POST /api/anamneses` quando ainda não há registro e `PUT /api/anamneses/{id}` quando a anamnese já existe. Os campos `queixaPrincipal` e `objetivos` são obrigatórios e rejeitam conteúdo somente com espaços, refletindo o contrato `@NotBlank` do backend.

### Avaliação Fisioterapêutica do paciente
A tela `/pacientes/:pacienteId/avaliacao-fisioterapeutica` usa Reactive Forms para criar ou editar a avaliação fisioterapêutica do paciente. O componente valida o parâmetro `pacienteId` com `parseRouteNumberParam`, carrega a identificação do paciente por `PacienteService.buscar` e lista as avaliações via `GET /api/avaliacoes-fisioterapeuticas/paciente/{pacienteId}`. O backend retorna uma lista ordenada por `dataAvaliacao` e ID em ordem decrescente; a tela edita a avaliação mais recente quando há itens e mantém o formulário em modo de cadastro quando a lista vem vazia. O salvamento usa `POST /api/avaliacoes-fisioterapeuticas` com `pacienteId` quando ainda não há registro carregado e `PUT /api/avaliacoes-fisioterapeuticas/{id}` quando já existe. Os campos `dataAvaliacao`, `queixaFuncional`, `escalaDor` e `diagnosticoFisioterapeutico` são obrigatórios; `queixaFuncional` e `diagnosticoFisioterapeutico` rejeitam conteúdo somente com espaços, e `escalaDor` deve ficar entre 0 e 10. Demais campos — `avaliacaoPostural`, `mobilidadeArticular`, `forcaMuscular`, `flexibilidade`, `equilibrio`, `coordenacaoMotora`, `padraoRespiratorio`, `testesFuncionaisRealizados` e `observacoesGerais` — são opcionais.

### Sessões de Pilates/Fisioterapia do paciente
A tela `/pacientes/:pacienteId/sessoes` lista as sessões de pilates ou fisioterapia vinculadas ao paciente. O componente valida `pacienteId` com `parseRouteNumberParam`, carrega a identificação do paciente por `PacienteService.buscar` e consulta as sessões por `GET /api/sessoes/paciente/{pacienteId}`. A rota `/pacientes/:pacienteId/sessoes/nova` cria uma sessão por `POST /api/sessoes` com `pacienteId`; a rota `/pacientes/:pacienteId/sessoes/:id/editar` carrega a sessão por `GET /api/sessoes/{id}`, valida que `sessao.pacienteId` corresponde ao paciente da rota e salva alterações via `PUT /api/sessoes/{id}`. O `PUT` aplica somente `dataHora` (desmembrado em `data`/`horario`), `duracao` e `observacoes`; por isso, na edição, os campos **Tipo**, **Profissional** e **Status** são exibidos desabilitados, apenas como informação, cada um com um hint explicando por quê. Tipo e profissional são definidos no agendamento (`POST /api/sessoes`) e o status muda apenas pelas ações Realizar/Cancelar da listagem. A listagem permite marcar sessões agendadas como realizadas por `PATCH /api/sessoes/{id}/realizar` e cancelar sessões agendadas por `PATCH /api/sessoes/{id}/cancelar`, sempre com confirmação antes da chamada. Os campos de UI `dataHora`, `tipo` e `duracao` são obrigatórios; o serviço converte `dataHora` em `data` e `horario`, envia `duracao` como `duracaoMinutos` e normaliza a resposta da API de volta para o modelo usado pelos componentes. `duracao` deve ficar entre 1 e 480 minutos. O tipo pode ser `PILATES` ou `FISIOTERAPIA`. O status é exibido como `AGENDADA`, `REALIZADA` ou `CANCELADA` com labels centralizados em `SESSAO_STATUS_LABEL`.

### Evolução de Sessão do paciente
A tela `/pacientes/:pacienteId/sessoes/:sessaoId/evolucao` permite registrar e editar a evolução clínica de uma sessão específica de pilates ou fisioterapia. O componente valida `pacienteId` e `sessaoId` com `parseRouteNumberParam`, carrega o paciente por `PacienteService.buscar`, carrega a sessão por `SessaoService.buscar` e só consulta a evolução depois de validar que `sessao.pacienteId` corresponde ao paciente da rota. A evolução é carregada por `GET /api/evolucoes-sessao/sessao/{sessaoId}`; retorno `404` nessa consulta indica ausência de evolução e mantém o formulário em modo de cadastro. O salvamento usa `POST /api/evolucoes-sessao` com `sessaoId` quando ainda não há registro e `PUT /api/evolucoes-sessao/{id}` quando já existe. O campo `dataHoraRegistro` é obrigatório. Os campos `exerciciosRealizados`, `equipamentosUtilizados`, `cargasMolas`, `respostaPaciente`, `intercorrencias`, `orientacoes` e `observacoesFisioterapeuta` são opcionais. A ordem visual do formulário coloca **Observações do fisioterapeuta** logo depois de **Data/hora do registro**, ainda na seção **Registro**, por ser o campo mais usado no atendimento (#204); os demais seguem em Registro, Dor e Resposta e orientações, terminando em **Orientações fornecidas**. `dorAntes` e `dorDepois`, quando informados, devem ficar entre 0 e 10. A listagem de sessões exibe um botão **Evolução** para cada sessão, navegando para a rota de evolução correspondente.

### Histórico de Evoluções do paciente
A tela `/pacientes/:pacienteId/evolucoes` (`PacienteEvolucaoListComponent`) é a **segunda forma de leitura** das evoluções: uma linha do tempo somente leitura com todas as evoluções do paciente numa única página, da mais recente para a mais antiga. A tela de cadastro/edição por sessão continua sendo a única forma de escrever evolução — nenhum `POST`/`PUT` sai daqui. O componente valida `pacienteId` com `parseRouteNumberParam`, carrega a identificação do paciente por `PacienteService.buscar` e então dispara um `forkJoin` de `SessaoService.listarPorPaciente` (`GET /api/sessoes/paciente/{id}`) e `EvolucaoSessaoService.listarPorPaciente` (`GET /api/evolucoes-sessao/paciente/{id}`), cruzando as duas listas no cliente por `sessaoId`. As duas requisições existem porque o `EvolucaoSessaoResponseDTO` não traz data nem tipo da sessão (o profissional ele traz, como snapshot — ver adiante); desnormalizá-lo economizaria uma chamada, mas a tela deixaria de conhecer as sessões **sem** evolução. Entram na linha do tempo as sessões com evolução, as sessões `REALIZADA` ainda sem evolução (marcadas como **Sem evolução registrada**, com link para registrar) e — defensivamente — evoluções cuja sessão não veio na listagem, que de outra forma sumiriam do prontuário. Sessões `AGENDADA` e `CANCELADA` sem evolução ficam de fora. A ordenação é por data/hora da sessão em ordem decrescente, com desempate por `sessaoId`. O cabeçalho de cada card traz data/hora, tipo, o responsável pelo registro e a variação `dorAntes → dorDepois` com indicação de melhora/piora pelos tokens `--c-success-text`/`--c-danger-text`; o corpo mantém **Observações do fisioterapeuta** sempre visível (é o campo mais lido — ver #204, e na base real é o único preenchido) e os demais campos atrás de um botão com `aria-expanded`/`aria-controls`, com **Expandir tudo**/**Recolher tudo** no topo da lista. O par de dor expõe um `aria-label` próprio ("Dor antes 7, depois 3, melhora"), já que o `→` é decorativo, e o valor ausente cai para `—`. O painel de detalhes fica sempre no DOM quando há campos colapsáveis — escondido pela classe `.evolucao-detalhes-recolhido`, no mesmo padrão dos filtros de `paciente-list` — para que o `aria-controls` do botão sempre resolva. Campos vazios não são renderizados.

O responsável exibido no cabeçalho (issue #214) é derivado em `descreverResponsavel(...)` e sai do **snapshot da evolução** (`profissionalNome`/`profissionalNumeroRegistro`), no formato `Fulana de Tal — Nr. de Registro: 350544-F`; sem número de registro fica só o nome, e sem profissional nenhum a linha não é renderizada. O snapshot tem precedência sobre `sessao.nomeProfissional` porque o vínculo da sessão pode mudar depois que a evolução foi assinada, e o prontuário precisa continuar apontando quem de fato registrou. O nome da sessão permanece como retaguarda em dois casos: a sessão `REALIZADA` ainda sem evolução — é quem vai registrar — e as evoluções anteriores ao snapshot no backend, que vêm com os campos nulos e perderiam a identificação que o cabeçalho já exibia.

Os erros das duas cargas usam `extrairMensagemErro`, e a falha do `forkJoin` exibe uma mensagem neutra ("Não foi possível carregar o histórico de evoluções."), porque quem falhou pode ter sido a listagem de sessões. A lista derivada (`itens`) fica num único ponto do componente, para que o gráfico de dor e o recorte dos filtros de período/tipo consumam a mesma coleção.

Acima da linha do tempo, a tela desenha o **gráfico da evolução da dor** (#206): um SVG inline montado pela função pura `montarGraficoDor(itens)`, sem biblioteca de gráficos e sem requisição própria — ele deriva da mesma coleção `itens`, invertida, porque a lista é decrescente e o eixo X é cronológico crescente. O eixo Y é fixo em 0–10 (a escala do formulário de evolução), e não normalizado pelos valores presentes. O eixo X reserva uma posição para cada sessão da linha do tempo, inclusive as sem evolução, e escreve a data de no máximo cinco delas — sempre a primeira e a última, ancoradas para dentro do `viewBox`. Uma sessão com dor `null` **não** vira ponto em `0`: a série é quebrada em trechos, cada trecho contínuo de dois pontos ou mais vira um `<polyline>` e trechos de um ponto só ficam como círculo. O gráfico não é renderizado enquanto **nenhuma série chega a dois pontos** — o critério é por série, e não o total de sessões com dor: duas medições em séries diferentes não se ligam e renderizariam eixos e legenda em volta de dois pontos soltos. Séries sem nenhum ponto são descartadas antes do desenho, de modo que a legenda nunca anuncia uma linha que não existe. Nos dois casos a linha do tempo segue normal. Acima de 30 sessões os círculos deixam de ser desenhados, exceto os pontos isolados: a linha basta, e o resto viraria uma faixa sólida com centenas de nós no DOM. O `<svg>` inteiro é um `role="img"` com `aria-label` textual (número de sessões, período e a primeira e a última medição de cada série), com grade, eixos e rótulos internos em `aria-hidden`; as duas séries são nomeadas numa legenda e distinguidas por cor e por tracejado, com `--c-warning-text` (antes) e `--text-brand` (depois) — neutras de propósito, porque a série "depois" não é necessariamente a boa notícia. O responsivo vem de `viewBox` + `preserveAspectRatio` com `width: 100%` e `max-width: 480px`, sem gerar scroll horizontal. As observações usam `white-space: pre-wrap`: o texto vem da API em blocos de queixas e conduta, e o HTML colapsaria as quebras num parágrafo só. O card usa `--bg-elev` e `--text-brand`, e **não** os `--surface`/`--c-primary` das demais listagens do prontuário — esses dois tokens nunca foram definidos em `_tokens.scss`, de modo que lá o fundo do card não é pintado e a borda de destaque cai para `currentColor`. Paginação, filtros server-side, gráfico, exportação e edição inline ficaram fora de escopo.

No topo da tela, acima do gráfico, uma barra de **filtros de período e tipo de sessão** (#207) recorta o histórico: data inicial, data final e um select de tipo (todos / pilates / fisioterapia) alimentado por `SESSAO_TIPO_LABEL`. O recorte é **client-side** sobre a coleção já carregada — nenhuma requisição sai da tela ao alterar ou limpar filtros — e é calculado num único ponto, a função pura `filtrarItens(itensCarregados, filtro)`, cujo resultado (`itens`) alimenta ao mesmo tempo a linha do tempo e `montarGraficoDor`: por construção não há como o gráfico plotar uma sessão que a lista não mostra. A fonte carregada fica preservada em `itensCarregados` para o "limpar filtros" voltar a ela. A comparação de datas usa o prefixo ISO (`yyyy-MM-dd`) de `dataHora` contra os limites do `<input type="date">`, que já vêm nesse formato: a ordem lexicográfica coincide com a cronológica, o dia inteiro entra nas duas pontas e nenhum `Date` é construído — o que reintroduziria o deslocamento de fuso que `formatarData` já evita. O item da linha do tempo passou a carregar `tipoSessao` (o tipo cru) além do rótulo exibido, para o filtro não comparar strings traduzidas. Data final anterior à inicial é recorte impossível, não recorte vazio: a mensagem aparece no formulário (`role="alert"`, `aria-describedby` nos dois campos, `is-invalid` nas bordas) e o período **não** é aplicado; o filtro de tipo, independente, continua valendo — derrubá-lo junto surpreenderia quem só errou a data. A regra do gráfico passa a valer sobre o conjunto filtrado, então um recorte que deixa menos de dois pontos numa série esconde o desenho e mantém a linha do tempo. O recorte sem resultados tem estado vazio próprio, com ação **Limpar filtros** (issue #146), distinto do "Nenhuma evolução registrada" de paciente sem histórico. A barra usa o mesmo grid `auto-fit`/`minmax(180px, 1fr)` dos filtros de `paciente-list`, que empilha sozinho em 375px sem media query, e os `.form-control` globais; no mobile ela fica atrás do disclosure do issue #163, como nas outras duas listagens com filtro, e um `<p role="status">` permanente logo abaixo anuncia o tamanho do recorte para o leitor de tela. A única exceção ao padrão global é a cor da mensagem de erro: o `.invalid-feedback` usa `--c-danger`, que também serve de fundo (`.btn-danger`) e por isso não é clareado no tema escuro — sobre a superfície elevada do card de filtros rende 2,1:1 em 11px, então o `.filtro-erro` usa `--c-danger-text`, a variante criada exatamente para esse caso (idêntica no tema claro, 5,0:1 no escuro). Ficaram fora de escopo: filtros server-side, persistência do filtro na URL ou em `localStorage`, atalhos de período pré-definido e filtros por profissional, faixa de dor ou intercorrências.

### Plano de Tratamento do paciente
A tela `/pacientes/:pacienteId/plano-tratamento` lista os planos de tratamento vinculados ao paciente. O componente valida `pacienteId` com `parseRouteNumberParam`, carrega a identificação do paciente por `PacienteService.buscar` e consulta os planos por `GET /api/planos-tratamento/paciente/{pacienteId}`. A rota `/pacientes/:pacienteId/plano-tratamento/novo` cria um plano por `POST /api/planos-tratamento` com `pacienteId`; a rota `/pacientes/:pacienteId/plano-tratamento/:id/editar` carrega o plano por `GET /api/planos-tratamento/{id}`, valida que `plano.pacienteId` corresponde ao paciente da rota e salva alterações via `PUT /api/planos-tratamento/{id}`. A listagem permite suspender planos ativos por `PATCH /api/planos-tratamento/{id}/suspender` e encerrar planos ativos ou suspensos por `PATCH /api/planos-tratamento/{id}/encerrar`, sempre com confirmação antes da chamada. Os campos `dataInicio`, `objetivosTerapeuticos`, `frequenciaSemanal`, `condutasPropostas` e `exerciciosIndicados` são obrigatórios; os textos obrigatórios rejeitam valores somente com espaços e `frequenciaSemanal` deve ficar entre 1 e 7. O status é exibido como `ATIVO`, `ENCERRADO` ou `SUSPENSO` com labels centralizados em `PLANO_TRATAMENTO_STATUS_LABEL`.

### Reavaliações do paciente
A tela `/pacientes/:pacienteId/reavaliacoes` lista as reavaliações periódicas vinculadas ao paciente. O componente valida `pacienteId` com `parseRouteNumberParam`, carrega a identificação do paciente por `PacienteService.buscar` e consulta as reavaliações por `GET /api/reavaliacoes/paciente/{pacienteId}`. A rota `/pacientes/:pacienteId/reavaliacoes/nova` cria uma reavaliação por `POST /api/reavaliacoes` com `pacienteId`; a rota `/pacientes/:pacienteId/reavaliacoes/:id/editar` carrega a reavaliação por `GET /api/reavaliacoes/{id}`, valida que `reavaliacao.pacienteId` corresponde ao paciente da rota e salva alterações via `PUT /api/reavaliacoes/{id}`. O único campo obrigatório é `dataReavaliacao`; os demais — `comparativoAvaliacaoAnterior`, `evolucaoDor`, `evolucaoForca`, `evolucaoMobilidade`, `evolucaoFuncional`, `objetivosAlcancados`, `pontosAtencao`, `ajustesPlanoTratamento` e `observacoesGerais` — são opcionais. Após criação bem-sucedida o formulário navega para a listagem; após atualização exibe mensagem de sucesso e permanece na tela.

### NFSEs emitidas do paciente
A tela `/pacientes/:pacienteId/nfse-emitidas` (`PacienteNfseEmitidaListComponent`) lista as NFSEs emitidas persistidas para o paciente. O componente valida `pacienteId` com `parseRouteNumberParam`, carrega a identificação do paciente por `PacienteService.buscar` e consulta as notas por `GET /api/api/nfse-emitidas/paciente/{pacienteId}`. As notas são ordenadas por `dataEmissao` decrescente e a getter `ultimaNota` expõe a NFSE mais recente, exibida em destaque no topo da listagem. Pacientes sem nota registrada veem um estado vazio explícito, e falhas de carregamento exibem mensagem amigável.

As rotas `/pacientes/:pacienteId/nfse-emitidas/nova` e `/pacientes/:pacienteId/nfse-emitidas/:id/editar` (`PacienteNfseEmitidaFormComponent`) registram ou atualizam a NFSE emitida via `POST /api/api/nfse-emitidas` (o backend faz upsert por paciente/competência). Os campos obrigatórios são `competencia` (formato `MM/AAAA`, mês entre `01` e `12`) e `dataEmissao`; os opcionais `numeroNota` (máx. 60 caracteres), `valor` (≥ 0) e `observacoes` são sanitizados — strings vazias viram `null` e o valor é convertido com `Number.isFinite` para número ou `null`. Como o backend não expõe busca de NFSE por ID, o modo de edição localiza a nota pela listagem do paciente (`listarPorPaciente`), validando que o `id` pertence ao paciente da rota e pré-preenchendo o formulário (estado vazio/“não encontrada” tratado). Após sucesso o formulário navega para a listagem. Cada card da listagem oferece a ação **Editar**. O `NfseEmitidaService` usa o prefixo `/api/api/nfse-emitidas` pelo mesmo motivo do `RelatorioService`: o backend expõe o recurso em `/api/nfse-emitidas` e o proxy de desenvolvimento e o Nginx removem o primeiro prefixo `/api`. A tela de detalhe do paciente oferece o card **NFSEs Emitidas** para acesso rápido.

O tratamento de erro de ambas as telas usa o utilitário `extrairMensagemErro` (`shared/utils/api-error.ts`), que respeita o contrato de erro do backend: `401`/`403` exibem mensagem de sessão/autorização, `429` exibe mensagem de excesso de requisições, e demais status (`400`/`409`/`422`) usam `message`/`error`/`detail` do corpo ou a junção das mensagens de erro por campo, caindo numa mensagem genérica quando não há texto aproveitável.

### Atualização de profissionais via PUT
O cadastro de profissionais segue o contrato do backend para atualização via `PUT /profissionais/{id}` com `ProfissionalUpdateDTO`. Ativação e inativação continuam usando endpoints específicos com `PATCH`. O `salvar()` do formulário monta esse DTO **campo a campo**, e não a partir do `getRawValue()` inteiro: todo campo novo do cadastro precisa ser acrescentado ali explicitamente, senão o `PUT` limpa o valor no servidor quando o usuário edita qualquer outro campo. Em modo de edição apenas o `cpf` é desabilitado — por isso o componente usa `getRawValue()` para ler o formulário.

### Número de registro no conselho profissional
O cadastro do profissional tem `numeroRegistro` (ex.: `350544-F`, padrão CREFITO): **opcional**, texto livre com `Validators.maxLength(30)` e `maxlength="30"` no template, sem máscara nem validação de formato, porque o padrão varia por conselho (CREFITO, CREF etc.). O campo fica no `form-row` do telefone, aparece no detalhe do profissional com fallback `-` e integra `ProfissionalRequestDTO`, `ProfissionalUpdateDTO`, `ProfissionalResponseDTO` e `ProfissionalResumoDTO`.

O campo em branco vai no payload como **string vazia**, e não como `null` — o que parece descuido e não é. O `PUT /profissionais/{id}` deste backend dá significados distintos aos dois: `null` (ou o campo omitido) quer dizer "não altere" e **preserva** o valor no servidor, enquanto `''` ou uma string só com espaços **limpa** o registro. Sanitizar o vazio para `null` no `salvar()`, como se fazia no formulário de NFSE, tiraria do usuário a única forma de apagar um número digitado errado: ele limparia o campo, salvaria, e o valor antigo continuaria lá. Há um teste de regressão fixando esse comportamento (`should send an empty numeroRegistro when the field is cleared`). O mesmo backend normaliza `''` para `null` na leitura, então o campo apagado volta como `null` e o detalhe cai no fallback `-`.

Na evolução da sessão o registro chega como **snapshot**: `EvolucaoSessaoResponseDTO` traz `profissionalId`, `profissionalNome` e `profissionalNumeroRegistro`, gravados pelo backend a partir da sessão no momento do registro. Os três estão fora de `EvolucaoSessaoRequestDTO`/`EvolucaoSessaoUpdateDTO` de propósito: o frontend só os consome, nunca os envia nem os recalcula a partir do cadastro atual do profissional — alterar o número no cadastro **não** reescreve as evoluções já registradas.

O registro fica visível a **todos os perfis autenticados** pela linha do tempo, e não só a `ADMIN`: `/pacientes/:pacienteId/evolucoes` está sob `authGuard`, enquanto o cadastro em `/profissionais` exige `roleGuard(['ADMIN'])`. É intencional — a rastreabilidade clínica serve justamente a quem lê o prontuário, e o número do conselho é público —, mas é o primeiro dado do cadastro de profissional a cruzar essa fronteira, e vale saber disso antes de acrescentar outros campos ao snapshot.

### Agenda individual do profissional
A tela `/profissionais/:id/agenda` (`ProfissionalAgendaComponent`, issue #126) mostra a semana de um profissional: aulas e sessões do período, contadores e comissão. A issue previa dois endpoints novos no backend (`GET /sessoes/profissional/{id}` e `GET /aulas/profissional/{id}`), que **não foram necessários**: as listagens por período criadas para a agenda do estúdio (issue #125) já aceitam `profissionalId` como filtro opcional, e `SessaoService.listarPorPeriodo`/`AulaService.listarPorPeriodo` ganharam um terceiro parâmetro opcional para isso. O recorte fica no servidor — a agenda de uma pessoa não tem por que baixar o estúdio inteiro para descartar quase tudo no cliente — e a assinatura antiga continua valendo para a agenda do estúdio, que pede o período sem filtro.

A comissão sai de `ProfissionalService.relatorioPagamento` (`GET /profissionais/{id}/relatorio-pagamento`), e não de uma conta local com `percentualPagamentoAula` como a issue sugeria. O percentual incide sobre o valor da aula, que vem do pagamento (`valor / quantidade de aulas`) e **não** existe no `AulaResponseDTO`: qualquer estimativa local seria um segundo número sobre a mesma semana, divergente do relatório que o estúdio usa para pagar. O preço dessa escolha é que só entram aulas **realizadas** de pacientes ativos — não há valor previsto para a aula agendada —, e por isso o card declara a base do cálculo. A requisição fica fora do `forkJoin` da agenda de propósito: falha nela mostra `—` e mantém a semana na tela.

A tela é somente leitura: realizar/cancelar continuam na agenda do estúdio (que já tem o painel de ações) e nas telas do prontuário. Ela reaproveita `shared/utils/calendario.ts` inteiro — grade, aritmética de datas e mapeamento —, e o mapeador `mapearEventosDoEstudio` foi renomeado para `mapearEventosComPaciente` na mesma mudança: o que ele decide é se o nome do paciente identifica o evento, e isso agora vale para duas telas. Uma consequência do filtro por `profissionalId` é que a aula pendente **sem** profissional atribuído não aparece na agenda de ninguém; o estado vazio diz isso, para que a semana não distribuída não se leia como semana livre.

### Confirmação de aulas com profissional
A confirmação de presença em aulas usa `PATCH /aulas/{id}/realizar?profissionalId={id}`. O frontend carrega profissionais ativos via `GET /profissionais?page=0&size=100&sort=nome`, exige seleção antes de confirmar e exibe o profissional retornado em aulas já realizadas. Antes do `PATCH`, o `AulaListComponent` abre o `ConfirmarDialogComponent` compartilhado exibindo a data e o profissional selecionado, protegendo o vínculo (que não tem desfazer na UI) contra cliques acidentais, e mantém o estado `acaoEmAndamento` bindado no input `processando` para evitar duplo disparo. Se o usuário aciona **Marcar como Realizada** sem escolher profissional, o `select` da linha é destacado com `is-invalid`/`aria-invalid` e uma mensagem `invalid-feedback` em vez de a ação falhar em silêncio. Quando não há profissionais ativos, além de o botão ficar desabilitado, um `field-hint` orienta "Cadastre um profissional ativo para confirmar aulas". Cada `select` de profissional recebe `aria-label` descritivo com a data da aula ("Profissional responsável pela aula de {data}").

A listagem é responsiva ao celular (issue #155): a tabela fica dentro de um `div.table-wrap` (`overflow-x: auto`) para conter o scroll no card e, abaixo de 640px, uma media query no `aula-list.component.scss` a converte em cards por aula (um `tr` por card, `td` empilhados com rótulo via `data-label`/`::before`, `select` e botão de ação em largura total), evitando o vazamento horizontal da página em viewports de 375px/414px. É uma mudança apenas de template/CSS: os dados, o serviço e o fluxo de confirmação permanecem os mesmos.

### Remarcação de aula
A aula gerada pelo pagamento não tinha como trocar de dia (issue #131): as únicas operações eram listar, atribuir profissional e realizar, e uma aula que o paciente desmarcava ficava pendente para sempre no dia errado. A listagem ganhou a ação **Remarcar** ao lado de "Marcar como Realizada", só em aulas **não realizadas** — a API recusa remarcar aula concluída com `409`, e oferecer o botão ali seria prometer o que não existe.

A ação abre o `ConfirmarDialogComponent` compartilhado com um `input[type=date]` **pré-preenchido com a data atual da aula**: remarcar é quase sempre mover um ou dois dias, e partir do valor vigente poupa a digitação inteira. A única validação local é obrigatoriedade. Em particular **não** se exige data futura, diferente do reagendamento de sessão (`dataHoraFutura` em `paciente-sessao-list`): a API aceita data passada de propósito, para que a recepção registre reposições já ocorridas, e replicar aqui uma regra que o servidor não tem bloquearia um caso de uso legítimo. O diálogo usa `fecharAoClicarFora="false"`, como o de reagendar sessão, para um clique perdido não descartar o que foi digitado.

O erro passa por `extrairMensagemErro`, e não por uma mensagem fixa: o `409` traz o motivo real — `Aula já realizada não pode ser remarcada` ou `Paciente já possui aula na data 2026-08-17` —, que é justamente o que a recepção precisa para escolher outro dia; um "Erro ao remarcar aula." genérico esconderia a informação útil, que fica só como retaguarda. E, porque é para agir sobre ela ali mesmo, a recusa é exibida **dentro do diálogo**, que permanece aberto, e não na faixa do topo da página: fechar o diálogo jogaria a mensagem para longe do campo, e reabri-lo a apagaria (`solicitarRemarcar` limpa o erro e devolve o campo à data original) na hora exata em que ela é necessária. Trocar a data limpa a mensagem, que era sobre a data anterior.

A chave que carrega esse texto é `erro` (`{"erro": "..."}`), e ela **passou a ser explícita** em `extrairMensagemErro`, junto de `message`/`error`/`detail`. Antes chegava ao usuário pelo ramo final, que junta os valores string do objeto: funciona enquanto o corpo tem um único campo string, mas costuraria dois numa frase só, e é contrato de todas as telas — não só desta. O `200` recarrega a listagem em vez de trocar o item no array (como faz o reagendamento de sessão), porque a data é o critério de ordenação da lista e a aula remarcada muda de lugar; o `markForCheck` antes do recarregamento existe para o diálogo sair da tela na hora, e não só quando a nova listagem responde — o callback do HTTP não marca a view `OnPush` como suja.

Duas guardas locais fecham caminhos que pareciam impossíveis. O `input[type=date]` **não** garante data de 4 dígitos: o segmento de ano aceita até seis, e `20261-05-12` passaria por uma checagem de obrigatoriedade e voltaria como `400 Dados inválidos` — por isso a validação é contra `/^\d{4}-\d{2}-\d{2}$/`, e não contra string vazia. E confirmar **sem trocar a data** é o caminho de um Enter distraído, já que o diálogo abre com a data atual e o `ConfirmarDialogComponent` põe o foco no botão de confirmar: a API trata como no-op e responde `200`, mas o `PATCH` e o recarregamento seriam desperdício, então o diálogo apenas fecha. Campo recusado também **recebe o foco**: a mensagem está ligada a ele por `aria-describedby`, que só é lido quando ele tem foco, e sem isso quem usa teclado ou leitor de tela ficava no botão sem aviso nenhum.

Os quatro comportamentos acima foram verificados contra o backend local (perfil `dev`, admin de seed), e não só por teste com dublê: `200` remarcando para dia livre, `200` remarcando para **data passada**, `409` com `Paciente já possui aula na data 2026-08-17` ao escolher dia já ocupado, `409` com `Aula já realizada não pode ser remarcada` numa aula concluída, e `400` (`{"campos": {"horario": "horario ainda não é suportado…"}}`) ao mandar `horario` — a prova de que omitir o campo é obrigatório, e não zelo excessivo. A aula usada foi restaurada à data original ao fim.

O **horário** que a issue pedia junto ficou de fora, e não por escolha de escopo: `AulaResponseDTO` não tem o campo, e o `AulaRemarcacaoRequestDTO` já reserva um `horario` que hoje é recusado com `400` por `@Null` — a entidade só ganha hora na issue #106 da API, ainda aberta. Por isso `AulaService.remarcar` envia **apenas** `data` no corpo, sem sequer serializar `horario: null`, e há teste garantindo que o corpo tem essa única chave. Quando a #106 fechar, entram o campo na exibição das listagens/calendário e um `input[type=time]` neste mesmo diálogo.

### Relatório de pagamento de profissional
O relatório usa `GET /profissionais/{id}/relatorio-pagamento?inicio=YYYY-MM-DD&fim=YYYY-MM-DD`. A tela valida seleção de profissional, datas obrigatórias e impede consulta quando a data inicial é posterior à final. O retorno usa o contrato estruturado da API com `profissional`, `periodo`, `resumo`, `pagamentos`, `aulas` e `geradoEm`, consolidando total de aulas, total devido, agrupamento por pagamento e detalhamento por aula.

A exportação usa os endpoints `GET /profissionais/{id}/relatorio-pagamento/pdf?inicio=YYYY-MM-DD&fim=YYYY-MM-DD` e `GET /profissionais/{id}/relatorio-pagamento/xlsx?inicio=YYYY-MM-DD&fim=YYYY-MM-DD`. O frontend solicita a resposta como `Blob`, observa os headers HTTP, usa o nome retornado em `Content-Disposition` quando disponível e bloqueia os botões de exportação durante a geração do arquivo.

### Relatório de emissão de NFSEs
O relatório usa o endpoint do backend `GET /api/relatorios/nfse?competencia=MM/AAAA`, com filtro opcional `notaAnteriorEmitida=true|false`. Como o proxy de desenvolvimento e o Nginx removem o primeiro prefixo `/api` das chamadas do frontend, o `RelatorioService` chama `/api/api/relatorios/nfse` para encaminhar ao backend como `/api/relatorios/nfse`. A tela exige competência no formato `MM/AAAA`, valida mês entre `01` e `12`, exibe total de registros, soma de valores pagos e lista os campos `nome`, `cpfCnpj`, `valorPago`, `competencia`, `descricaoServico`, `notaAnteriorEmitida`, `dataPagamento` e `observacoes`. A partir da issue de backend #38, o campo `notaAnteriorEmitida` reflete a NFSE emitida persistida do paciente (ver **NFSEs emitidas do paciente**) em vez de uma inferência por pagamento anterior; o contrato do `RelatorioNfseResponseDTO` e o fluxo da tela permanecem inalterados.

A exportação usa o mesmo endpoint com `formato=CSV` ou `formato=XLSX`. O frontend trata a resposta como `Blob`, preserva o nome de arquivo retornado em `Content-Disposition` quando disponível e aplica fallback `relatorio-nfse-MM-AAAA.csv|xlsx`.

### Dashboard inicial
O dashboard inicial usa `GET /api/dashboard/resumo`, que o proxy local e o Nginx encaminham para `GET /dashboard/resumo` no backend. A tela exibe os indicadores principais em cards: pacientes ativos, profissionais ativos, receita confirmada do mês e aulas do mês atual. Também detalha pagamentos pendentes, pagos e vencidos, além de mostrar a proporção de aulas realizadas versus agendadas no mês corrente.

O retorno esperado segue o contrato `DashboardResumoDTO`, com grupos `pacientes`, `profissionais`, `pagamentos`, `aulas` e o timestamp `geradoEm`. O componente trata carregamento e falha de consulta com mensagens dedicadas, sem tentar reconstruir os indicadores por múltiplas chamadas de listagem.

### Bloqueios de agenda
Feriados, manutenções e eventos ganharam cadastro próprio em `/admin/bloqueios` (issue #135). A decisão que organiza tudo o mais é que o bloqueio é **informativo**: a API não impede a geração automática de aulas do pagamento nem o agendamento de sessões no período, e o frontend não inventa a regra que o backend não tem. A tela avisa — na agenda e no formulário de sessão — e a decisão continua com a recepção. A listagem diz isso em texto, porque "bloqueio" sugere impedimento e a leitura errada seria cara: alguém confiaria que cadastrar o feriado limpou a agenda.

O escopo da issue previa que a geração de aulas pulasse os dias bloqueados. Isso é regra de negócio do backend e ficou lá; do lado do frontend não há nada a fazer além de exibir o que a API decidir, e nenhuma tela daqui gera aula.

A **leitura** dos bloqueios é aberta a qualquer usuário autenticado, embora `POST`/`PUT`/`DELETE` exijam `ADMIN` — escolha consciente do backend, porque é a consulta que alimenta o aviso das telas usadas pela recepção. Por isso `BloqueioAgendaService` é consumido por três telas com perfis diferentes, e só o cadastro fica sob `roleGuard(['ADMIN'])`.

"Dia inteiro" existe **só no formulário**, não no contrato de escrita: a API deriva o dia inteiro do par `horaInicio`/`horaFim` (as duas ausentes significam dia inteiro; mandar apenas uma volta `400`). O checkbox desabilita e limpa a faixa em vez de apenas ignorá-la, porque controle desabilitado sai do cálculo de validade — é isso que deixa o `Validators.required` das horas conviver com o bloqueio de dia inteiro sem travar o salvamento. E como o `PUT` é substituição completa, marcar a opção na edição é também o que remove uma faixa cadastrada antes.

O horário volta da API como `HH:mm` **ou** `HH:mm:ss` — o Jackson só escreve os segundos quando eles não são zero —, então comparar a string crua faria `"08:00"` e `"08:00:00"` divergirem, e o `<input type="time">` só aceita `HH:mm`. Toda leitura passa por `normalizarHora` em `shared/utils/bloqueio-agenda.ts`, que também concentra a cobertura do dia, a sobreposição de horários e as frases de exibição, para que agenda, formulário de sessão e tela administrativa não descrevam o mesmo bloqueio de formas diferentes. O mesmo utilitário trata `diaInteiro` como derivado das horas, e não como fonte de verdade: um flag divergente esconderia a faixa da tela sem que ninguém percebesse.

Na agenda os bloqueios entram no `forkJoin` do período — para não dessincronizar dele — mas com `catchError` devolvendo lista vazia: a grade é a informação principal, e derrubá-la porque o feriado não carregou seria trocar o essencial pelo acessório. Eles não passam pelos filtros (que recortam eventos), não entram em `totalEventos` e não abrem o painel de detalhe. `diasComEventos` virou `diasDaLista` e passou a incluir o dia bloqueado sem eventos: na visão diária ele seria a única coisa a mostrar, e omiti-lo faria o feriado se confundir com dia livre. O índice `dia -> bloqueios` é montado uma vez por carga porque, com `OnPush`, filtrar a lista em cada célula refaria o trabalho a cada ciclo de detecção.

No formulário de sessão a consulta é **de um dia só**, disparada por `distinctUntilChanged` sobre o dia extraído de `dataHora`: a tela agenda uma sessão por vez, um `GET` por dia é mais barato que baixar o ano do estúdio, e sem o `distinct` cada digitação da hora custaria uma requisição. O aviso é recalculado no template, e não guardado num campo, para que mudar a **duração** — que estende a sessão até dentro da faixa bloqueada — o atualize sem nova ida à API. O critério é sobreposição de intervalos, e não o instante inicial: uma sessão das 07:00 às 09:00 invade um bloqueio das 08:00 às 12:00 mesmo começando antes dele. A comparação acontece no dia de início; sessão que atravessasse a meia-noite não é conferida contra o bloqueio do dia seguinte, caso que a duração máxima de 480 minutos e o horário do estúdio não produzem.

### Seção administrativa
A seção administrativa fica em `pages/admin/` e é acessada por `/admin`, protegida por `roleGuard(['ADMIN'])`. O `AdminHomeComponent` serve como hub inicial e expõe links para os fluxos administrativos disponíveis. As rotas de usuários (`/admin/usuarios`, `/admin/usuarios/novo`, `/admin/usuarios/:id/editar`) e de bloqueios de agenda (`/admin/bloqueios`, `/admin/bloqueios/novo`, `/admin/bloqueios/:id/editar`) também exigem `ADMIN`. A navbar exibe o link **Administração** apenas para usuários com perfil `ADMIN`. O `UsuarioFormComponent` valida o parâmetro `id` da rota com `parseRouteNumberParam` para alternar entre os modos de cadastro e edição e exibir **Identificador inválido.** quando o parâmetro não é um inteiro positivo seguro.

### Listagem administrativa de usuários
A tela `/admin/usuarios` (`UsuarioListComponent`) consome `UsuarioAdminService.listar` com paginação server-side, sincroniza `currentPage` e `pageSize` com os metadados de `Page<UsuarioAdminResponseDTO>` e renderiza no máximo 5 botões de página simultâneos (mesmo padrão da listagem de profissionais). A tabela exibe nome, e-mail, perfil (renderizado pelo label de `ROLE_OPTIONS`) e status (`Ativo`/`Inativo`/`—` quando o campo `active` não vier preenchido). O cabeçalho traz o botão **+ Novo Usuário** que navega para `/admin/usuarios/novo`; cada linha tem **Editar** (rota `/admin/usuarios/:id/editar`), **Inativar** ou **Reativar** (escolhido conforme o campo `active`, chamando `PATCH /api/users/{id}/inativar` ou `PATCH /api/users/{id}/ativar`) e **Excluir** (`DELETE /api/users/{id}`). Toda ação destrutiva (inativar, reativar, excluir) abre um diálogo de confirmação antes do disparo da requisição. Os botões de inativar/excluir ficam desabilitados para o próprio usuário logado (consultado via `AuthService.getCurrentUser()`), refletindo as regras de segurança do backend. A tela trata loading, mensagem de erro e estado vazio. Após inativar, reativar ou excluir o último item de uma página, o componente recua automaticamente para a página anterior válida.

### Service de usuários administrativos
O `UsuarioAdminService` centraliza todas as chamadas HTTP para `/api/users`. Os models em `core/models/usuario-admin.ts` definem `UsuarioAdminResponseDTO`, `UsuarioAdminCreateRequestDTO`, `UsuarioAdminUpdateRequestDTO`, `RoleOption` e `ROLE_OPTIONS`. O tipo `UserRole` é reutilizado de `auth.ts`; o tipo `Page<T>` é reutilizado de `paciente.ts`. O service segue o padrão dos demais services do projeto: `@Injectable({ providedIn: 'root' })`, injeção de `HttpClient`, URLs relativas `/api/*` e métodos em português (`listar`, `buscar`, `buscarPerfil`, `cadastrar`, `atualizar`, `excluir`, `ativar`, `inativar`, `listarRoles`, `alterarSenha`). O método `alterarSenha(dto: AlterarSenhaRequestDTO)` envia `PUT /api/users/me/senha` com o payload `{ senhaAtual, novaSenha, confirmacaoNovaSenha }` e é usado pelo `AlterarSenhaComponent`.

### Troca de senha do usuário autenticado
A tela `/perfil/alterar-senha` (`AlterarSenhaComponent`) permite que o usuário logado altere a própria senha. O componente é standalone, importa apenas `ReactiveFormsModule` e `RouterLink` e usa Reactive Forms com três controles obrigatórios: `senhaAtual`, `novaSenha` (mínimo de 8 caracteres) e `confirmacaoNovaSenha`. Dois validadores de grupo garantem que (1) `confirmacaoNovaSenha` coincida com `novaSenha` (erro `naoConfere`) e (2) `novaSenha` seja diferente de `senhaAtual` (erro `igualAtual`). Cada campo de senha tem um botão de toggle de visibilidade com `aria-pressed` e `aria-label` que alterna o `type` entre `password` e `text` sem afetar o valor do controle. O submit chama `UsuarioAdminService.alterarSenha`; respostas com código/campo explícito de `senhaAtual` exibem "Senha atual incorreta." e marcam o controle `senhaAtual` com erro `incorreta`; `401`/`403` sem esse sinal exibem mensagem de autorização/sessão; `400` propaga `err.error.message` quando disponível ou cai em "Dados inválidos para troca de senha."; outros erros usam "Erro ao alterar a senha. Tente novamente.". Após sucesso, o componente exibe mensagem de confirmação, limpa o formulário, chama `AuthService.clearSession()` e redireciona para `/login` (com um pequeno atraso para o usuário visualizar a mensagem). A flag `salvando` evita disparos duplicados. O link **Alterar senha** aparece na navbar (`AppComponent`) para todo usuário autenticado, ao lado dos botões de tema e de logout; em telas estreitas, as ações da navbar quebram linha para evitar overflow.

### Autorização por role
O `roleGuard` é uma factory function que recebe um array de `UserRole` permitidos e retorna um `CanActivateFn`. Ele verifica primeiro se o usuário está autenticado (token em `localStorage`); caso contrário redireciona para `/login`. Em seguida valida o role do usuário logado; quando o token existe mas `currentUser` está ausente ou inválido, a sessão local é limpa e o usuário volta para `/login`. Usuário autenticado sem o perfil exigido é redirecionado para `/403`. A tela `/403` (`ForbiddenComponent`) exibe código 403, mensagem orientativa e duas ações: link primário **Ir para o início** (`/`) e botão secundário **Voltar** que usa `Location.back()` quando há histórico do navegador (`window.history.length > 1`) e cai para `/` caso contrário, evitando que o usuário saia da SPA em deep links. Não acessa nenhum serviço externo. As rotas de `profissionais` e `relatorios` exigem `ADMIN`; as demais rotas continuam protegidas apenas pelo `authGuard`. Os links da navbar para `Profissionais` e `Relatórios`, além da ação de relatórios no dashboard, aparecem apenas para usuários administradores.

### Autenticação JWT
O frontend implementa autenticação stateless via JWT consumindo `POST /api/auth/login`. O retorno esperado contém `accessToken`, `tokenType` e o objeto `user` com os campos `id`, `name`, `email`, `role` e `active` opcional. O token é armazenado em `localStorage` pela `AuthService` sob a chave `accessToken`. Além do token, o `AuthService` armazena o objeto do usuário logado como JSON na chave `currentUser`. O método `getCurrentUser()` recupera esses dados tipados como `AuthenticatedUser`; retorna `null` quando não há sessão, o JSON armazenado está corrompido ou o formato não contém os campos obrigatórios. Os métodos `getCurrentUserRole()`, `isAdmin()` e `hasRole(role)` centralizam consultas de perfil. O `authInterceptor` (functional interceptor) injeta o header `Authorization: Bearer <token>` em todas as requisições, exceto no próprio endpoint de login. O interceptor só executa logout quando a resposta `401` indica explicitamente que o token é inválido ou expirou — via cabeçalho `WWW-Authenticate: Bearer error="invalid_token"` (ou `expired_token`) ou via corpo com `code` em `{TOKEN_EXPIRED, INVALID_TOKEN, TOKEN_INVALID}` — e somente quando há sessão ativa. Demais respostas `401` (por exemplo, rota inexistente que o Spring Security devolve como `401`, ou falhas pontuais do backend) são propagadas ao componente, que decide como exibir o erro, sem derrubar a sessão do usuário. O `authGuard` (functional guard) protege todas as rotas autenticadas e redireciona para `/login` quando não há token. O `AppComponent` exibe a navbar e o botão **Sair** apenas quando o usuário está autenticado, e exibe links administrativos apenas para `ADMIN`. O logout remove o token e os dados do usuário e redireciona para `/login`. O controle de acesso por perfil está implementado via `roleGuard` e a tela `/403` está disponível.

O `forbiddenInterceptor` complementa o `authInterceptor` tratando as respostas `403` da API (usuário autenticado, sem permissão para o recurso) sem derrubar a sessão e somente quando há sessão ativa — nos fluxos públicos (login, esqueci/redefinir senha) o `403` é propagado para a tela tratar: `403` em `GET` (carregamento de dados de uma tela) redireciona para `/403`; `403` nos demais métodos (ação pontual) exibe a mensagem padrão de acesso negado no banner global do `AppComponent`, alimentado pelo `NotificacaoService` e limpo a cada navegação, mantendo o usuário na tela. Requisições cujo `403` já é tratado pela própria tela marcam o `HttpContextToken` `TRATA_403_LOCALMENTE` para desligar o tratamento global e evitar mensagem duplicada.

### Proxy de desenvolvimento para CORS
O browser bloqueia requisições cross-origin de `localhost:4200` para `localhost:8080`. A solução adotada foi o proxy do Angular CLI: `proxy.conf.json` redireciona `/api/*` para `http://localhost:8080/*` no lado do servidor, eliminando o problema de CORS em desenvolvimento. O `PacienteService` usa a URL relativa `/api/pacientes`.

### Docker com Nginx
A imagem Docker usa build multi-stage: `node:22-alpine3.24` instala dependências e executa `npm run build`; `nginx:1.27-alpine3.21` serve o conteúdo de `dist/carlessopilatesfe/browser`. A tag do estágio de build carrega a versão do Alpine porque as tags antigas deixam de ser atualizadas — a `node:22-alpine3.21` anterior parou no Node 22.21.1, abaixo do piso declarado em `engines`.

O Nginx mantém o mesmo contrato de URL relativa do frontend:

- `/` serve a SPA com fallback para `index.html`
- `/api/*` é redirecionado para `${BACKEND_URL}/*`

O valor padrão de `BACKEND_URL` é `http://host.docker.internal:8080`, adequado para backend rodando na máquina host durante desenvolvimento local com Docker Compose. Em ambientes integrados, a variável deve apontar para o serviço real da API.

A configuração por `environment.ts` ainda não é necessária porque o frontend continua usando URLs relativas e o alvo da API é resolvido no proxy.

---

## Arquitetura da Aplicação

```
┌─────────────────────────────────────────────┐
│              Angular SPA (porta 4200)        │
│                                             │
│  ┌─────────┐   ┌──────────┐  ┌──────────┐  │
│  │  List   │   │   Form   │  │  Detail  │  │
│  │Component│   │Component │  │Component │  │
│  └────┬────┘   └────┬─────┘  └────┬─────┘  │
│       │             │              │        │
│       └─────────────┼──────────────┘        │
│                     │                       │
│           ┌─────────▼──────────┐            │
│           │  PacienteService   │            │
│           │  GET /api/pacientes │            │
│           └─────────┬──────────┘            │
│                     │                       │
│        ┌────────────▼───────────┐           │
│        │  Angular CLI Proxy     │           │
│        │  /api/* → :8080/*      │           │
│        └────────────┬───────────┘           │
└─────────────────────┼───────────────────────┘
                      │ HTTP (sem CORS)
              ┌───────▼────────┐
              │  Backend API   │
              │ localhost:8080  │
              └────────────────┘
```

---

## Módulos Futuros Previstos

| Módulo         | Descrição                                     |
|----------------|-----------------------------------------------|
| Autorização    | Refinamento de controle de acesso por perfil em novas rotas |
| Administração  | CRUD de usuários, atribuição de perfis e demais configurações administrativas |
| Aulas          | Cadastro de modalidades e horários            |
| Agendamentos   | Vínculo paciente ↔ aula ↔ data/hora           |
| Frequência     | Controle de presença por sessão               |
| Financeiro     | Planos, pagamentos e cobranças                |
| Relatórios     | Relatório de pagamento de profissional por período com exportação PDF/XLSX; relatório de emissão de NFSEs por competência com exportação CSV/XLSX; futuras métricas avançadas do estúdio |

---

## Convenções de Nomenclatura

| Item             | Convenção                            | Exemplo                        |
|------------------|--------------------------------------|--------------------------------|
| Componentes      | PascalCase + sufixo `Component`      | `PacienteFormComponent`        |
| Serviços         | PascalCase + sufixo `Service`        | `PacienteService`              |
| Interfaces/DTOs  | PascalCase + sufixo `DTO`            | `PacienteRequestDTO`           |
| Arquivos         | kebab-case                           | `paciente-form.component.ts`   |
| Variáveis        | camelCase                            | `pacienteId`, `totalPages`     |
| CSS Variables    | kebab-case com prefixo `--`          | `--primary`, `--text-light`    |
| Métodos          | verbos em português (camelCase)      | `carregar()`, `inativar()`     |

---

## Relacionamento com o Backend

A API segue o padrão REST. O frontend espera os seguintes contratos:

- `GET /pacientes?page=0&size=10&sort=nome` → `Page<PacienteResponseDTO>`
- `GET /pacientes?page=0&size=10&sort=nome&nome=&email=&cpf=&telefone=&ativo=true` → `Page<PacienteResponseDTO>`
- `GET /pacientes/{id}` → `PacienteResponseDTO`
- `POST /pacientes` (body: `PacienteRequestDTO`) → `PacienteResponseDTO`
- `PUT /pacientes/{id}` (body: `PacienteUpdateDTO`) → `PacienteResponseDTO`
- `PATCH /pacientes/{id}/inativar` → `204 No Content`
- `PATCH /pacientes/{id}/ativar` → `204 No Content`
- `POST /anamneses` (body: `AnamneseRequestDTO`) → `AnamneseResponseDTO`
- `GET /anamneses/{id}` → `AnamneseResponseDTO`
- `GET /anamneses/paciente/{pacienteId}` → `AnamneseResponseDTO`
- `PUT /anamneses/{id}` (body: `AnamneseUpdateDTO`) → `AnamneseResponseDTO`
- `POST /avaliacoes-fisioterapeuticas` (body: `AvaliacaoFisioterapeuticaRequestDTO`) → `AvaliacaoFisioterapeuticaResponseDTO`
- `GET /avaliacoes-fisioterapeuticas/paciente/{pacienteId}` → `AvaliacaoFisioterapeuticaResponseDTO[]`
- `PUT /avaliacoes-fisioterapeuticas/{id}` (body: `AvaliacaoFisioterapeuticaUpdateDTO`) → `AvaliacaoFisioterapeuticaResponseDTO`
- `GET /planos-tratamento/paciente/{pacienteId}` → `PlanoTratamentoResponseDTO[]`
- `GET /planos-tratamento/{id}` → `PlanoTratamentoResponseDTO`
- `POST /planos-tratamento` (body: `PlanoTratamentoRequestDTO`) → `PlanoTratamentoResponseDTO`
- `PUT /planos-tratamento/{id}` (body: `PlanoTratamentoUpdateDTO`) → `PlanoTratamentoResponseDTO`
- `PATCH /planos-tratamento/{id}/encerrar` → `PlanoTratamentoResponseDTO`
- `PATCH /planos-tratamento/{id}/suspender` → `PlanoTratamentoResponseDTO`
- `GET /sessoes?inicio=YYYY-MM-DD&fim=YYYY-MM-DD[&profissionalId=]` → `SessaoResponseDTO[]` (agenda do estúdio; período obrigatório, limitado a 92 dias e 5000 registros; `profissionalId` opcional recorta a agenda de um profissional — a API também aceita `pacienteId`, `tipo` e `status`, que o frontend ainda não usa)
- `GET /sessoes/paciente/{pacienteId}` → `SessaoResponseDTO[]`
- `GET /sessoes/{id}` → `SessaoResponseDTO`
- `POST /sessoes` (body: `SessaoRequestDTO`) → `SessaoResponseDTO`
- `PUT /sessoes/{id}` (body: `SessaoUpdateDTO`) → `SessaoResponseDTO`
- `PATCH /sessoes/{id}/realizar` → `SessaoResponseDTO`
- `PATCH /sessoes/{id}/cancelar` → `SessaoResponseDTO`
- `GET /aulas?inicio=YYYY-MM-DD&fim=YYYY-MM-DD[&profissionalId=]` → `AulaResponseDTO[]` (agenda do estúdio; mesmos limites de período do endpoint de sessões; `profissionalId` opcional recorta a agenda de um profissional — a API também aceita `pacienteId` e `realizada`, que o frontend ainda não usa)
- `GET /aulas/paciente/{pacienteId}` → `AulaResponseDTO[]`
- `GET /aulas/pagamento/{pagamentoId}` → `AulaResponseDTO[]`
- `PATCH /aulas/{id}/realizar?profissionalId={id}` → `AulaResponseDTO`
- `PATCH /aulas/{id}/remarcar` (body: `{ data: 'YYYY-MM-DD' }`) → `AulaResponseDTO` (só aula não realizada; a data pode estar no passado; o `horario` do contrato está reservado para a issue #106 da API e volta `400` com qualquer valor)
- `GET /evolucoes-sessao/sessao/{sessaoId}` → `EvolucaoSessaoResponseDTO`
- `POST /evolucoes-sessao` (body: `EvolucaoSessaoRequestDTO`) → `EvolucaoSessaoResponseDTO`
- `PUT /evolucoes-sessao/{id}` (body: `EvolucaoSessaoUpdateDTO`) → `EvolucaoSessaoResponseDTO`
- `GET /reavaliacoes/paciente/{pacienteId}` → `ReavaliacaoResponseDTO[]`
- `GET /reavaliacoes/{id}` → `ReavaliacaoResponseDTO`
- `POST /reavaliacoes` (body: `ReavaliacaoRequestDTO`) → `ReavaliacaoResponseDTO`
- `PUT /reavaliacoes/{id}` (body: `ReavaliacaoUpdateDTO`) → `ReavaliacaoResponseDTO`
- `GET /profissionais?page=0&size=10&sort=nome` → `Page<ProfissionalResponseDTO>`
- `GET /profissionais?page=0&size=10&sort=nome&nome=&email=&tipoContrato=&percentualPagamentoAula=&ativo=true` → `Page<ProfissionalResponseDTO>`
- `GET /profissionais/{id}` → `ProfissionalResponseDTO`
- `POST /profissionais` (body: `ProfissionalRequestDTO`) → `ProfissionalResponseDTO`
- `PUT /profissionais/{id}` (body: `ProfissionalUpdateDTO`) → `ProfissionalResponseDTO`
- `PATCH /profissionais/{id}/ativar` → `204 No Content`
- `PATCH /profissionais/{id}/inativar` → `204 No Content`
- `GET /profissionais/{id}/relatorio-pagamento?inicio=YYYY-MM-DD&fim=YYYY-MM-DD` → `ProfissionalPagamentoRelatorioDTO`
- `GET /profissionais/{id}/relatorio-pagamento/pdf?inicio=YYYY-MM-DD&fim=YYYY-MM-DD` → `application/pdf` com `Content-Disposition: attachment`
- `GET /profissionais/{id}/relatorio-pagamento/xlsx?inicio=YYYY-MM-DD&fim=YYYY-MM-DD` → `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` com `Content-Disposition: attachment`
- `GET /api/nfse-emitidas/paciente/{pacienteId}` → `NotaFiscalEmitidaResponseDTO[]` (frontend chama `/api/api/nfse-emitidas/paciente/{pacienteId}`)
- `POST /api/nfse-emitidas` (body: `NotaFiscalEmitidaRequestDTO`) → `NotaFiscalEmitidaResponseDTO` (frontend chama `/api/api/nfse-emitidas`)
- `GET /api/relatorios/nfse?competencia=MM/AAAA` → `RelatorioNfseResponseDTO[]`
- `GET /api/relatorios/nfse?competencia=MM/AAAA&notaAnteriorEmitida=false` → `RelatorioNfseResponseDTO[]`
- `GET /api/relatorios/nfse?competencia=MM/AAAA&formato=CSV` → `text/csv` com `Content-Disposition: attachment`
- `GET /api/relatorios/nfse?competencia=MM/AAAA&formato=XLSX` → `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` com `Content-Disposition: attachment`
- `GET /dashboard/resumo` → `DashboardResumoDTO`
- `PUT /api/users/me/senha` (body: `AlterarSenhaRequestDTO` com `senhaAtual`, `novaSenha`, `confirmacaoNovaSenha`) → `204 No Content`

Erros são tratados no subscribe via callback de erro, exibindo mensagem genérica ao usuário.

Rotas com parâmetros numéricos, como `id`, `pacienteId` e `pagamentoId`, usam validação explícita antes de acionar serviços. Apenas inteiros positivos seguros são aceitos. Quando o parâmetro está ausente ou em formato inválido, a tela exibe **Identificador inválido.** e interrompe o carregamento para não gerar URLs de API contendo `NaN`.

---

## Informações do Repositório

- **Repositório:** `carlessopilatesfe`
- **Branch principal:** `master`
- **Autor:** Fabio Carlesso
